# Memecoin Trader Status

Project owner: Berj
Domain: automated Solana meme coin trading
Default execution mode: paper trading

<!-- AUTO-MANAGED -->

## Key Config

- Project name: `memecoin-trader`
- Repo path: `/home/dev/projects/memecoin-trader`
- Python: `>=3.11`
- Settings file: `config/settings.yaml`
- Task prefix: `CT`

## Architecture

- `src/core/` defines shared config, database helpers, and Pydantic domain contracts, including conservative live-guardrail settings that stay inactive unless explicitly armed.
- `src/chain/` contains Solana/Jupiter/wallet integration placeholders plus a pre-live Jito block-engine scaffold for MEV-aware bundle request building and injectable submission testing.
- `src/risk/` contains token risk checks, aggregate scoring, funding and honeypot research helpers, signal-aware enrichment, RugCheck-assisted authority/holder/honeypot data, and parallelized provider prechecks. Strict checks retain `UNKNOWN` when evidence is absent; paper-only discovery relaxation is explicit `discovery_relaxed` research metadata, not strict approval. `paper_momentum.py` provides the explicit New Pairs-only paper evidence lane with a 90% top-holder cap and a caller-supplied rendered-UI-age fallback for otherwise unknown provider age; it never mutates strict/default/live assessments. Jupiter quote output is not accepted as pool liquidity.
- `src/execution/` defines the execution adapter contract plus paper and live adapter implementations. Its DexScreener price provider accepts only requested-mint / wSOL base-oriented, finite positive SOL prices; unsupported, inverted, USDC-quoted, and non-finite data degrades to unavailable. Paper PnL preserves unavailable entry/mark states and uses remaining-token cost basis for partial exits. `JupiterPriceProvider` wraps `JupiterClient.get_quote()` for entry pricing via Jupiter v6 API. The rest of the execution layer includes disabled-by-default live submission helpers, guardrails, preflight, execution-config validation, reconciliation, circuit breaker, readiness, guarded live exit, and guarded live buy; live buy records unpriced fills until actual fill data exists.
- `src/signals/` defines async signal-source contracts; `aggregator.py` safely starts/stops/polls sources, clusters same-mint signals by time window, boosts multi-source composites, now attaches candidate mode labels and mode-count diagnostics alongside bounded pump.fun weak-identity context for downstream ranking, while `modes.py` provides conservative launch-vs-migration candidate labeling helpers, `novelty.py`, `novelty_fixture.py`, `novelty_compare.py`, `novelty_quality.py`, and `helius_fresh_mint_diagnostic.py` provide caller-supplied novelty and fake-transport diagnostic helpers, and `whale_tracker.py`, `pump_fun.py`, `onchain.py`, and `twitter.py` provide Helius, PumpPortal, DexScreener, and Twitter/X-backed signal feeds. `whale_tracker.py` also carries 6 standalone signal-layer functions (`load_tracked_wallets`, `get_recent_wallet_transactions`, `enrich_wallet_pnl`, `check_fresh_coin_buys`, `get_whale_signal`, `detect_new_launchpad_tokens`) with Believe/Moonshot on-chain coverage constants.
- `src/strategy/` contains decision, portfolio, position, fixed-exit helpers, gated dynamic-exit runtime hooks for calibrated volume-decay exits, optional market-regime classification helpers for later risk tuning, a default-off decision metadata hook that can surface regime summaries without changing entry or sizing behavior, and a position-manager option for session-local paper evaluation that ignores previously persisted positions without changing live or default paper behavior.
- `src/monitoring/` contains lightweight health, alert, and dashboard helpers for runtime visibility. `alerts.py` now includes `send_imessage()` that appends to `/tmp/alerts_pending.log` for Mac-side iMessage dispatch.
- `src/cli.py` exposes operator commands including health/config inspection, `env-readiness`, and a bounded `paper-cycle` runtime that evaluates ranked aggregated opportunities from pump.fun, whale, on-chain, and Twitter sources, persists paper trades/positions, and prints a safe summary before terminating, with strict and discovery paper-only risk profiles, candidate mode counts and visible launch/migration/unknown diagnostics, safe `live-readiness`, guarded `live-exit`, guarded `live-buy`, and the `paper-soak` audit command, a bounded discovery-only weak-identity ranking penalty for pump.fun launch quality, safe show-config live-guardrail diagnostics, and a manual-only Grok social-check prompt export for the top discovery candidates using safe normalized fields only.
- `paper-migration-lifecycle` is a bounded paper-only operator path: it accepts only Pump.fun migration/graduation signals that pass the existing paper-minimum evidence gate and have a current requested-mint/wSOL DexScreener quote, requires `--confirm` before entry, then immediately re-marks and closes only the newly created position. It preserves existing strict/live policy and leaves a position open rather than inventing a close when the post-entry mark is unavailable.

- `docs/MICRO_LIVE_RUNBOOK.md`: guarded micro-live operator runbook documenting required env vars, pre-live checklist, exact command sequence, abort conditions, and post-live review checklist for executing a single tiny SOL smoke trade while keeping live trading disabled by default.
- `docs/POST_LIVE_REVIEW_TEMPLATE.md`: structured post-live review template for the first future guarded micro-live smoke test — documents test metadata, pre-live readiness state, trade details, wallet/position state, fees, circuit breaker, logs review, issues, go/no-go decision, and kill-switch confirmation.
- `docs/HELIUS_READINESS_PROVIDER_WIRING.md`: design report for wiring three real Helius-backed providers (`wallet_balance_lookup`, `transaction_simulator`, `wallet_holdings_lookup`) — proposed classes, env/config needs, safety constraints, fake-client test strategy, and implementation order starting with `transaction_simulator` (no wallet key required).
- `docs/MEMECOIN_DECISION_LEDGER_PATTERNS.md`: design-only constraints for a future `memecoin_decision` observability ledger, including narrow-tool access, SQLite compatibility, evidence provenance, and explicit non-interference with trading behavior.
- `scripts/analyze_historical_survivor_traits.py`: read-only analysis script that queries all 606 mints from price_snapshots, joins to earliest paper_decisions/trades/positions/live_candidate_observations evidence, and produces a feature matrix CSV + pattern report. Detached — no runtime, config, risk-policy, or trading-path changes.
- `scripts/collect_first_hour_paths.py`: detached read-only collection script that discovers up to 20 fresh Solana candidates via DexScreener token-profiles API, captures initial and follow-up DexScreener snapshots at ~1/5/15/30/60 minutes, and produces long-form snapshots CSV + summary CSV + report. Detached — no runtime, config, risk-policy, or trading-path changes.
- `docs/MEMECOIN_DECISION_LEDGER_SCHEMA.md`: design-only proposed `memecoin_decision` PostgreSQL schema, evidence/attribution invariants, narrow tool contracts, and phased diagnostic-first implementation boundary.
- `docs/CANDIDATE_WALLET_FIXTURE_TEMPLATE.md`: manual top-level JSON-array fixture template with separate UTC matched-window metadata, required Signal and whale-label fields, and the checker-before-comparator gate; it contains no collected data or runtime integration.
- `docs/WINDOW_1_FIXTURE_MANIFEST_TEMPLATE.md`: blank manual checklist for the two future window-1 fixture labels/paths, shared UTC metadata, required-field completeness, and complete-versus-stop gate result; it contains no fixture data or collection path.

- `.env.example`: expected environment variables for RPC, APIs, wallet, position caps, closed-by-default live arming guardrails (`LIVE_TRADING_ENABLED`, confirmation phrase, kill switch, and tiny live caps), the minimum live wallet balance reserve used by preflight, and conservative execution config for primary/backup RPC plus priority fee and Jito tip overrides.
- `pyproject.toml`: Python package metadata, dependencies, dev dependencies, pytest, and Ruff config.
- `config/settings.yaml`: risk, position, exit, execution, monitoring, and conservative live-guardrail defaults, including a default-off dynamic exit gate plus tiny live caps, minimum wallet reserve checks, and bounded priority-fee/Jito-tip settings that do not enable live trading by themselves.
- `config/wallets_to_track.yaml`: public sample wallet watchlist used for safe whale-tracker dry runs until real tracked wallets are configured locally.
- `config/tracked_wallets.json`: 50 curated smart-money wallets built from BONK/WIF/POPCAT early-buyer overlap via Helius transactions API — 38 wallets scored 3 (early on all three), 12 scored 2.
- `config/narrative_accounts.json`: 476 tracked Twitter/X handles for narrative monitoring — celebrities, crypto influencers, politicians, news outlets, exchanges, projects, meme coins, on-chain analysts, VCs, AI accounts, and more.
- `scripts/check_token.py`: smoke-check script for building a token model and running risk scoring.
- `scripts/run_mt438_momentum_trailing.py`: detached paper-only momentum collector that resolves an explicit New Pairs UI capture, creates up to three momentum-lane paper entries, records 60-second marks, and closes by hard stop, activated trailing stop, or 30-minute max hold before writing the lifecycle markdown and CSV log. Holds tuned exit parameters from MT-439: hard stop -20%; standard trail 8%; tightened trail +25%/5%.
- `scripts/run_mt444_momentum_trailing.py`: MT-444 variant with 15s marks (vs 60s) for faster-cadence comparison — same capture, exits, and sizing.
- `scripts/run_mt451_high_liquidity_5s.py`: MT-451 high-liquidity 5-second paper comparison runner. Fetches candidates from 3 DexScreener API sources (token-profiles, token-boosts top, token-boosts latest), filters by Solana/age<=1h/liq>$50K/cap>$7K/vol>$1K board criteria, applies paper safety gate, enters up to 3 candidates at 0.01 SOL each with 5-second mark interval and MT-444 exit parameters. Produces report, trade log CSV, and board capture JSON.
- `scripts/run_paper_loop.py`: self-contained three-layer paper trading loop that scans Profile B graduated coins via browser-pc, resolves mint addresses via DexScreener search, quotes entry prices via Jupiter, records paper entries, and monitors open positions with trailing stop (4% from peak) / take-profit (+60%) / hard stop (-10%) / time stop closes. Entry trades carry DexScreener entry metadata (mcap, volume, txns, liquidity, age, price change, FDV) captured by `fetch_entry_metadata()` and persisted via trades.metadata_json. All `try_enter` failure paths log at WARNING level for entry debugging. Standalone script — no CLI integration.
- `/home/dev/watchdog_memecoin.sh`: external three-minute watchdog for browser-pc plus Strategy A and Strategy B. In addition to process checks and restart behavior, it kills either strategy when its log mtime is over five minutes old so the restart path can recover a hung scan loop.
- `src/signals/grok_xsearch.py`: async Grok Responses API client for X-search mention counts with retry logic (exponential backoff for 429/5xx/timeout). Exports `count_unique_mentions` (delegates internally to `get_mentions_with_timestamps` for 0-5min temporal bucket) and `get_mentions_with_timestamps` (temporal bucketing: 0-5min, 5-15min, 15-60min, >60min post-launch). Reads `GROK_API_KEY` from environment (dotenv in caller scripts), requests `grok-4.3` with the `x_search` tool, extracts integers via keyword-aware regex with JSON-string fallback, and returns zeros with warnings on all failures.
- `src/signals/influencer_tracker.py`: async Grok x_search client for monitoring crypto influencer X accounts. `query_account(handle, hours)` searches `from:{handle}` and extracts `$TICKER` mentions, Solana mint addresses, timestamps, and post snippets via regex. `enrich_with_dexscreener(mentions)` adds live DexScreener market data (mcap, liquidity, volume_h1, age, chain). `run_once()` queries all 6 configured accounts (SolanaFloor, lookonchain, pumpdotfun, blknoiz06, 0xVonGogh, 973Meech) with 1.5s delay, deduplicates by ticker, enriches, and sorts by recency. Reads `GROK_API_KEY` from env via dotenv.
- `src/signals/narrative_tracker.py`: async Grok-backed narrative tracker that monitors up to 30 high-signal X accounts per cycle, extracts narrative keywords with stopword/stem filtering and cross-account scoring, then searches DexScreener for <30min old Solana coins matching those keywords with >$1K liquidity. Exports `run_once()` returning keywords, account_posts, and matching coins. `_query_single_account()` queries Grok x_search for individual accounts (5 concurrent via semaphore). `extract_narrative_keywords()` scores words by frequency, cross-account mentions (2-6 accounts = 2x boost), and memeability (3x boost for known meme terms). `find_matching_coins()` searches DexScreener top 15 keywords and deduplicates by mint.
- `scripts/run_strategy_b.py`: standalone paper-only Strategy B runtime that scans DexScreener new-pairs page via browser-pc (all Solana DEXs, 0-15min), resolves mint addresses via DexScreener search, screens through age/mcap/DexScreener txns/vol/buy-sell-ratio and RugCheck security gates (mint/freeze authority, top-10 holder concentration, creator holdings), requires a Grok X 0-5min mention threshold for a DexScreener-priced paper entry, and monitors paper positions every 30 seconds for fixed take-profit, hard-stop (-30%), or time-stop exits. Uses age-adjusted transaction threshold (`_age_adjusted_min_txns()`), age-aware holder concentration tiers (`_age_holder_tier()`), and non-blocking low_fees warning in paper mode. `screen_coin()` returns per-gate diagnostics dict with 11 gates. `scan_loop()` logs 13-field `Gates:` counters (total, age/mcap/txn/volume/vol_mcap/low_fees/buy_sell/rugcheck/holder/full_screen passes, entry_attempts, entered, main_blocker), catches and logs unexpected full-cycle exceptions before retrying, and expires `seen_mints` entries after one hour. Supports `--test` mode for single-cycle validation. Logs to stdout and `/tmp/strategy_b.log`; no CLI integration.
- `scripts/paper_results.py`: standalone human-readable paper-trading summary that resolves the runtime SQLite database, lists open and recently closed positions, obtains live DexScreener marks for open positions, and reports realized/unrealized SOL PnL. It shows an empty-state summary when the database is absent.
- `scripts/test_grok_lookback.py`: detached Grok lookback window test over 20 curated coins. Resolves mints via DexScreener search with age disambiguation. Queries 6 temporal windows per coin (1h/6h/12h/24h/72h/168h) via `get_mentions_with_timestamps` with 2s delay between calls. Prints results table and zero-threshold conclusion. Saves to `research/mt479_output/`. Detached — no runtime, config, risk-policy, or trading-path changes.
- `scripts/run_whale_tracker.py`: standalone periodic whale tracker that scans 50 tracked wallets every 60 seconds, checks Helius for recent buys, validates freshness via DexScreener (<15min age, >$1K liquidity), deduplicates by mint with merged whale counts, and enriches top-wallet PnL via Birdeye/Solscan fallback. Manual start; logs to `/tmp/whale_tracker.log`.
- `scripts/run_influencer_tracker.py`: standalone periodic influencer scanner. Runs `run_once()` from `src/signals/influencer_tracker.py` every 5 minutes, prints new tickers with mcap/age/vol, reports silent accounts. Logs to `/tmp/influencer_tracker.log`. Manual start; does not auto-start.
- `scripts/run_narrative_tracker.py`: periodic narrative tracker runner. Calls `run_once()` from `src/signals/narrative_tracker.py` every 5 minutes, prints trending keywords, per-account post excerpts, matching fresh Solana coins, and keywords with no matches. Tracks seen mints for new-match-only display. Logs to `/tmp/narrative_tracker.log`. Manual start only.
- `scripts/compare_discovery_apis.py`: detached read-only API comparison runner for GeckoTerminal, Birdeye, and Moralis Solana discovery endpoints. It loads API keys from local environment files without printing values, uses 15-second request timeouts and graceful error handling, and writes a markdown comparison plus sanitized result samples to the shared API-comparison folder; it does not touch runtime, trading, database, or configuration state.
- `scripts/research_winners_mentions.py`: detached research script that collects up to 50 winning Solana token candidates via DexScreener token-boosts + search API (with browser-pc fallback for gainers page), resolves mints via DexScreener token API, fetches launch timestamps and market-cap metadata, and queries Grok via `get_mentions_with_timestamps` for temporal mention data (0-5min/5-15min/15-60min/after-60min buckets). Saves to `research/mt477_output/winners_mentions.json`. Detached — no runtime, config, risk-policy, or trading-path changes.
- `scripts/research_losers_mentions.py`: detached research script that fetches up to 50 failed (non-graduated) PumpFun coins, gets launch timestamps from the PumpFun API, and queries Grok via `get_mentions_with_timestamps` for temporal mention data. Saves to `research/mt477_output/losers_mentions.json`. Detached — no runtime, config, risk-policy, or trading-path changes.
- `research/mt444_output/RESULTS.md`: MT-444 results summary comparing 15s vs 60s mark intervals.
- `research/mt449_output/RESULTS.md`: MT-449 high-liquidity 5s comparison report — zero candidates qualified from 44 checked.
- `scripts/backfill_price_snapshots.py`: detached script that queries all distinct mints from trades/positions/paper_decisions/live_candidate_observations, fetches DexScreener `/latest/dex/tokens/{mint}` for each in concurrent batches, and persists current price/volume/liquidity/FDV snapshots to the `price_snapshots` table.
- `src/chain/jito.py`: pre-live Jito block-engine scaffold that normalizes serialized transactions into bundle payloads and carries optional tip metadata. Its public `submit_bundle()` path now fails closed; only the guarded Jupiter live adapter calls the private injectable HTTP transport path after safety checks.
- `src/core/config.py`: YAML settings loader and Pydantic config models for risk, position, exit, execution, monitoring, and conservative live-guardrail defaults plus env overrides for tiny live caps.
- `src/execution/jupiter_live.py`: live Jupiter adapter boundary that still blocks direct live swaps and now runs every serialized submission through explicit live guardrails, live preflight, and live execution-config validation before any Jito/RPC path, with optional primary-to-backup RPC failover in fake/live-ready fixtures and fail-closed `guardrails`, `preflight`, or `config` diagnostics when setup is invalid.
- `src/execution/live_circuit_breaker.py`: stateful operational breaker that tracks consecutive RPC, simulation, and submission failures plus required health-check freshness, returns fail-closed diagnostics for live mode, and supports explicit reset for operator recovery.
- `src/execution/live_buy.py`: guarded micro-live buy helper that requires a fully ready live state, refuses oversized trade sizes or duplicate open positions, and only opens a local live-mode position after successful fake/live-ready submission.
- `src/execution/live_exit.py`: guarded sell-only live exit helper that requires a ready live status gate, an existing local position, an injected exit-transaction builder, and successful submission checks before closing a position, while refusing to open new positions.
- `src/execution/live_execution_config.py`: conservative helper that validates priority fee and Jito tip bounds, resolves primary/backup RPC configuration from settings or env, redacts RPC diagnostics down to host labels, and reports whether future live submission wiring is valid without exposing secrets.
- `src/execution/live_guardrails.py`: closed-by-default helper for live arming and cap config. `live_daily_caps.py` adds fail-closed per-day trade/loss enforcement through an injectable fresh state lookup; absence, stale, malformed, or exhausted state blocks submission.
- `src/execution/live_preflight.py`: closed-by-default helper that checks wallet SOL balance reserve and serialized transaction simulation through fakeable interfaces, blocks submission on low/unknown balance or simulation failure, and returns sanitized diagnostics without echoing transactions or secrets.
- `src/execution/env_readiness.py`: safe env/secrets visibility diagnostics reporting present/missing for 11 env vars (`HELIUS_API_KEY`, `TRADING_WALLET_PUBLIC_KEY`, `TRADING_WALLET_PRIVATE_KEY`, `LIVE_*`, `MAX_*`, `PRIMARY_RPC_URL`, `BACKUP_RPC_URL`) without revealing values, prefixes, suffixes, or partial secrets.
- `src/execution/helius_providers.py`: Helius-backed provider implementations — `HeliusTransactionSimulator` (simulateTransaction/getLatestBlockhash), `HeliusWalletBalanceLookup` (getBalance), `HeliusWalletHoldingsLookup` (getTokenAccountsByOwner). All use fakeable HTTP client, resolve RPC URL + wallet public key from env or `.env` without exposing secrets, and degrade to `not_ready` on missing config or unreachable RPC. Factories return None when config is absent so readiness gates can report `unavailable` vs `failed`.
- `src/execution/live_readiness.py`: diagnostic micro-live readiness gate that combines guardrails, execution config, preflight, position reconciliation, circuit breaker state, and health into a single `READY`/`NOT READY` report with actionable `recommended_env` hints and split position-manager vs wallet-holdings-lookup diagnostics; does not mutate state or enable live trading.
- `src/execution/position_reconciliation.py`: diagnostic reconciliation helper that compares persisted live-mode open positions against fakeable wallet token holdings, skips paper-mode positions, reports `no_live_positions_to_reconcile` when no live positions exist, flags wallet-only, local-only, and material balance mismatch cases for live positions, and fails closed on missing holdings data without mutating positions automatically.
- `src/risk/rugcheck.py`: read-only RugCheck client for `v1/tokens/{mint}/report` that normalizes authority flags, top-holder concentration, liquidity state, honeypot indicator, and provider risk score/level while degrading safely on provider failures or malformed responses.
- `src/risk/paper_momentum.py`: isolated New Pairs paper-only evidence gate that preserves paper-minimum authority, freeze, liquidity, age, and deferred-evidence rules while replacing only holder concentration acceptance with a finite 0-90% bound.
- `src/risk/funding_provider.py`: Helius-backed inbound SOL funding adapter that resolves `HELIUS_API_KEY` from env or local `.env`, fetches recent enhanced transactions through an injectable HTTP layer, normalizes only inbound native SOL transfers into `InboundTransfer` records, and degrades safely on missing credentials, non-200 responses, timeouts, and malformed payloads.
- `src/risk/honeypot_simulation.py`: standalone read-only sell-simulation adapter that accepts serialized sell payloads plus fake parsed instructions, routes through an injected Helius/Blowfish-style backend, normalizes pass/block outcomes, and flags Token-2022 transfer hooks or suspicious conditional sell-block hints without wallet or signing requirements.
- `src/risk/funding_analysis.py`: async funding-source analyzer that groups buyer wallets by recent inbound funder, computes bundled-buyer concentration metrics, and flags likely sybil or bundled launches while degrading gracefully when provider data is missing.
- `src/risk/scorer.py`: aggregate risk scoring plus signal-aware `TokenInfo` enrichment from raw pump.fun/on-chain payload fields, parallel RugCheck and funding-analysis prechecks, optional honeypot-simulation fallback when RugCheck leaves the honeypot outcome unknown and the signal provides sell-simulation input, discovery-mode age relaxation, and read-only holder lookups that only backfill concentration data when RugCheck does not already provide it.
- `src/cli.py`: Typer entrypoint for health/config commands plus the bounded `paper-cycle` runner over integrated aggregated signal sources, including strict/discovery paper-only risk profiles, aggregate source/rejection diagnostics for skipped opportunities, explicit paper-capacity diagnostics, visible `candidate_mode` labels and mode-count guidance in discovery output, `live-readiness`, guarded `live-exit`, guarded `live-buy`, `paper-soak` audit commands, `paper-pnl` (PnL summary with per-position detail, persisted paper fill-quality/confidence labeling, mark-coverage diagnostics, actionable hints, and `--marks {live,unavailable}` flag), `paper-close` (close by mint with optional `--price`/`--use-mark`, or `--all --confirm` bulk close, with low-confidence legacy preview labeling), and `paper-state` (read-only state plus `--legacy` listing and `--archive-legacy --confirm` exclusion workflow for legacy paper rows) commands, `PaperSoakAudit` dataclass and `run_paper_soak` helper that runs a bounded discovery-mode cycle then adds health/readiness/guardrail/circuit-breaker diagnostics into a consolidated audit report, an optional fresh evaluation-session mode that ignores previously persisted paper positions for one bounded run, a bounded discovery-only pump.fun weak-identity ranking penalty surfaced as `attn=score(-penalty)` and `pen=` diagnostics in candidate summaries, and a bounded copy-ready Grok social-check prompt export for the top five discovery candidates that does not influence decisions. The default paper-cycle adapter now wires DexScreener entry quotes into paper execution so persistent positions can become `priced_quote` when a quote is available. Position aging diagnostics (`_position_age_label` / `_close_suggestion`) surface `fresh`/`aging`/`stale`/`missing_mark`/`low_confidence_fill` labels in `paper-state`, `paper-report`, and `paper-pnl` outputs with informational `paper-close --preview` suggestions (1h fresh, 24h stale thresholds). `--persist-positions` flag on `paper-soak` enables reportable position persistence.
- `src/cli.py` command: `paper-rejected-provider-mark-coverage` is a bounded read-only report over persisted rejected snapshots. Its compact evidence summary directly shows replayable rows, provider successes, explicit failures, historical not-recorded rows, and later-outcome-eligible baselines before the exact provider-status and source/mode/blocker breakdown. It makes no provider calls or state changes.
- `src/signals/aggregator.py`: independent signal aggregator that safely polls registered sources, deduplicates same-mint signals inside a configurable time window, boosts cross-source composites, ranks opportunities, best-effort logs them to any compatible signals table or recorder, now surfaces aggregate-only Twitter credibility diagnostics, attaches deterministic `candidate_mode` labels plus mode-count diagnostics, and carries normalized pump.fun identity context for partial/sparse metadata and promotional weak-identity launch names without hard-rejecting them.
- `src/signals/modes.py`: conservative pure-logic candidate classifier that labels signal-like objects or aggregated opportunity payloads as `launch`, `migration`, or `unknown` using only existing source/type/payload context and degrading safely on malformed data.
- `src/signals/novelty.py`: pure in-memory `summarize_novelty()` helper over caller-supplied raw `Signal` records. It counts session-local novel/duplicate/invalid mint observations, source-level mix, optional existing whale labels, and per-mint source origins without acquiring signals or touching runtime, config, persistence, pricing, evaluation, execution, or live paths.
- `src/signals/novelty_fixture.py`: opt-in `summarize_json_signal_fixture()` helper that reads one explicit caller-supplied top-level JSON-array fixture, validates records as `Signal`, and delegates to `summarize_novelty()` without default paths, source acquisition, output persistence, or runtime integration.
- `src/signals/novelty_compare.py`: `compare_json_signal_fixtures()` requires at least two nonblank caller labels mapped to explicit JSON fixture paths, delegates each path to the fixture helper, and returns ordered per-label novelty summaries plus a valid-observation duplicate rate without selecting a winner or persisting output.
- `src/signals/novelty_quality.py`: `check_json_signal_fixtures()` validates explicit fixture JSON and caller-supplied declared time windows, returning `complete`, `incomplete`, `malformed`, `unmatched_window`, or `inconclusive` states with reason labels and optional in-memory novelty summaries; it does not acquire data or persist checks.
- `src/signals/helius_fresh_mint_diagnostic.py`: fake-transport-only scaffold with a contract identifier and hard five-record cap. It reduces injected `{mint: ...}` records into sanitized aggregate counts and never resolves keys, constructs an HTTP client, calls a provider, or registers a runtime source.
- `src/signals/onchain.py`: DexScreener-backed read-only on-chain monitor that discovers candidate mints from token profiles, fetches Solana pair snapshots, scores volume spikes, buy/sell momentum, liquidity changes, and recent-pool activity, carries valid pair creation time as conservative age provenance, then deduplicates emitted mints within a five-minute poll window.
- `src/signals/dexscreener_new_pairs.py`: explicit UI-capture loader for paper-only New Pairs workflows. It parses rendered row ages, resolves only caller-supplied Solana pair IDs through the public pair endpoint, and emits read-only signals carrying UI-age provenance; it does not scrape browser credentials or alter generic signal sources.
- `src/signals/pump_fun.py`: websocket-first pump.fun monitor with verified PumpPortal `subscribeNewToken` and `subscribeMigration` subscriptions plus v3 HTTP fallback over `/coins` and `/coins/currently-live`, and a conservative source-layer junk filter that now skips missing-identity, URL-like, and obvious placeholder-symbol launches before they reach discovery review.
- `src/signals/twitter.py`: Twitter/X monitor that uses recent search when `TWITTER_BEARER_TOKEN` is present, safely degrades without configured credentials, extracts `$TICKER` and Solana mint mentions, deduplicates posts by ID, scores author credibility from follower/verified/account-age/spam/duplicate signals, and emits deterministic credibility-weighted `Signal` objects only when a mint address is present.
- `src/signals/whale_tracker.py`: Helius enhanced-transactions poller that reads `HELIUS_API_KEY` from environment or local `.env`, includes token-account activity, deduplicates signatures, and emits whale-buy `Signal` objects.
- `src/strategy/decision_engine.py`: async risk-gated buy evaluation plus open-position exit scanning and sell execution, with structured rejection reasons for paper-cycle diagnostics, opt-in liquidity-aware position caps that preserve flat sizing by default, and a default-off market-regime metadata hook that can attach regime summaries to paper decisions without changing trade selection.
- `src/strategy/dynamic_exits.py`: helper-only dynamic exit calibration module with deterministic checks for volume-decay exits, trail-start eligibility, liquidity-drop emergencies, and aggregate reason-label summaries used by the exit runtime when enabled.
- `src/strategy/market_regime.py`: deterministic market-regime classifier that labels aggregate meme-market conditions as `hot`, `normal`, `thin`, `risky`, or `unknown` from pool/liquidity/flow/signal/trade-success inputs and returns conservative adjustment hints without wiring them into runtime behavior.
- `src/strategy/position_sizing.py`: helper-only liquidity tier mapping that classifies pool liquidity into conservative max-position caps and skip conditions for decision-engine use.
- `src/strategy/position_manager.py`: async open-position persistence, exposure tracking, partial exits, and close handling. Position caches and lookup/close APIs are mode-aware (`paper`/`live`) so same-mint rows cannot overwrite each other; paper-cycle capacity/exposure uses paper rows only.
- `src/strategy/exits.py`: take-profit ladder, stop-loss, time-stop, liquidity, emergency exit evaluation, and a default-off `DynamicExitState` hook for calibrated dynamic-exit checks with testable reason labels.
- `src/monitoring/health.py`: process-level health probe plus the dashboard-compatible `HealthMonitor` shim.
- `src/monitoring/dashboard.py`: Rich terminal dashboard that reads the canonical runtime DB path, recent trades/positions, and monitoring health.
- `tests/`: smoke tests for risk contracts, signal aggregation, paper execution, whale tracker polling behavior, and pump.fun normalization/deduplication.
- `tests/test_jito.py`: focused coverage for Jito bundle payload construction, bundle-id parsing, graceful degradation on non-200, timeout, malformed JSON, explicit confirmation that no wallet/private-key or real network call is required, plus closed-by-default live-adapter guardrail blocking, wallet-balance and simulation preflight blocking, fully armed fake-provider submission behavior, and sanitized diagnostics that do not echo confirmation phrases or serialized transactions.
- `tests/test_env_readiness.py`: focused coverage for safe env-readiness diagnostics, all vars missing by default, all vars present when set, CLI output containing no secret values/fragments, lines format, and empty/whitespace value detection.
- `tests/test_live_execution_config.py`: focused coverage for safe default execution config, malformed and out-of-bounds priority fee rejection, and redacted show-config RPC diagnostics that expose host labels but not secrets.
- `tests/test_live_circuit_breaker.py`: focused coverage for below-threshold behavior, threshold trips, stale or missing health-check blocking, explicit reset, and no impact on paper mode.
- `tests/test_helius_providers.py`: focused coverage for `HeliusTransactionSimulator` — missing config returns unavailable, fake healthy/unhealthy/malformed RPC responses handled correctly, API key and raw tx never leak into output, private key not required, healthy simulator alone does not bypass guardrails, real transaction simulation paths, and safe close.
- `tests/test_live_readiness.py`: focused coverage for default `NOT READY` state, surfaced stale/unsafe readiness checks, fully fake-ready `READY` fixtures, three-state readiness distinction (intentionally unconfigured / configured-but-unhealthy / configured-and-healthy), split position-reconciliation diagnostics, recommended-env hints, guardrail-non-bypass verification, malformed/missing config fail-closed verification, and safe CLI readiness output without secret leakage.
- `tests/test_live_buy.py`: focused coverage for default blocked live buy behavior, oversized trade rejection, duplicate-position blocking, fully fake-ready buy success that opens a local live-mode position, and fail-closed CLI behavior by default.
- `tests/test_live_exit.py`: focused coverage for sell-only live exit blocking when no position exists, readiness-gate blocking, fully fake-ready exit success that closes an existing position, and fail-closed CLI behavior by default.
- `tests/test_position_reconciliation.py`: focused coverage for mode-aware position reconciliation — legacy positions default to paper, paper positions ignored by live wallet reconciliation, live positions included, local-only live positions still produce mismatch, matching live positions pass, mixed paper+live correctly filters, wallet-only holdings flagged with live positions, fail-closed behavior on missing wallet lookup data, and default mode remains paper.
- `tests/test_live_guardrails.py`: focused coverage for default fail-closed live guardrails, paper-mode non-arming, missing env enable, kill-switch blocking, tiny live max trade enforcement, malformed daily-cap rejection, fully armed pass cases, and safe show-config diagnostics without confirmation-phrase leakage.
- `tests/test_aggregator.py`: focused coverage for signal-source fan-out, same-mint clustering inside/outside the dedupe window, composite ranking, social-credibility diagnostic summaries, candidate-mode attachment/count diagnostics, pump.fun weak-identity context attachment, safe single-source passthrough, source-failure isolation, and optional SQLite signal logging.
- `tests/test_signal_modes.py`: focused coverage for launch-vs-migration classification, safe unknown fallback on non-pump or malformed inputs, composite raw-data migration detection, and proof that classification is pure and does not mutate signal scoring fields.
- `tests/test_novelty.py`: in-memory fixture coverage for raw-signal novelty counts, cross-source and whale-label origins, invalid mint/origin diagnostics, and input immutability.
- `tests/test_novelty_fixture.py`: temporary JSON-fixture coverage for file-fed novelty summaries, read-only fixture bytes, and invalid top-level/record shape rejection.
- `tests/test_novelty_compare.py`: temporary matched-fixture coverage for ordered labeled comparisons, summary/source/wallet-origin data, duplicate rates, fixture-byte immutability, and invalid labels or insufficient fixture count.
- `tests/test_novelty_quality.py`: temporary fixture coverage for complete, incomplete explicit timestamp, malformed JSON, unmatched declared windows, inconclusive empty samples, reason labels, and read-only fixture bytes.
- `tests/test_helius_fresh_mint_diagnostic.py`: fake/in-memory transport coverage for bounded aggregate mint counts, invalid records, duplicates, blank contracts, and sanitized provider-error state.
- `tests/test_cli_paper_cycle.py`: bounded paper-cycle coverage for accepted/rejected fake signals, aggregator integration, composite opportunities, non-fatal source degradation, paper-only enforcement, max-signal/timeout termination, SQLite persistence, visible launch/migration/unknown mode diagnostics, bounded pump.fun weak-identity ranking penalties, safe CLI summary output, and the bounded Grok prompt export.
- `tests/test_cli_summary.py`: stable summary-table structure coverage for paper-cycle output, including mode-count-aware summary construction.
- `tests/test_decision_ledger_prototype.py`: disabled ledger-contract coverage plus AST checks preventing runtime imports of the ledger prototype or synthetic test harness and runtime calls to the provider-coverage CLI command.
- `tests/test_dynamic_exits.py`: focused helper and runtime-gate coverage for volume-decay timing, liquidity emergency calibration, early trail-start signaling, aggregate reason labels, and preservation of existing behavior while dynamic exits stay disabled by default.
- `tests/test_market_regime.py`: focused coverage for deterministic `hot`, `risky`, `thin`, `unknown`, and `normal` classifications plus stable confidence, reason labels, adjustment hints, and no-runtime-side-effect guarantees.
- `tests/test_position_sizing.py`: focused helper coverage for liquidity tier matching, unknown/invalid liquidity skips, and custom tier overrides.
- `tests/test_e2e_paper.py`: offline signal-to-trade smoke covering decision-engine approval, paper execution, trade persistence, position persistence, and dashboard visibility on a temporary SQLite DB.
- `tests/test_funding_analysis.py`: focused coverage for bundled-buyer detection thresholds, diverse and unknown funding outcomes, empty input neutrality, provider-failure degradation, and timestamp window filtering without live network calls.
- `tests/test_funding_provider.py`: focused provider coverage for Helius inbound SOL normalization, missing-key and HTTP/provider failure degradation, malformed JSON handling, irrelevant transfer filtering, and explicit confirmation that fake fetchers prevent live network calls.
- `tests/test_honeypot_simulation.py`: focused adapter coverage for successful/blocked sell simulations, Token-2022 transfer-hook detection, suspicious conditional sell-block hint detection, timeout/provider degradation, malformed/missing-payload handling, and explicit confirmation that only fake providers are used.
- `tests/test_onchain_provider.py`: focused provider coverage for `OnChainMonitor` interface conformance, deterministic DexScreener scoring helpers, graceful provider degradation, mint-level dedupe, and explicit confirmation that no wallet/trading code is involved.
- `tests/test_pump_fun_provider.py`: focused provider-shape coverage for live-style pump.fun create payloads, graduation detection from migration fields, safe handling of websocket ack/malformed messages, and conservative rejection of obvious junk launch identity while preserving quirky but usable names.
- `tests/test_rugcheck.py`: focused client coverage for successful RugCheck normalization plus graceful handling of missing fields, non-200 responses, timeouts/provider errors, malformed JSON, and non-object payloads.
- `tests/test_paper_momentum.py`: focused coverage for the explicit 90% paper-only momentum lane, retained non-holder evidence checks, and non-interference with strict/default 50% behavior.
- `tests/test_twitter_provider.py`: focused provider coverage for Twitter/X payload normalization, no-key degradation, ticker/mint extraction helpers, credibility tiering, spam-resistant weighting, diversity sensitivity, mention-velocity scoring, and post-ID deduplication without live network calls.
- `tests/test_whale_tracker_provider.py`: focused provider tests for dotenv-based Helius key loading, token-account polling params, Helius-style buy normalization, dedupe, and safe empty/malformed handling.
- `tests/test_strategy.py`: focused coverage for decision-engine risk gating, opt-in liquidity sizing caps/fallbacks, paper sizing diagnostics, and exit-rule behavior.
- `tests/test_monitoring.py`: focused coverage for dashboard DB-path resolution, health compatibility, and one-shot dashboard rendering.

- `src/signals/influencer_tracker.py`: async Grok x_search client for monitoring 6 configured crypto influencer X accounts. Exports `query_account()` (mentions by handle), `enrich_with_dexscreener()` (live market data), and `run_once()` (full cycle). Reads `GROK_API_KEY` from env via dotenv.
- `scripts/run_influencer_tracker.py`: periodic influencer scan loop (every 5 min). Calls `run_once()`, prints new tickers with mcap/age/vol, reports silent accounts, logs to `/tmp/influencer_tracker.log`. Manual start only.
- `scripts/test_grok_lookback.py`: detached Grok lookback window test over 20 curated coins (10 newer, 10 older, 53min–8d). Resolves mints via DexScreener search with age disambiguation. Queries 6 temporal windows per coin (1h/6h/12h/24h/72h/168h) with 2s Grok delay. Prints results table and zero-threshold conclusion. Output: `research/mt479_output/grok_lookback_results.json`.
- `scripts/test_grok_time_operators.py`: detached Grok `since_time`/`until_time` operator test over 5 curated coins with known launch timestamps. Sends both `$TICKER` and mint-address queries with 5-minute launch windows. Prints per-coin verdict and saves raw Grok responses to `research/mt479_output/grok_time_operator_test.json`.
- `scripts/research_early_mentions.py`: detached winner vs loser early-mention research using `since_time`/`until_time` operators. Collects Solana coins from DexScreener search (2h–7d old), queries Grok for 5min and 15min post-launch mention windows, prints comparison table with avg/median/p90, and recommends MIN_MENTIONS threshold. Output: `research/mt484_output/winners.json` and `losers.json`.
- `scripts/build_whale_list.py`: Helius-backed smart-money wallet builder that collects early buyers of BONK/WIF/POPCAT via Helius transactions API, scores wallets by cross-coin overlap, enriches top 50 with recent trade stats, and saves to `config/tracked_wallets.json`. One-shot build script — run manually when wallet list needs regeneration.
- `scripts/grid_search_robust.py`: read-only exit parameter research (MT-502). Loads closed positions from `data/trades.db`, runs a finer grid (trailing 4-16%, TP 15-60%, hard stop 10-25%, skip trailing>=hard, 2.5% slippage on trailing exits) under walk-forward validation (400-train/200-test windows sliding by 200), ranks combos by average test PnL, prints sensitivity heatmaps for the top 3 combos, and saves full results to `data/grid_search_results.json`. Pure analysis — never writes to the DB.
- `src/strategy/gate_tuner.py`: holds mutable Strategy B gate thresholds, records an initial gate configuration, and applies bounded 20th-percentile winning-entry updates after each new 50-trade closed block.
- `scripts/run_strategy_b.py`: uses browser rows only as discovery hints, then resolves DexScreener search API pairs and filters on `pairCreatedAt` before screening. It records every API-aged candidate with gate diagnostics, persists Strategy B positions separately from Strategy A, records entry market snapshots, and invokes the in-process gate tuner after closes.

## Last 10 Changes
- 2026-08-05 Executed MT-509 - Strategy B top-10 holder gate set to 100% to bootstrap the MT-506 auto-tuner (`MAX_TOP10_HOLDER_PCT` in `scripts/run_strategy_b.py`; all four `HOLDER_TIERS` hard-reject values follow the constant). The gate check was also relaxed from strict `<` to inclusive `<=` because every fresh pump.fun token reports exactly 100.0% concentration - a 100.0 threshold with strict comparison still would have blocked all candidates. The auto-tuner will learn the real threshold once it has 50+ closed trades. Restarted the live runtime (PID 210368); gate log shows `holder=4` with no `top10=...>=...` fails - main blocker is now `txn_pass`, a market condition. Test updated to assert 100.0 with inclusive comparison; 4/4 pass.


- 2026-08-05 Executed MT-508 - Strategy B top-10 holder gate widened from 90% to 95% (`MAX_TOP10_HOLDER_PCT` in `scripts/run_strategy_b.py`; all four `HOLDER_TIERS` hard-reject values follow the constant). MT-507's 90% limit still blocked every candidate because fresh pump.fun tokens sit at 100% concentration; 95% keeps the safety check while letting the MT-506 auto-tuner tighten from real trade data. Restarted the live runtime; `top10=100.0%>=95.0%` confirms the new threshold is active. Current market candidates are exactly 100.0% concentrated, so no entry has passed yet — that is market conditions, not the gate. Test updated to assert 95.0 across all age tiers; 4/4 pass.

- 2026-08-05 Executed MT-507 - Strategy A pending confirmations now run as independent 45-second asyncio tasks instead of waiting for the next 180-second scan. Pending capacity is reserved before scheduling, preventing confirmations from oversubscribing the three-position cap. Strategy B's age-tiered top-10 holder hard gate is uniformly widened to 90%. Focused tests cover a 152-second simulated pending entry that still calls entry confirmation and all Strategy B age tiers at the 90% limit. Live restart confirmed a candidate reached CONFIRM at 45 seconds rather than stale_pending; it was then correctly blocked by the pre-existing Strategy A 80% holder safety gate at 99.4%. Both runtime scripts now suppress httpx INFO request logs so API-key query parameters are not written to process logs.

- 2026-08-05 Executed MT-506 - Strategy B source, telemetry, and tuning overhaul. DexScreener browser `maxAge` is no longer authoritative: the unfiltered New Pairs page supplies discovery hints, while `latest/dex/search?q={ticker}` resolves a Solana/wSOL pair and `pairCreatedAt` enforces the 30-minute live gate. Added `candidate_log` for every API-aged scan candidate and `gate_config` for startup/tuned threshold history. Positions now carry a backfilled strategy column; Strategy A defaults to A and Strategy B runs with isolated B positions. Both entry loops persist a complete nested DexScreener snapshot in trade metadata. Added an in-process GateTuner that starts after 50 B closes, uses 20th-percentile profitable entry data, and limits each threshold movement to 25%. Validation: live B test scan logged 6 candidates with realistic 5.5-29.5 minute ages, then the restarted runtime added another 6 candidate rows; initial gate config exists. Current live sample had zero full passes due to max-market-cap/holder gates, so it produced no B paper entry yet. Focused tests: 12 passed.

- 2026-08-05 Executed MT-505 — Fixed Chrome CDP routing for browser-pc. Root cause: the WSL relay `chrome-cdp-wsl-relay.service` (19222) targeted `172.21.32.1:9223`, which the Windows netsh portproxy (0.0.0.0:9223 → Mac 100.82.234.100 for Armada) intercepted and forwarded to the Mac; the local Windows Chrome had fallen back to `[::1]` only (IPv4 bind preempted by the 0.0.0.0 portproxy listeners) and was unreachable from WSL NAT. Every `/capture` since ~Aug 3 hung on the dead Mac route → 500s. Fix: started a dedicated MT Chrome on Windows (`--remote-debugging-port=9262 --user-data-dir=D:\chrome-mt-profile`), added netsh portproxy rule `0.0.0.0:19223 → 127.0.0.1:9262`, added Windows Firewall allow rule "MT Chrome CDP 19223" (the WSL Hyper-V firewall default-blocks new inbound ports; `NATInboundRuleNotApplicable`), and retargeted the systemd relay to `172.21.32.1:19223` (restarted via `systemctl --user restart chrome-cdp-wsl-relay.service`). Updated `/home/dev/watchdog_memecoin.sh` to health-check `172.21.32.1:19223` and restart Chrome with the MT flags. Deviations from the task spec (documented): `[::1]:9222` as relay target is impossible from WSL (NAT cannot reach Windows loopback) and port 9226 is portproxy-routed to Mac (9222-9261 all are), so 9262 + portproxy hop used instead. Validation: `curl http://127.0.0.1:19222/json/version` → Chrome 151 JSON; `localhost:8099/health` → ok; live `/capture` of dexscreener.com → 100 candidates; paper_loop scan cycles now log real candidates (13:42 EDT cycle: 6 structured candidates) instead of 500 errors. Portproxy rules 9222-9261 → Mac untouched; browser-pc UA override untouched; Strategy A/B loop params untouched.

- 2026-08-05 Executed MT-504 — Fixed browser-pc DexScreener rendering in `/home/dev/browser_service/server.py` by applying a Windows Chrome User-Agent through `Network.setUserAgentOverride` immediately after connecting to each CDP tab and before navigation. The override removes `HeadlessChrome`; CDP command IDs were updated to remain unique. Restarted only browser-pc. Validation: syntax compilation passed; live `/capture` of `https://dexscreener.com/new-pairs/solana` returned `success=true`, 100 DOM-extracted candidates, and non-empty rendered text in 10.9 seconds.
- 2026-08-03 Executed MT-503 — Applied optimized exit settings + entry metadata logging to Strategy A (`scripts/run_paper_loop.py`). Exit constants now TRAILING_STOP_PCT=0.04 (was 0.14), HARD_STOP_PCT=0.10 (was 0.20), take-profit threshold raised from +30% to +60% (`pct_from_entry >= 0.60`). Added `fetch_entry_metadata()` (optional injectable httpx client): queries DexScreener search API by mint and returns entry_mcap/entry_volume_24h/entry_volume_1h/entry_txns_24h/entry_txns_1h/entry_liquidity_usd/entry_age_hours/entry_price_change_1h/entry_price_change_5m/entry_fdv, merged into the BUY trade's metadata dict before `record_trade` (persists via trades.metadata_json); failures return {} and never block entry. entry_age_hours computed from pairCreatedAt (DexScreener API has no pairAge field). Added 4 MockTransport tests to `tests/test_paper_loop.py`. Validation: compileall, 640 tests pass (1 pre-existing env-dependent whale_tracker failure unchanged), live restart confirmed take_profit fired at +77% (old +30% threshold would have closed earlier), metadata wiring verified end-to-end into SQLite.
- 2026-08-03 Executed MT-502 — Robust exit parameter grid search with walk-forward validation. Created `scripts/grid_search_robust.py`: loads 1,157 closed positions from `data/trades.db` (read-only, `mode=ro`), runs a finer 430-combo grid (trailing 4-16% in 1% steps, TP 15-60% in 5% steps, hard stop 10-25% in 5% steps, skipping trailing>=hard, 2.5% slippage on trailing exits) across 4 walk-forward windows (400 train / 200 test, slide 200). Result: trail=4% TP=60% hard=10% was top-1 on train in all 4/4 windows and ranked #1 by average test PnL (+1.15 SOL/window; min +1.04, max +1.27). 12/12 sensitivity-neighborhood combos profitable (full-dataset PnL +4.87 to +6.78 SOL). Hard stop is a no-op (identical PnL across 10/15/20/25% — trailing exit never binds below floor). Confidence: HIGH. Results: `data/grid_search_results.json`. Note: the finer grid upgrades the earlier ad-hoc 6%/50% finding to 4%/60%; TP=50% at trail=4% ranks #9 (+1.13).
- 2026-07-19 Executed MT-499 — Strategy B gate repair + per-gate diagnostics. Refactored `screen_coin()` to return per-gate `gates` dict with 11 boolean keys. Added age-adjusted transaction threshold (`_age_adjusted_min_txns()`) replacing fixed MIN_TRANSACTIONS. Weakened `low_fees` to warning-only (non-blocking) in paper mode. Added age-aware holder concentration tiers (`_age_holder_tier()`) with graduated hard-reject thresholds (80/65/50/40%). Replaced old `Pipe:` counters in `scan_loop()` with 13-field `Gates:` diagnostic counters including true `txn_pass`, `entry_attempts`, `entered`, and `main_blocker`. Validation: compileall + live test confirmed WORLD passed full screen (first time any coin passed since gate repair), low_fees now warns instead of blocking, holder tiers age-correct. Report: `audits/MT-499_strategy_b_gate_repair_report.md`.
- 2026-07-19 Executed MT-495 — Added `scripts/dashboard.py`, a stdlib `http.server` dashboard on `0.0.0.0:8100` with 10-second refresh. It reports Strategy A/B, browser-pc, and watchdog health; reads open/closed position data from `data/trades.db`; renders daily PnL and peak/left-on-table metrics; and summarizes the last 50 strategy-log lines plus the last five tracker-log lines. Validation: `python3 -m compileall scripts/dashboard.py` and a temporary localhost HTTP smoke test passed; server was stopped after validation.

## Known Issues

- MT-505 note: browser-pc Chrome CDP now routes via the MT-505 chain — Windows Chrome on `127.0.0.1:9262` (profile `D:\chrome-mt-profile`) → netsh portproxy `0.0.0.0:19223 → 127.0.0.1:9262` + firewall rule "MT Chrome CDP 19223" → WSL systemd relay `chrome-cdp-wsl-relay.service` (19222) → browser-pc. Windows ports 9222-9261 are ALL portproxy-routed to the Mac for Armada; any new service that needs WSL→Windows inbound must use a port > 9261, add a portproxy rule AND a Windows Firewall allow rule (the WSL Hyper-V firewall default-blocks new inbound ports, `NATInboundRuleNotApplicable` in NAT mode). If the MT Chrome is manually started elsewhere, it must use `--remote-debugging-port=9262 --user-data-dir=D:\chrome-mt-profile`; the watchdog restarts it automatically on 9262.

- MT-130 remediation tasks MT-131 through MT-135 are implemented: direct transport is guarded, state identity is mode-aware, price/PnL truthfulness is hardened, discovery semantics are separated from strict approval, and daily caps/diagnostics now fail closed and redact credentials. MT-136 post-remediation audit remains required. Do not enable live trading or relax any guardrail until that audit passes.
- All three protocol-based providers are now implemented and healthy when env config is present: `transaction_simulator` (available), `wallet_balance_lookup` (provider reached, wallet unfunded — insufficient balance), `wallet_holdings_lookup` (available). Position reconciliation passes (`no_live_positions_to_reconcile`) — paper-mode positions no longer trigger false mismatches. The remaining blocker before a guarded micro-live smoke test is live arming config: `TRADING_WALLET_PRIVATE_KEY`, `LIVE_TRADING_ENABLED`, confirmation phrase, `LIVE_KILL_SWITCH`, tiny live caps, RPC URLs.
- Live Jupiter execution is still intentionally disabled for direct swaps; the serialized submission helper only wires optional Jito/RPC routing for future guarded use and does not enable trading by itself.
- Whale tracker is polling-only for now; webhook ingestion still needs a public callback endpoint and receiver.
- pump.fun websocket token-creation and migration subscriptions are now verified against PumpPortal, but the short dry-run did not capture a live migration payload; graduation normalization is still proven by offline fixtures rather than observed runtime traffic.
- Risk checks use conservative local token fields until on-chain enrichment is implemented.
- Dynamic exits are only active when `exits.dynamic_exits_enabled` is enabled and the exit runtime receives `DynamicExitState` context.
- Latest integrated paper-cycle runs still produce 0 strict paper trades, and MT-054 confirms holder concentration remains the dominant strict-mode blocker even after the narrow pump.fun bonding-curve artifact fix; the current recommendation is to treat further holder work as a parallel calibration lane while attention/narrative scoring becomes the primary lane for ranking discovery candidates that now make it through the warning-policy stack. MT-055 now makes capacity blockers explicit and adds a fresh-session paper-only evaluation mode when older persisted paper positions would otherwise mask that ranking signal.
- Liquidity-aware sizing remains disabled by default; operators must opt in through `position.liquidity_sizing_enabled` before the new conservative liquidity caps affect decisions.
