# Memecoin Trader Status

Project owner: Berj
Domain: automated Solana meme coin trading
Default execution mode: paper trading

<!-- AUTO-MANAGED -->

## Key Config

- Project name: `memecoin-trader`
- Repo path: `/home/dev/projects/memecoin-trader`
- Python: `>=3.11`
- Settings file: `config/settings.yaml`
- Task prefix: `CT`

## Architecture

- `src/core/` defines shared config, database helpers, and Pydantic domain contracts, including conservative live-guardrail settings that stay inactive unless explicitly armed.
- `src/chain/` contains Solana/Jupiter/wallet integration placeholders plus a pre-live Jito block-engine scaffold for MEV-aware bundle request building and injectable submission testing.
- `src/risk/` contains token risk checks, aggregate scoring, funding and honeypot research helpers, signal-aware enrichment, RugCheck-assisted authority/holder/honeypot data, and parallelized provider prechecks. Strict checks retain `UNKNOWN` when evidence is absent; paper-only discovery relaxation is explicit `discovery_relaxed` research metadata, not strict approval. Jupiter quote output is not accepted as pool liquidity.
- `src/execution/` defines the execution adapter contract plus paper and live adapter implementations. Its DexScreener price provider accepts only requested-mint / wSOL base-oriented, finite positive SOL prices; unsupported, inverted, USDC-quoted, and non-finite data degrades to unavailable. Paper PnL preserves unavailable entry/mark states and uses remaining-token cost basis for partial exits. The rest of the execution layer includes disabled-by-default live submission helpers, guardrails, preflight, execution-config validation, reconciliation, circuit breaker, readiness, guarded live exit, and guarded live buy; live buy records unpriced fills until actual fill data exists.
- `src/signals/` defines async signal-source contracts; `aggregator.py` safely starts/stops/polls sources, clusters same-mint signals by time window, boosts multi-source composites, now attaches candidate mode labels and mode-count diagnostics alongside bounded pump.fun weak-identity context for downstream ranking, while `modes.py` provides conservative launch-vs-migration candidate labeling helpers, `novelty.py`, `novelty_fixture.py`, `novelty_compare.py`, `novelty_quality.py`, and `helius_fresh_mint_diagnostic.py` provide caller-supplied novelty and fake-transport diagnostic helpers, and `whale_tracker.py`, `pump_fun.py`, `onchain.py`, and `twitter.py` provide Helius, PumpPortal, DexScreener, and Twitter/X-backed signal feeds.
- `src/strategy/` contains decision, portfolio, position, fixed-exit helpers, gated dynamic-exit runtime hooks for calibrated volume-decay exits, optional market-regime classification helpers for later risk tuning, a default-off decision metadata hook that can surface regime summaries without changing entry or sizing behavior, and a position-manager option for session-local paper evaluation that ignores previously persisted positions without changing live or default paper behavior.
- `src/monitoring/` contains lightweight health, alert, and dashboard helpers for runtime visibility.
- `src/cli.py` exposes operator commands including health/config inspection, `env-readiness`, and a bounded `paper-cycle` runtime that evaluates ranked aggregated opportunities from pump.fun, whale, on-chain, and Twitter sources, persists paper trades/positions, and prints a safe summary before terminating, with strict and discovery paper-only risk profiles, candidate mode counts and visible launch/migration/unknown diagnostics, safe `live-readiness`, guarded `live-exit`, guarded `live-buy`, and the `paper-soak` audit command, a bounded discovery-only weak-identity ranking penalty for pump.fun launch quality, safe show-config live-guardrail diagnostics, and a manual-only Grok social-check prompt export for the top discovery candidates using safe normalized fields only.
- `paper-migration-lifecycle` is a bounded paper-only operator path: it accepts only Pump.fun migration/graduation signals that pass the existing paper-minimum evidence gate and have a current requested-mint/wSOL DexScreener quote, requires `--confirm` before entry, then immediately re-marks and closes only the newly created position. It preserves existing strict/live policy and leaves a position open rather than inventing a close when the post-entry mark is unavailable.

- `docs/MICRO_LIVE_RUNBOOK.md`: guarded micro-live operator runbook documenting required env vars, pre-live checklist, exact command sequence, abort conditions, and post-live review checklist for executing a single tiny SOL smoke trade while keeping live trading disabled by default.
- `docs/POST_LIVE_REVIEW_TEMPLATE.md`: structured post-live review template for the first future guarded micro-live smoke test — documents test metadata, pre-live readiness state, trade details, wallet/position state, fees, circuit breaker, logs review, issues, go/no-go decision, and kill-switch confirmation.
- `docs/HELIUS_READINESS_PROVIDER_WIRING.md`: design report for wiring three real Helius-backed providers (`wallet_balance_lookup`, `transaction_simulator`, `wallet_holdings_lookup`) — proposed classes, env/config needs, safety constraints, fake-client test strategy, and implementation order starting with `transaction_simulator` (no wallet key required).
- `docs/MEMECOIN_DECISION_LEDGER_PATTERNS.md`: design-only constraints for a future `memecoin_decision` observability ledger, including narrow-tool access, SQLite compatibility, evidence provenance, and explicit non-interference with trading behavior.
- `docs/MEMECOIN_DECISION_LEDGER_SCHEMA.md`: design-only proposed `memecoin_decision` PostgreSQL schema, evidence/attribution invariants, narrow tool contracts, and phased diagnostic-first implementation boundary.
- `docs/CANDIDATE_WALLET_FIXTURE_TEMPLATE.md`: manual top-level JSON-array fixture template with separate UTC matched-window metadata, required Signal and whale-label fields, and the checker-before-comparator gate; it contains no collected data or runtime integration.
- `docs/WINDOW_1_FIXTURE_MANIFEST_TEMPLATE.md`: blank manual checklist for the two future window-1 fixture labels/paths, shared UTC metadata, required-field completeness, and complete-versus-stop gate result; it contains no fixture data or collection path.

- `.env.example`: expected environment variables for RPC, APIs, wallet, position caps, closed-by-default live arming guardrails (`LIVE_TRADING_ENABLED`, confirmation phrase, kill switch, and tiny live caps), the minimum live wallet balance reserve used by preflight, and conservative execution config for primary/backup RPC plus priority fee and Jito tip overrides.
- `pyproject.toml`: Python package metadata, dependencies, dev dependencies, pytest, and Ruff config.
- `config/settings.yaml`: risk, position, exit, execution, monitoring, and conservative live-guardrail defaults, including a default-off dynamic exit gate plus tiny live caps, minimum wallet reserve checks, and bounded priority-fee/Jito-tip settings that do not enable live trading by themselves.
- `config/wallets_to_track.yaml`: public sample wallet watchlist used for safe whale-tracker dry runs until real tracked wallets are configured locally.
- `scripts/check_token.py`: smoke-check script for building a token model and running risk scoring.
- `src/chain/jito.py`: pre-live Jito block-engine scaffold that normalizes serialized transactions into bundle payloads and carries optional tip metadata. Its public `submit_bundle()` path now fails closed; only the guarded Jupiter live adapter calls the private injectable HTTP transport path after safety checks.
- `src/core/config.py`: YAML settings loader and Pydantic config models for risk, position, exit, execution, monitoring, and conservative live-guardrail defaults plus env overrides for tiny live caps.
- `src/execution/jupiter_live.py`: live Jupiter adapter boundary that still blocks direct live swaps and now runs every serialized submission through explicit live guardrails, live preflight, and live execution-config validation before any Jito/RPC path, with optional primary-to-backup RPC failover in fake/live-ready fixtures and fail-closed `guardrails`, `preflight`, or `config` diagnostics when setup is invalid.
- `src/execution/live_circuit_breaker.py`: stateful operational breaker that tracks consecutive RPC, simulation, and submission failures plus required health-check freshness, returns fail-closed diagnostics for live mode, and supports explicit reset for operator recovery.
- `src/execution/live_buy.py`: guarded micro-live buy helper that requires a fully ready live state, refuses oversized trade sizes or duplicate open positions, and only opens a local live-mode position after successful fake/live-ready submission.
- `src/execution/live_exit.py`: guarded sell-only live exit helper that requires a ready live status gate, an existing local position, an injected exit-transaction builder, and successful submission checks before closing a position, while refusing to open new positions.
- `src/execution/live_execution_config.py`: conservative helper that validates priority fee and Jito tip bounds, resolves primary/backup RPC configuration from settings or env, redacts RPC diagnostics down to host labels, and reports whether future live submission wiring is valid without exposing secrets.
- `src/execution/live_guardrails.py`: closed-by-default helper for live arming and cap config. `live_daily_caps.py` adds fail-closed per-day trade/loss enforcement through an injectable fresh state lookup; absence, stale, malformed, or exhausted state blocks submission.
- `src/execution/live_preflight.py`: closed-by-default helper that checks wallet SOL balance reserve and serialized transaction simulation through fakeable interfaces, blocks submission on low/unknown balance or simulation failure, and returns sanitized diagnostics without echoing transactions or secrets.
- `src/execution/env_readiness.py`: safe env/secrets visibility diagnostics reporting present/missing for 11 env vars (`HELIUS_API_KEY`, `TRADING_WALLET_PUBLIC_KEY`, `TRADING_WALLET_PRIVATE_KEY`, `LIVE_*`, `MAX_*`, `PRIMARY_RPC_URL`, `BACKUP_RPC_URL`) without revealing values, prefixes, suffixes, or partial secrets.
- `src/execution/helius_providers.py`: Helius-backed provider implementations — `HeliusTransactionSimulator` (simulateTransaction/getLatestBlockhash), `HeliusWalletBalanceLookup` (getBalance), `HeliusWalletHoldingsLookup` (getTokenAccountsByOwner). All use fakeable HTTP client, resolve RPC URL + wallet public key from env or `.env` without exposing secrets, and degrade to `not_ready` on missing config or unreachable RPC. Factories return None when config is absent so readiness gates can report `unavailable` vs `failed`.
- `src/execution/live_readiness.py`: diagnostic micro-live readiness gate that combines guardrails, execution config, preflight, position reconciliation, circuit breaker state, and health into a single `READY`/`NOT READY` report with actionable `recommended_env` hints and split position-manager vs wallet-holdings-lookup diagnostics; does not mutate state or enable live trading.
- `src/execution/position_reconciliation.py`: diagnostic reconciliation helper that compares persisted live-mode open positions against fakeable wallet token holdings, skips paper-mode positions, reports `no_live_positions_to_reconcile` when no live positions exist, flags wallet-only, local-only, and material balance mismatch cases for live positions, and fails closed on missing holdings data without mutating positions automatically.
- `src/risk/rugcheck.py`: read-only RugCheck client for `v1/tokens/{mint}/report` that normalizes authority flags, top-holder concentration, liquidity state, honeypot indicator, and provider risk score/level while degrading safely on provider failures or malformed responses.
- `src/risk/funding_provider.py`: Helius-backed inbound SOL funding adapter that resolves `HELIUS_API_KEY` from env or local `.env`, fetches recent enhanced transactions through an injectable HTTP layer, normalizes only inbound native SOL transfers into `InboundTransfer` records, and degrades safely on missing credentials, non-200 responses, timeouts, and malformed payloads.
- `src/risk/honeypot_simulation.py`: standalone read-only sell-simulation adapter that accepts serialized sell payloads plus fake parsed instructions, routes through an injected Helius/Blowfish-style backend, normalizes pass/block outcomes, and flags Token-2022 transfer hooks or suspicious conditional sell-block hints without wallet or signing requirements.
- `src/risk/funding_analysis.py`: async funding-source analyzer that groups buyer wallets by recent inbound funder, computes bundled-buyer concentration metrics, and flags likely sybil or bundled launches while degrading gracefully when provider data is missing.
- `src/risk/scorer.py`: aggregate risk scoring plus signal-aware `TokenInfo` enrichment from raw pump.fun/on-chain payload fields, parallel RugCheck and funding-analysis prechecks, optional honeypot-simulation fallback when RugCheck leaves the honeypot outcome unknown and the signal provides sell-simulation input, discovery-mode age relaxation, and read-only holder lookups that only backfill concentration data when RugCheck does not already provide it.
- `src/cli.py`: Typer entrypoint for health/config commands plus the bounded `paper-cycle` runner over integrated aggregated signal sources, including strict/discovery paper-only risk profiles, aggregate source/rejection diagnostics for skipped opportunities, explicit paper-capacity diagnostics, visible `candidate_mode` labels and mode-count guidance in discovery output, `live-readiness`, guarded `live-exit`, guarded `live-buy`, `paper-soak` audit commands, `paper-pnl` (PnL summary with per-position detail, persisted paper fill-quality/confidence labeling, mark-coverage diagnostics, actionable hints, and `--marks {live,unavailable}` flag), `paper-close` (close by mint with optional `--price`/`--use-mark`, or `--all --confirm` bulk close, with low-confidence legacy preview labeling), and `paper-state` (read-only state plus `--legacy` listing and `--archive-legacy --confirm` exclusion workflow for legacy paper rows) commands, `PaperSoakAudit` dataclass and `run_paper_soak` helper that runs a bounded discovery-mode cycle then adds health/readiness/guardrail/circuit-breaker diagnostics into a consolidated audit report, an optional fresh evaluation-session mode that ignores previously persisted paper positions for one bounded run, a bounded discovery-only pump.fun weak-identity ranking penalty surfaced as `attn=score(-penalty)` and `pen=` diagnostics in candidate summaries, and a bounded copy-ready Grok social-check prompt export for the top five discovery candidates that does not influence decisions. The default paper-cycle adapter now wires DexScreener entry quotes into paper execution so persistent positions can become `priced_quote` when a quote is available. Position aging diagnostics (`_position_age_label` / `_close_suggestion`) surface `fresh`/`aging`/`stale`/`missing_mark`/`low_confidence_fill` labels in `paper-state`, `paper-report`, and `paper-pnl` outputs with informational `paper-close --preview` suggestions (1h fresh, 24h stale thresholds). `--persist-positions` flag on `paper-soak` enables reportable position persistence.
- `src/cli.py` command: `paper-rejected-provider-mark-coverage` is a bounded read-only report over persisted rejected snapshots. Its compact evidence summary directly shows replayable rows, provider successes, explicit failures, historical not-recorded rows, and later-outcome-eligible baselines before the exact provider-status and source/mode/blocker breakdown. It makes no provider calls or state changes.
- `src/signals/aggregator.py`: independent signal aggregator that safely polls registered sources, deduplicates same-mint signals inside a configurable time window, boosts cross-source composites, ranks opportunities, best-effort logs them to any compatible signals table or recorder, now surfaces aggregate-only Twitter credibility diagnostics, attaches deterministic `candidate_mode` labels plus mode-count diagnostics, and carries normalized pump.fun identity context for partial/sparse metadata and promotional weak-identity launch names without hard-rejecting them.
- `src/signals/modes.py`: conservative pure-logic candidate classifier that labels signal-like objects or aggregated opportunity payloads as `launch`, `migration`, or `unknown` using only existing source/type/payload context and degrading safely on malformed data.
- `src/signals/novelty.py`: pure in-memory `summarize_novelty()` helper over caller-supplied raw `Signal` records. It counts session-local novel/duplicate/invalid mint observations, source-level mix, optional existing whale labels, and per-mint source origins without acquiring signals or touching runtime, config, persistence, pricing, evaluation, execution, or live paths.
- `src/signals/novelty_fixture.py`: opt-in `summarize_json_signal_fixture()` helper that reads one explicit caller-supplied top-level JSON-array fixture, validates records as `Signal`, and delegates to `summarize_novelty()` without default paths, source acquisition, output persistence, or runtime integration.
- `src/signals/novelty_compare.py`: `compare_json_signal_fixtures()` requires at least two nonblank caller labels mapped to explicit JSON fixture paths, delegates each path to the fixture helper, and returns ordered per-label novelty summaries plus a valid-observation duplicate rate without selecting a winner or persisting output.
- `src/signals/novelty_quality.py`: `check_json_signal_fixtures()` validates explicit fixture JSON and caller-supplied declared time windows, returning `complete`, `incomplete`, `malformed`, `unmatched_window`, or `inconclusive` states with reason labels and optional in-memory novelty summaries; it does not acquire data or persist checks.
- `src/signals/helius_fresh_mint_diagnostic.py`: fake-transport-only scaffold with a contract identifier and hard five-record cap. It reduces injected `{mint: ...}` records into sanitized aggregate counts and never resolves keys, constructs an HTTP client, calls a provider, or registers a runtime source.
- `src/signals/onchain.py`: DexScreener-backed read-only on-chain monitor that discovers candidate mints from token profiles, fetches Solana pair snapshots, scores volume spikes, buy/sell momentum, liquidity changes, and recent-pool activity, then deduplicates emitted mints within a five-minute poll window.
- `src/signals/pump_fun.py`: websocket-first pump.fun monitor with verified PumpPortal `subscribeNewToken` and `subscribeMigration` subscriptions plus v3 HTTP fallback over `/coins` and `/coins/currently-live`, and a conservative source-layer junk filter that now skips missing-identity, URL-like, and obvious placeholder-symbol launches before they reach discovery review.
- `src/signals/twitter.py`: Twitter/X monitor that uses recent search when `TWITTER_BEARER_TOKEN` is present, safely degrades without configured credentials, extracts `$TICKER` and Solana mint mentions, deduplicates posts by ID, scores author credibility from follower/verified/account-age/spam/duplicate signals, and emits deterministic credibility-weighted `Signal` objects only when a mint address is present.
- `src/signals/whale_tracker.py`: Helius enhanced-transactions poller that reads `HELIUS_API_KEY` from environment or local `.env`, includes token-account activity, deduplicates signatures, and emits whale-buy `Signal` objects.
- `src/strategy/decision_engine.py`: async risk-gated buy evaluation plus open-position exit scanning and sell execution, with structured rejection reasons for paper-cycle diagnostics, opt-in liquidity-aware position caps that preserve flat sizing by default, and a default-off market-regime metadata hook that can attach regime summaries to paper decisions without changing trade selection.
- `src/strategy/dynamic_exits.py`: helper-only dynamic exit calibration module with deterministic checks for volume-decay exits, trail-start eligibility, liquidity-drop emergencies, and aggregate reason-label summaries used by the exit runtime when enabled.
- `src/strategy/market_regime.py`: deterministic market-regime classifier that labels aggregate meme-market conditions as `hot`, `normal`, `thin`, `risky`, or `unknown` from pool/liquidity/flow/signal/trade-success inputs and returns conservative adjustment hints without wiring them into runtime behavior.
- `src/strategy/position_sizing.py`: helper-only liquidity tier mapping that classifies pool liquidity into conservative max-position caps and skip conditions for decision-engine use.
- `src/strategy/position_manager.py`: async open-position persistence, exposure tracking, partial exits, and close handling. Position caches and lookup/close APIs are mode-aware (`paper`/`live`) so same-mint rows cannot overwrite each other; paper-cycle capacity/exposure uses paper rows only.
- `src/strategy/exits.py`: take-profit ladder, stop-loss, time-stop, liquidity, emergency exit evaluation, and a default-off `DynamicExitState` hook for calibrated dynamic-exit checks with testable reason labels.
- `src/monitoring/health.py`: process-level health probe plus the dashboard-compatible `HealthMonitor` shim.
- `src/monitoring/dashboard.py`: Rich terminal dashboard that reads the canonical runtime DB path, recent trades/positions, and monitoring health.
- `tests/`: smoke tests for risk contracts, signal aggregation, paper execution, whale tracker polling behavior, and pump.fun normalization/deduplication.
- `tests/test_jito.py`: focused coverage for Jito bundle payload construction, bundle-id parsing, graceful degradation on non-200, timeout, malformed JSON, explicit confirmation that no wallet/private-key or real network call is required, plus closed-by-default live-adapter guardrail blocking, wallet-balance and simulation preflight blocking, fully armed fake-provider submission behavior, and sanitized diagnostics that do not echo confirmation phrases or serialized transactions.
- `tests/test_env_readiness.py`: focused coverage for safe env-readiness diagnostics, all vars missing by default, all vars present when set, CLI output containing no secret values/fragments, lines format, and empty/whitespace value detection.
- `tests/test_live_execution_config.py`: focused coverage for safe default execution config, malformed and out-of-bounds priority fee rejection, and redacted show-config RPC diagnostics that expose host labels but not secrets.
- `tests/test_live_circuit_breaker.py`: focused coverage for below-threshold behavior, threshold trips, stale or missing health-check blocking, explicit reset, and no impact on paper mode.
- `tests/test_helius_providers.py`: focused coverage for `HeliusTransactionSimulator` — missing config returns unavailable, fake healthy/unhealthy/malformed RPC responses handled correctly, API key and raw tx never leak into output, private key not required, healthy simulator alone does not bypass guardrails, real transaction simulation paths, and safe close.
- `tests/test_live_readiness.py`: focused coverage for default `NOT READY` state, surfaced stale/unsafe readiness checks, fully fake-ready `READY` fixtures, three-state readiness distinction (intentionally unconfigured / configured-but-unhealthy / configured-and-healthy), split position-reconciliation diagnostics, recommended-env hints, guardrail-non-bypass verification, malformed/missing config fail-closed verification, and safe CLI readiness output without secret leakage.
- `tests/test_live_buy.py`: focused coverage for default blocked live buy behavior, oversized trade rejection, duplicate-position blocking, fully fake-ready buy success that opens a local live-mode position, and fail-closed CLI behavior by default.
- `tests/test_live_exit.py`: focused coverage for sell-only live exit blocking when no position exists, readiness-gate blocking, fully fake-ready exit success that closes an existing position, and fail-closed CLI behavior by default.
- `tests/test_position_reconciliation.py`: focused coverage for mode-aware position reconciliation — legacy positions default to paper, paper positions ignored by live wallet reconciliation, live positions included, local-only live positions still produce mismatch, matching live positions pass, mixed paper+live correctly filters, wallet-only holdings flagged with live positions, fail-closed behavior on missing wallet lookup data, and default mode remains paper.
- `tests/test_live_guardrails.py`: focused coverage for default fail-closed live guardrails, paper-mode non-arming, missing env enable, kill-switch blocking, tiny live max trade enforcement, malformed daily-cap rejection, fully armed pass cases, and safe show-config diagnostics without confirmation-phrase leakage.
- `tests/test_aggregator.py`: focused coverage for signal-source fan-out, same-mint clustering inside/outside the dedupe window, composite ranking, social-credibility diagnostic summaries, candidate-mode attachment/count diagnostics, pump.fun weak-identity context attachment, safe single-source passthrough, source-failure isolation, and optional SQLite signal logging.
- `tests/test_signal_modes.py`: focused coverage for launch-vs-migration classification, safe unknown fallback on non-pump or malformed inputs, composite raw-data migration detection, and proof that classification is pure and does not mutate signal scoring fields.
- `tests/test_novelty.py`: in-memory fixture coverage for raw-signal novelty counts, cross-source and whale-label origins, invalid mint/origin diagnostics, and input immutability.
- `tests/test_novelty_fixture.py`: temporary JSON-fixture coverage for file-fed novelty summaries, read-only fixture bytes, and invalid top-level/record shape rejection.
- `tests/test_novelty_compare.py`: temporary matched-fixture coverage for ordered labeled comparisons, summary/source/wallet-origin data, duplicate rates, fixture-byte immutability, and invalid labels or insufficient fixture count.
- `tests/test_novelty_quality.py`: temporary fixture coverage for complete, incomplete explicit timestamp, malformed JSON, unmatched declared windows, inconclusive empty samples, reason labels, and read-only fixture bytes.
- `tests/test_helius_fresh_mint_diagnostic.py`: fake/in-memory transport coverage for bounded aggregate mint counts, invalid records, duplicates, blank contracts, and sanitized provider-error state.
- `tests/test_cli_paper_cycle.py`: bounded paper-cycle coverage for accepted/rejected fake signals, aggregator integration, composite opportunities, non-fatal source degradation, paper-only enforcement, max-signal/timeout termination, SQLite persistence, visible launch/migration/unknown mode diagnostics, bounded pump.fun weak-identity ranking penalties, safe CLI summary output, and the bounded Grok prompt export.
- `tests/test_cli_summary.py`: stable summary-table structure coverage for paper-cycle output, including mode-count-aware summary construction.
- `tests/test_decision_ledger_prototype.py`: disabled ledger-contract coverage plus AST checks preventing runtime imports of the ledger prototype or synthetic test harness and runtime calls to the provider-coverage CLI command.
- `tests/test_dynamic_exits.py`: focused helper and runtime-gate coverage for volume-decay timing, liquidity emergency calibration, early trail-start signaling, aggregate reason labels, and preservation of existing behavior while dynamic exits stay disabled by default.
- `tests/test_market_regime.py`: focused coverage for deterministic `hot`, `risky`, `thin`, `unknown`, and `normal` classifications plus stable confidence, reason labels, adjustment hints, and no-runtime-side-effect guarantees.
- `tests/test_position_sizing.py`: focused helper coverage for liquidity tier matching, unknown/invalid liquidity skips, and custom tier overrides.
- `tests/test_e2e_paper.py`: offline signal-to-trade smoke covering decision-engine approval, paper execution, trade persistence, position persistence, and dashboard visibility on a temporary SQLite DB.
- `tests/test_funding_analysis.py`: focused coverage for bundled-buyer detection thresholds, diverse and unknown funding outcomes, empty input neutrality, provider-failure degradation, and timestamp window filtering without live network calls.
- `tests/test_funding_provider.py`: focused provider coverage for Helius inbound SOL normalization, missing-key and HTTP/provider failure degradation, malformed JSON handling, irrelevant transfer filtering, and explicit confirmation that fake fetchers prevent live network calls.
- `tests/test_honeypot_simulation.py`: focused adapter coverage for successful/blocked sell simulations, Token-2022 transfer-hook detection, suspicious conditional sell-block hint detection, timeout/provider degradation, malformed/missing-payload handling, and explicit confirmation that only fake providers are used.
- `tests/test_onchain_provider.py`: focused provider coverage for `OnChainMonitor` interface conformance, deterministic DexScreener scoring helpers, graceful provider degradation, mint-level dedupe, and explicit confirmation that no wallet/trading code is involved.
- `tests/test_pump_fun_provider.py`: focused provider-shape coverage for live-style pump.fun create payloads, graduation detection from migration fields, safe handling of websocket ack/malformed messages, and conservative rejection of obvious junk launch identity while preserving quirky but usable names.
- `tests/test_rugcheck.py`: focused client coverage for successful RugCheck normalization plus graceful handling of missing fields, non-200 responses, timeouts/provider errors, malformed JSON, and non-object payloads.
- `tests/test_twitter_provider.py`: focused provider coverage for Twitter/X payload normalization, no-key degradation, ticker/mint extraction helpers, credibility tiering, spam-resistant weighting, diversity sensitivity, mention-velocity scoring, and post-ID deduplication without live network calls.
- `tests/test_whale_tracker_provider.py`: focused provider tests for dotenv-based Helius key loading, token-account polling params, Helius-style buy normalization, dedupe, and safe empty/malformed handling.
- `tests/test_strategy.py`: focused coverage for decision-engine risk gating, opt-in liquidity sizing caps/fallbacks, paper sizing diagnostics, and exit-rule behavior.
- `tests/test_monitoring.py`: focused coverage for dashboard DB-path resolution, health compatibility, and one-shot dashboard rendering.

## Last 10 Changes

- 2026-07-13 Executed MT-393 holder concentration v3 fix. Local top-holder calculation now excludes concrete burn/null/system identities, known Raydium program/authority identities, and explicitly identified Raydium pool/PDA records before summing retained accounts; ordinary wallet whales remain counted and the configured 50% threshold is unchanged. Standard `getTokenLargestAccounts` records lack pool/PDA ownership metadata, so unlabeled accounts remain retained rather than inferred non-human. Added focused burn, pool/PDA, real-whale, and all-excluded tests. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/risk-fix-reports/holder_concentration_fix_v3.md`. Validation: 94 focused tests and compileall passed; Ruff unavailable because it is not installed. No other risk check, threshold, config, schema, live path, or paper gate changed.

- 2026-07-13 Executed MT-389 established pair paper scan as skipped. A bounded direct DexScreener search collection returned 20 established Solana/wSOL pairs with nonzero direct pair liquidity, 24-hour volume, and native price values. The unchanged paper-minimum gate blocked 3/20 for known liquidity below the configured 10 SOL minimum, 18/20 for top-holder evidence, and the two otherwise clean candidates for unknown token age; pair creation time was not misused as token-age provenance. MT-388's sanitized report had only liquidity rejection labels, not historical values, so this report records that limitation instead of reconstructing or inventing values. No candidate qualified for an immediate entry quote, so no paper trade, position, or database state was created. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/established_pair_paper_scan.md`. Validation: 93 focused tests passed. No live path, real swap, wallet/key action, risk/config/schema/provider change, raw provider payload, PnL, or performance claim occurred.

- 2026-07-13 Executed MT-388 bigger DexScreener scan v2 as skipped. One larger capped ten-profile existing `OnChainMonitor` scan found two DexScreener paired, non-`NEW_POOL` candidates. Existing paper-minimum checks rejected candidate 1 for liquidity and candidate 2 for liquidity plus top-holder evidence, so neither reached an immediate quote or a paper entry. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/bigger_dexscreener_scan_v2.md`. Validation: 30 focused on-chain/paper-minimum/RugCheck tests passed. No paper state, live path, real swap, wallet/key action, risk/config/schema/provider change, raw provider payload, PnL, or performance claim occurred.

- 2026-07-13 Executed MT-386 DexScreener paired scan v2 as skipped. One capped five-profile existing `OnChainMonitor` scan found one DexScreener paired, non-`NEW_POOL` candidate with existing liquidity context; it failed the unchanged paper-minimum evidence gate, so no immediate quote was requested and no paper entry, position, trade, mark, close, or database state was created. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/dexscreener_paired_scan_v2.md`. Validation: 22 focused on-chain/provider/paper-minimum tests passed. No live path, real swap, wallet/key action, risk/config/schema/provider change, raw provider payload, PnL, or performance claim occurred.

- 2026-07-13 Reviewed MT-382 through MT-383 migrated lifecycle v2 - PASS WITH NOTES. Review: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-lifecycle-reviews/migrated_lifecycle_review_v2.md`. MT-382's bounded migration scan found zero migration/graduation candidates, so it created no quoted entry; MT-383 correctly skipped mark/close. Reviewed commits (`598db02`, `891b691`) changed only STATUS.md. No live path, wallet/key action, real swap, risk loosening, provider/schema/config change, raw payload, simulated PnL, or outcome/performance claim occurred. The measurable entry-mark-close lifecycle was not achieved.

- 2026-07-13 Executed MT-383 migrated mark-close v2 as skipped. MT-382 created no quoted paper position because its bounded migration scan found zero migration/graduation candidates, so this task made no mark or paper-close request. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/migrated_mark_close_v2.md`. No persistence, simulated PnL, live path, real swap, wallet/key action, config/risk/schema/provider change, or raw provider payload occurred.

- 2026-07-13 Executed MT-382 migrated entry v2 as skipped. One 30-second confirmed `paper-migration-lifecycle` scan considered 57 sanitized source signals but found 0 migration/graduation candidates, leaving 0 paper-minimum eligible and 0 quote-ready candidates. No paper entry, position, trade, mark, close, or database state was created. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/migrated_entry_v2.md`. Validation: `python3 -m pytest -q tests/test_paper_migration_lifecycle.py` passed (4 tests). No live path, real swap, wallet/key action, risk/config/schema/provider change, raw provider payload, PnL, or performance claim occurred.

- 2026-07-13 Added `paper-migration-lifecycle`, a bounded paper-only migration/graduation candidate path. It requires the existing paper-minimum evidence checks, a current requested-mint/wSOL DexScreener quote, an explicit `--confirm`, duplicate/open-position and portfolio-cap checks, then a separate mark before closing only its new position. A missing mark leaves the quoted entry open without invented PnL. Added focused fake-source/fake-price tests covering successful entry-mark-close, confirmation gating, quote absence, mark absence, and sanitized review reports. Validation: 105 focused/regression tests and `python3 -m compileall src` passed; Ruff unavailable. No live path, wallet/key action, real swap, risk/config/schema change, raw payload output, or performance claim occurred.

- 2026-07-13 Reviewed MT-375 through MT-376 quote-ready lifecycle - PASS WITH NOTES. Review: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-lifecycle-reviews/quote_ready_lifecycle_review.md`. MT-375 stopped at an unavailable immediate requested-mint/wSOL quote without creating paper state; MT-376 then correctly skipped its mark/close path. Reviewed commits (`1f4afec`, `09efa33`) changed only STATUS.md, and reports document no DB write. No live path, wallet/key action, real swap, risk loosening, provider/schema change, simulated PnL, or outcome/performance claim occurred. The measurable entry-mark-close lifecycle was not achieved.

- 2026-07-13 Executed MT-376 quote-ready mark-close v2 as skipped. MT-375 created no quoted paper position because its immediate requested-mint/wSOL quote gate returned `no_pairs`, so MT-376 stopped before a mark or close request. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/quote_ready_mark_close_v2.md`. No simulated sale, PnL, paper persistence, live path, real swap, wallet/key action, config/risk/schema/provider change, or raw payload occurred.

- 2026-07-13 Executed MT-378 observation quote blocker report. One capped five-candidate opt-in `paper-minimum-dry-run --record-observations` scan wrote five new sanitized ledger rows: 5 unique mints, 5 new rows, 0 repeats, and 0 paper-minimum eligible rows. All five were blocked by existing top-holder evidence checks before quote retrieval, so the existing read-only DexScreener quote provider was not called; quote-available and no-pair eligible counts are both 0/not applicable. This scan provides no evidence that repeats waste entry attempts; the observable blocker was eligibility evidence, not quote availability. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/dataset-table-reports/observation_quote_blocker_report.md`. No live trade, real swap, paper entry, mark, close, wallet/config/risk change, historical import, raw provider payload, or quote-provider call occurred.

- 2026-07-13 Executed MT-371 quote-gated lifecycle v2 review - PASS WITH NOTES. Review: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-lifecycle-reviews/quote_gated_lifecycle_review.md`. MT-368 stopped at an unavailable immediate quote without creating paper state; MT-369 and MT-370 then correctly skipped their mark/close paths. The reviewed commits (`6fa7d71`, `de69e5a`, `300834e`) changed only STATUS.md, and reports document no DB write. No live path, wallet/key action, real swap, risk loosening, provider change, simulated PnL, or outcome/performance claim occurred. The measurable lifecycle was not achieved.

- 2026-07-13 Executed MT-373 dataset newness report v2. Two capped five-candidate `paper-minimum-dry-run --record-observations` scans wrote 10 sanitized observation rows to the existing ledger: 5 unique PumpFun launch mints, 5 first-scan new rows, and 5 second-scan prior-run repeat rows. Three observation rows were paper-minimum eligible (an observation count, not distinct-mint count). Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/dataset-table-reports/dataset_newness_report_v2.md`. No trade, quote, mark, close, live action, wallet/config/risk change, raw provider payload, or historical import occurred.

- 2026-07-13 Executed MT-370 as skipped/no-op. MT-369 had no current mark because MT-368 created no non-null quoted paper position, so no priced and marked paper position existed to close. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/paper_close_lifecycle_v2.md`. No close request, persistence, simulated PnL, paper trade, live path, real swap, wallet/key action, config/risk/provider change, or secret/raw payload output occurred.

- 2026-07-13 Executed MT-369 as skipped/no-op. MT-368 created no non-null quoted paper position, so no qualifying priced paper position existed to mark. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/priced_paper_mark_snapshot.md`. No mark request, persistence, paper trade, PnL, live path, real swap, wallet/key action, config/risk/provider change, or secret/raw payload output occurred.

- 2026-07-13 Executed MT-365 live scan observation wiring. `paper-minimum-dry-run` keeps zero-write default behavior and now supports explicit `--record-observations` plus optional `--db-path`; opt-in runs persist sanitized candidate evidence only and report observation/new/repeat counts with a short mint label per candidate. Added focused default/opt-in/repeat/sanitization tests. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/dataset-table-reports/live_scan_observation_wiring_report.md`. Validation: 11 focused tests and `python3 -m compileall` passed; Ruff unavailable. No trade/live/provider/config/risk/wallet/raw-payload/secret change occurred.

- 2026-07-13 Executed MT-368 quote-gated paper entry v2 as skipped. A bounded current scan found 5/11 paper-minimum eligible candidates, but the selected redacted candidate's immediate requested-mint/wSOL DexScreener quote was unavailable (`no_pairs`), so no paper trade, priced position, fill, or DB write occurred. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/quote_gated_paper_entry.md`. MT-369 and MT-370 are ineligible; proceed directly to MT-371. No live path, real swap, key/wallet action, config/risk/provider change, raw payload, PnL, or performance claim occurred.

- 2026-07-13 Executed MT-364 candidate-observations SQLite persistence. Added `live_candidate_observations` initialization plus sanitized `CandidateObservation` / `record_candidate_observation()` helpers in `src/core/database.py`; first-seen/repeat counts and same-run source collapse are persisted without accepting raw payloads. Added focused temporary-SQLite coverage in `tests/test_candidate_observations.py`. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/dataset-table-reports/candidate_observations_table_report.md`. Validation: 5 focused tests passed and `python3 -m compileall` passed; Ruff was unavailable. No runtime wiring, paper trade, live path, provider, risk/config, wallet, or secret change occurred.

- 2026-07-13 Executed MT-363 live candidate dataset table contract. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/dataset-table-reports/live_dataset_table_contract.md`. The design specifies an append-only SQLite observation ledger keyed by run/mint with source/mode/novelty/repeat/risk/paper-minimum/blocker/quote/fill fields and exact decision/trade/position linkage rules. Existing paper-cycle is a safe SQLite-only implementation path; `paper-minimum-dry-run` remains no-persistence unless separately changed with an explicit opt-in contract. No DB schema/runtime/provider/config/risk/live/paper-trade change, raw payload, or secret output occurred.

- 2026-07-13 Executed MT-366 bounded quote availability/stability diagnostic. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/quote-diagnostics/quote_availability_stability.md`. Of 11 deduplicated launch candidates, 2 were paper-minimum eligible; 4 had valid requested-mint/wSOL DexScreener quotes and 7 had `no_pairs`. Repeated two-probe checks on both eligible candidates found one stable available (`live_dexscreener` twice) and one consistently unavailable (`no_pairs` twice). This clears a future task to attempt exactly one priced paper buy only with immediate quote revalidation. No trade, position, mark, close, DB, config, live/runtime wiring, secret/raw provider payload, PnL, or performance claim occurred.

- 2026-07-13 Reviewed MT-355 through MT-358 paper lifecycle chain - PASS WITH NOTES. Review: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-lifecycle-reviews/paper_lifecycle_review.md`. The chain stayed paper-only: MT-355 used existing safe pricing audit, MT-356 stopped before persistence when immediate quote was unavailable, and MT-357/MT-358 correctly skipped mark/close after their preconditions failed. No live/wallet/key/config/risk-threshold/provider/schema scope expansion occurred. `data/trades.db` contains only the earlier MT-353 normal paper buy/position, documented as unpriced; no mark, close, PnL, or measurable complete lifecycle exists. Required follow-up is a bounded read-only quote-availability/stability diagnostic before any new priced entry attempt, not a risk or live-path change.

- 2026-07-13 Executed MT-358 as skipped/no-op. MT-357 had no current mark because no priced position exists after MT-356, so MT-358's close-smoke precondition failed. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/paper_close_lifecycle_smoke.md`. No close, simulated sell, PnL calculation, DB write, config, live path, wallet action, or secret output occurred.

- 2026-07-13 Executed MT-357 as skipped/no-op. MT-356's immediate quote failure meant no priced paper position existed, so MT-357's required precondition failed. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/paper_position_mark_snapshot.md`. No mark request, persistence, trade, DB write, config, live path, wallet action, or secret output occurred.

- 2026-07-13 Executed MT-356 second priced paper buy attempt. MT-355 pricing availability and paper-minimum eligibility were revalidated, but the immediate safe quote was unavailable at the selected candidate's execution point. Per task boundary, no priced trade or position was created and no additional paper state was written. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/second_priced_paper_buy_report.md`. This fails MT-357's priced-position precondition, so later mark/close tasks must skip. No live path, key, wallet action, config/filter change, or secret/raw provider output occurred.

- 2026-07-13 Executed MT-355 paper fill pricing audit. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/paper_fill_pricing_audit.md`. The MT-353 fill was unpriced because `PaperExecutionAdapter` received no valid quote at creation time, not because paper persistence loses prices. Existing `DexScreenerPriceProvider` is already safely wired into paper fills and only accepts finite requested-mint/wrapped-SOL pricing; its current sanitized probe for the latest paper mint returned `live_dexscreener`. No code change or provider expansion was needed. Paper fill pricing is therefore available for MT-356 only with immediate quote revalidation; if unavailable then, the new priced buy must stop. No live path, key, config, filter, DB schema, or trade change occurred in this audit.

- 2026-07-13 Executed MT-353 conditional first bounded paper trade after both precondition reports showed paper-minimum eligibility (MT-350 6/13; MT-352 2/12). Revalidated a current eligible candidate immediately before simulation with the explicit paper-only labels `paper_minimum_pass` plus deferred unknown honeypot/creator/buyer/launch-age evidence. Created exactly one existing paper-mode simulated BUY for `0.01 SOL`, then persisted its normal paper trade and position state in `data/trades.db`; the fill is `unpriced`, so no price, PnL, outcome, or performance conclusion exists. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-trade-reports/first_bounded_paper_trade_report.md`. Forced paper mode; no private key, live adapter/path, wallet action, real swap, config/filter/threshold change, or secret output occurred.

- 2026-07-13 Executed MT-352 reusable paper-minimum dry-run CLI. Added `paper-minimum-dry-run` and its bounded no-persistence helper in `src/cli.py`; defaults collect only PumpFun HTTP fallback (websocket disabled) and DexScreener/on-chain candidates, dedupe at a 25-mint maximum, use read-only enrichment, and apply strict plus pure paper-minimum gating. It never invokes paper-cycle, decision engine, execution adapter, position manager, or DB/ledger writes. Added network-free CLI/helper tests for source lifecycle, dedupe, required-evidence block labels, eligibility/deferred labels, and explicit report output. Demo report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-evaluation-reports/paper_minimum_cli_dry_run_report.md`; 12 unique mints, 0 strict passes, and 2 paper-minimum eligible with only paper-only deferred unknowns. Eight known top-holder failures and two age-unknown candidates blocked the rest. This independently satisfies MT-353's eligibility precondition, with no trade, config, runtime/live wiring, secret/raw provider output, PnL, or performance claim. Validation: focused CLI/paper-minimum/RugCheck/risk tests passed (98) and compileall passed.

- 2026-07-13 Executed MT-351 launch-age provenance audit. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-evaluation-reports/launch_age_provenance_report.md`. No mapping fix was safe or needed: `build_token_from_signal()` already maps explicit PumpFun `NEW_POOL` signals to `observed_at`, producing known strict age failures that the paper-only gate can defer only under explicit launch research mode. The age-unknown outcome belongs to conservative DexScreener/on-chain activity: its pair timestamp is not token creation provenance and its observed time must not be treated as token age. MT-349 tests already cover PumpFun launch deferral and non-launch age blocking. No provider call, code/filter/threshold change, DB/ledger, config, source/runtime/live wiring, trade, or secret/raw payload output occurred. MT-352 may proceed; MT-350 already found six eligible candidates for MT-353's later condition.

- 2026-07-13 Executed MT-350 larger bounded paper-minimum launch scan. Read-only PumpFun (`limit=10`, websocket disabled) and DexScreener profile limit 5 produced 13 unique mints; all 13 were enriched without paper-cycle, execution, or persistence. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/paper-evaluation-reports/larger_paper_minimum_scan.md`. Strict passes remained 0/13, but paper-minimum eligibility was 6/13: authority/freeze passed 13/13, liquidity passed 12/13, and top-holder evidence passed 7/13. The six eligible explicit PumpFun launches deferred only age, creator, honeypot, and unique-buyer unknowns under the named paper-only profile. Five known top-holder failures, one liquidity unknown, and one unknown age blocked the remaining candidates. This satisfies the eligibility prerequisite for MT-353 after MT-352, without any filter change, trade, DB/ledger, config, live/runtime wiring, raw payload, secret, PnL, or performance claim.


- 2026-07-13 Executed MT-348 fixable launch-evidence rerun. Updated `RugCheckClient` with a presence-aware authority extractor: explicit present-null `token.mintAuthority`/`token.freezeAuthority` now normalize to revoked, while absent paths and unrelated update/metadata/fee authorities remain unknown/ignored. Added fixture coverage for present-null and unrelated authority paths. Bounded direct PumpFun/DexScreener sample collected five unique launch mints and enriched three in memory without CLI/paper-cycle/DB/execution. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/evidence-rerun-reports/launch_evidence_rerun_report.md`. Mapping result: mint and freeze authority passed 3/3, replacing prior unknowns; strict and discovery-relaxed passes remain 0/3 because two top-holder and two strict age failures, plus creator/unique-buyer/honeypot unknowns and one liquidity/age unknown, remain. Holder lookup still reported `holder_lookup_failed_provider` for two applicable mints. No thresholds, filters, wallet config, source/runtime/live wiring, DB/ledger, trade, secret/raw payload output, or quality/outcome/policy claim changed. Validation: focused RugCheck/risk tests passed (85) and compileall passed.

- 2026-07-13 Diagnosed RugCheck authority/freeze schema with three bounded public launch reports. Contract report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/provider-contract-reports/rugcheck_authority_schema_contract.md`. All three reports were `ok` and consistently exposed `token.mintAuthority` and `token.freezeAuthority` as present `NoneType` fields, with duplicate top-level nullable representations. Current `RugCheckClient._extract_authority_flag()` recognizes these paths but its boolean extractor returns `None` for both present-null and absent fields, so `DiscoveryRiskScorer` leaves strict authority checks unknown. This confirms a narrowly scoped nullable no-authority mapping gap; unrelated `tokenMeta.updateAuthority` and metadata/transfer-fee authority categories are explicitly not mint/freeze evidence. Active non-null authority semantics remain unverified. A separate reviewed mapping task should add presence-aware present-null handling with fixture-only tests while preserving unknown for absent paths. No mapping/filter/scorer/config/runtime/live/persistence changes, DB write, trade, raw response, or secret output occurred.

- 2026-07-13 Audited launch evidence gaps after MT-346. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/source-diversity-reports/launch_evidence_gap_audit.md`. Static paths plus one bounded public PumpFun (`limit=1`) and RugCheck presence probe show that PumpFun preserves a creator address but supplies no creator percentage, buyer count/list, authority, or honeypot field; current creator check needs a percentage, and unique-buyer funding cannot run without buyer-wallet inputs. RugCheck holder mapping works when supplied (as in 2/3 MT-346 mints), while read-only holder lookup returned `holder_lookup_failed_provider` and intentionally hides the underlying error. The probe's RugCheck response was `ok` and had authority-related raw structure but normalized neither authority, so this is a schema/mapping ambiguity requiring a sanitized contract diagnostic before any mapping change; it had no honeypot verdict. Age remains a strict policy failure rather than an evidence gap. Recommended next work is separate, bounded diagnostics/contracts for RugCheck authority/freeze schema, holder RPC status, creator-token ownership percentage, buyer/trade data, and honeypot verdict/simulation input. No source/filter/config/runtime/live/persistence changes, DB write, trade, key/RPC output, raw payload retention, or quality/outcome/policy claim occurred.

- 2026-07-13 Executed MT-346 bounded read-only launch enrichment baseline. Collected three unique PumpFun launch mints with direct public HTTP fallback (`limit=3`, websocket disabled); DexScreener returned no launch candidates in this short snapshot. For the three-mint cap, ran existing `DiscoveryRiskScorer` evidence enrichment only: RugCheck report normalization, DexScreener/Jupiter explicit-liquidity fallback, and read-only holder lookup; funding analysis and honeypot simulation stayed disabled. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/source-diversity-reports/launch_enrichment_baseline_report.md`. Enrichment made liquidity known/pass for all 3 and yielded top-holder evidence for 2, but holder RPC failed for all 3, and authority, freeze, honeypot, creator, and unique-buyer evidence remained unknown. Strict and existing discovery-relaxed policies each passed 0/3: all 3 were strict age failures and 2 also failed top-holder threshold. This classifies the remaining state as strict-age block plus top-holder risk failures, holder-provider coverage gap, and inconclusive creator/buyer/authority/honeypot evidence; it makes no token-quality, outcome, or policy claim. No paper cycle/decision engine/execution/position manager, DB/ledger, wallet config, source wiring, threshold change, key/RPC output, raw provider payload, or trade occurred.

- 2026-07-13 Executed MT-345 bounded launch baseline without persistence. Used the MT-343 direct public launch monitors only: PumpFun HTTP fallback with websocket disabled and at most two `limit=10` requests, plus DexScreener `OnChainMonitor` at profile limit 5. Applied existing pure `risk.scorer.assess_signal()` with default `RiskConfig` directly in memory, avoiding enrichment/provider clients, settings/env loading, decision engine, position manager, execution adapter, CLI, and database. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/source-diversity-reports/launch_baseline_report.md`. Collection produced 12 unique mints (10 PumpFun `NEW_POOL`, 2 DexScreener `BUY`/`VOLUME_SPIKE`) with zero duplicates. Strict filter result: 0 all-check passes and 12 rejected/unknown; all lacked authority, holder, honeypot, creator, and unique-buyer evidence, 10 failed age, and 2 also had unknown liquidity. `run_bounded_paper_cycle()` was not used because it initializes SQLite and persists paper decision/trade telemetry, prohibited by this task; no simulated trade was created. No key/env access, wallet config, runtime wiring, persistence, or live/paper trade occurred.

- 2026-07-13 Executed MT-343 bounded read-only launch-source probe. Directly polled existing `PumpFunMonitor` with websocket disabled and at most two public HTTP fallback requests (`limit=10`), `OnChainMonitor` with DexScreener profile limit 5 (at most six public requests), and existing configured `WhaleWalletTracker` with at most two `limit=25` watched-wallet requests. Report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/source-diversity-reports/launch_source_probe.md`. PumpFun produced 10 unique `NEW_POOL` mints, DexScreener produced 3 unique `NEW_POOL`/`VOLUME_SPIKE` mints, and whale tracker produced 9 unique `BUY` mints from 12 signals at 25.00% duplicates. The fresh aggregate had 13 unique mints, 0 duplicates, and 0 whale overlap, so it was `more_diverse_in_this_snapshot`; 12 signals carried launch-class labels. A newer-than-whale conclusion remains unproven because one snapshot has no matched token-creation timestamps. No CLI/paper cycle, DB/ledger, wallet config, source/runtime wiring, key output, ranking, risk/execution action, or trade occurred.

- 2026-07-13 Executed MT-342 fresh-source compare gate. `MT-338` failed closed because no approved Helius global fresh-mint endpoint/RPC method, selector/program, request/response schema proving mint creation, one-page contract, tier entitlement/maximum credit cost, or real leaf transport exists. Per task instruction, made no provider call and wrote `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/source-diversity-reports/fresh_mint_blocked.md` listing those blockers and the one-shot retry bounds (2 requests, 10 references, 5 candidates, one page, zero retries, 10 seconds/request, 15 seconds total). Consequently no fresh unique-mint, duplicate-rate, or whale-overlap comparison exists. No key/environment value, whale tracker, wallet config, source/runtime wiring, persistence, ranking, risk/execution action, or trade was touched.

- 2026-07-13 Executed MT-340 same-window read-only wallet fixture comparison. Used the existing Helius watched-wallet enhanced-transaction poll once per fixture with `limit=25` and `token-accounts=balanceChanged`; collected normalized `Signal` JSON only for the two configured sample wallets plus five public article-listed, non-configured candidates (Cented, Theo, Decu, Heyitsyolo, Clukz). Output is under `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/candidate-wallet-fixtures/`: six fixtures and `wallet_compare_report.md`. Existing `compare_json_signal_fixtures()` validated all fixture records with zero invalid mints/wallet origins. Decision: `discard` for this window. The current baseline had 12 signals, 9 unique mints, and 25.00% duplicate rate; Cented 3/75.00%, Theo 2/81.82%, Decu 3/72.73%, Heyitsyolo 0/n-a, and Clukz 7/65.00%, so none met the predeclared greater-unique/no-worse-duplicate criterion for second-window collection. The comparison is fixture-count evidence only, not PnL, source quality, outcome, attribution, readiness, policy, or trading evidence. No wallet config, source/runtime wiring, trade, persistence, or repository data file changed.

- 2026-07-12 Planned MT-338 Helius live-call approval gate - no call or implementation. A future real paper diagnostic requires written approval of one exact Helius endpoint or RPC method, authentication placement, fixed selector/program/filter, request shape, pagination prohibition, response schema that proves a mint-creation event, and a sanitized fixture matching that schema; the generic MT-336 `{mint: ...}` parser contract is insufficient. The request must name its fixed window and why it is bounded. Before invocation, separately approve a one-invocation budget of at most 2 provider requests, 10 discovery references, 5 decoded candidate records, one page, zero retries, 10 seconds per request, and 15 seconds total, plus the provider-documented maximum credit cost for those calls. If the endpoint, schema, selector, cost, or tier entitlement is unknown, the gate fails closed without a call. A future leaf transport may read `HELIUS_API_KEY` only for request authentication after the approval; it must never log/display/store the key, RPC URL, authorization header, raw request/response, or unredacted error, and its caller may receive only contract alias, source label, state/reason code, cap/timeout status, total/unique/invalid/duplicate counts, and caller-supplied whale-overlap counts. It must run isolated from `build_signal_sources()`, aggregator, `paper-cycle`, decision/risk/execution code, wallet config, database/ledger, and all persistence; no result authorizes source wiring, ranking, pricing, PnL/outcome/attribution/readiness/policy claim, or trade. The comparison input is a caller-supplied, same-window normalized whale-mint snapshot, never a `WhaleWalletTracker` poll. Technical success requires the approved contract and budget, no cap/timeout breach, `ok` transport state, schema-valid records, and sanitized output. Comparison success requires at least one unique diagnostic mint absent from that whale snapshot; all diagnostic mints overlapping it is `repeat_comparison_fail`, while zero valid diagnostic mints is `inconclusive`, not proof of absence. Any provider error, unconfigured key, schema mismatch, unsafe output, or exceeded bound is `fail` and stops without retry. Separate approvals remain required for fake-to-real transport implementation, the single capped paper call, and any later source integration. No API, key, config, persistence, runtime, or trading behavior changed.

- 2026-07-12 Reviewed MT-337 Helius scaffold - PASS. MT-336 adds only a stdlib fake-transport parser and fake/in-memory tests. Its transport is an explicit caller-supplied callable, not an HTTP client; a blank contract returns `unconfigured` before calling it, and every provider exception becomes a fixed `provider_error` aggregate with no exception detail. The hard `islice` cap limits consumption to five records; only nonblank mapping `mint` strings affect unique/duplicate counts, while invalid records stay explicit in aggregate counts. The module has no Helius endpoint, API key/environment/config access, wallet config reference, database/ledger/persistence call, trading/execution/risk import, CLI command, SignalSource implementation, aggregator registration, or source selection wiring. Tests cover fake aggregate counts, bad records, duplicate rate, cap, blank-contract no-call behavior, and a fake secret-bearing error that does not surface. It is safe/useful as a parser scaffold only; live contract/schema/transport and any real call remain separately gated. This approves no outcome, PnL, attribution, readiness, policy, or trading claim.

- 2026-07-12 Implemented MT-336 fake-transport Helius fresh-mint diagnostic scaffold. Added `src/signals/helius_fresh_mint_diagnostic.py` with an explicit contract ID request, injected synchronous transport, fixed source label, and strict five-record consumption cap. It reduces only fake `{mint: ...}` records to aggregate total, unique, invalid, duplicate-rate, and state fields (`ok`, `unconfigured`, `provider_error`); blank contracts skip transport and exceptions return a sanitized aggregate report without raw error detail. It imports no HTTP, environment, config, database, wallet, execution, risk, or runtime wiring module and is not registered as a source or CLI command. Fake-only tests cover aggregate counts, duplicate rate, invalid records, cap enforcement, no-call blank contract, and provider-error redaction behavior. No Helius endpoint/schema/key behavior is implemented or implied. Validation: `python3 -m pytest -q tests/test_helius_fresh_mint_diagnostic.py tests/test_pump_fun_provider.py tests/test_whale_tracker_provider.py` (13 passed); `python3 -m compileall -q src/signals/helius_fresh_mint_diagnostic.py tests/test_helius_fresh_mint_diagnostic.py` passed; Ruff is unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Designed MT-335 bounded Helius fresh-mint diagnostic - paper-only and unimplemented. Do not add it to `build_signal_sources()`, `SignalAggregator`, `paper-cycle`, the decision engine, any database/ledger writer, or a live path. A future isolated `HeliusFreshMintDiagnostic` must accept an explicit `FreshMintDiagnosticRequest` containing only an approved provider discovery-contract identifier, a caller-supplied whale-mint set for in-memory overlap comparison, and bounded parameters clamped to at most 10 discovery references, 5 decoded candidate records, 2 provider requests, 10 seconds per request, 15 seconds total, one page, and zero retries. The contract must be separately documented and approved before live use: it needs an exact Helius endpoint/RPC method, fixed program or discovery selector, response schema proving a mint-creation event, and a reason the scan is bounded; the current audit does not establish any of these. Future transport is injected for all parser tests; fixtures cover valid creation events, duplicate mints, malformed responses, provider errors, cap enforcement, and no-key/no-raw-payload output. A future real transport may resolve `HELIUS_API_KEY` only inside the request path, never return/display the key, RPC URL, authorization header, raw request, raw response, or unredacted provider error. The report is in-memory aggregate-only: state (`unconfigured`, `provider_error`, `schema_unknown`, or `ok`), references examined, candidate/unique count, skipped reason counts, and overlap versus the caller-supplied whale set; it must not poll `WhaleWalletTracker`, alter wallet config, rank, filter, claim source quality, persist observations, fetch prices, evaluate risk, or trade. Compare fresh-mint evidence to repeated whales only as counts/overlap, never PnL, outcome, attribution, readiness, or policy. First implementation approval should authorize fake-transport tests only; a later separate approval is required for one explicit capped paper diagnostic call, and another for any source wiring. No API call, key access, data persistence, runtime/config change, or live behavior occurred in this design task.

- 2026-07-12 Reviewed MT-334 Helius source audit - PASS. MT-333 changed only `STATUS.md` and accurately reports that no Helius network-wide fresh-mint source exists in the signals layer: Helius is limited there to watched-wallet enhanced transactions, while PumpFun public subscriptions/fallbacks and DexScreener on-chain discovery are the only fresh-launch-capable paths. It correctly distinguishes in-memory per-process whale dedupe, current two-sample-wallet configuration, unquotaed aggregate ranking, code-only PumpFun overrides, and separate Helius readiness/funding adapters from actual fresh-mint sourcing. The audit makes no provider-availability claim because it did not call APIs or inspect keys. It leaves wallet config, runtime wiring, persistence, trading, and live behavior unchanged. Safest next source step: design a one-shot paper-only Helius fresh-mint diagnostic with an explicitly documented provider request contract, injected HTTP transport, fixture-based parser tests, strict cap/timeout, sanitized aggregate diagnostics, and no decision engine, paper-cycle, aggregation, database, key display, or live execution integration; do not implement or call it before separate approval. This supports no outcome, PnL, attribution, readiness, policy, or trading claim.

- 2026-07-12 Audited MT-333 Helius fresh-mint/new-token sourcing v2 - inspection only. There is no Helius-backed network-wide fresh-mint, new-token, launch, webhook, program-subscription, or mint-discovery source wired under `src/signals/`: its only signals-layer Helius URL is `WhaleWalletTracker`'s watched-address enhanced-transaction endpoint (`/v0/addresses/{wallet}/transactions`), which requires `HELIUS_API_KEY`, loops enabled rows in `config/wallets_to_track.yaml`, and caps each wallet request at `poll_limit=25`. Its signature and known-position dedupe is in-memory per tracker instance, so a new bounded CLI process reconstructs the tracker and can observe the same recent whale rows again; the current config contains only two enabled sample wallets. Helius RPC adapters elsewhere serve live-readiness/preflight/holdings and Helius funding serves risk analysis, not signal discovery. Default `build_signal_sources()` wires PumpFun, whale tracker, DexScreener on-chain monitor, and Twitter; the aggregator polls all sources but ranks pooled opportunities with no source quota, then bounded paper-cycle takes the top remaining capacity. Fresh-launch capability exists only outside Helius: `PumpFunMonitor` uses public PumpPortal `subscribeNewToken`/migration subscriptions plus two best-effort pump.fun HTTP fallbacks (50-row endpoint limit, 10-second poll default, junk/repeat/initial-liquidity filters), and `OnChainMonitor` uses DexScreener profiles (20-mint cap), pair/new-pool scoring, and five-minute emitted-mint dedupe. PumpFun overrides (`PUMP_FUN_WS_URL`, `PUMP_FUN_HTTP_URLS`) are code-only optional env reads and are absent from `.env.example`; its builder itself notes the provider contract is best-effort/TODO. The repeated whale-tracker observations are therefore consistent with watched-wallet-only Helius source semantics, per-process dedupe reset, two sample wallets, and unquotaed aggregate ranking, but this static audit does not establish current provider availability or why any non-whale live source returned zero. No APIs, keys, wallet config, persistence, trading, runtime, schema, or live behavior were touched.

- 2026-07-12 Planned MT-331 window-1 fixture run gate - no data or config change. First require the two explicit caller-supplied paths, nonblank window-1 labels, identical inclusive UTC `Z` start/end metadata, and `check_json_signal_fixtures()` state `complete`. Any checker state of `malformed`, `incomplete`, or `unmatched_window` is a `fail` stop: do not compare, request window 2, collect through this project, edit wallet config, or make an outcome/trading claim. Checker `inconclusive` is an `inconclusive` stop for this batch: record only the reason externally and do not compare or change config. After a complete quality gate, compare exactly the same paths. Window 1 is `complete_for_window_2_request` only when the candidate has at least one valid novel mint, complete fixture-level baseline-potential fields, documented source/wallet-origin mix, higher unique or novel mint count than the sample, and duplicate rate no worse than the sample. It is `fail` when the candidate has zero valid novel mints, missing required evidence, lower unique/novel count, or worse duplicate rate; it is `inconclusive` when complete fixtures produce a tie or mixed non-worsening metrics without the required strict improvement. Before requesting window 2, retain externally: exact labels/paths, shared UTC interval, quality state/reasons, comparator totals/unique/novel/duplicate metrics and rates, source/wallet origins, baseline-potential completeness, and the resulting gate state. A complete window 1 only authorizes a request for the second independent matched observation, not a wallet-config change, price/PnL/outcome/attribution/readiness/policy claim, or trade. No data was collected, no fixture/config file was created or changed, and no acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, credentials, live paths, or trading behavior changed.

- 2026-07-12 Reviewed MT-330 fixture manifest - PASS. The MT-329 document is a blank manual checklist only and matches the window-1 plan: exactly two future candidate/sample label and path placeholders, matching UTC `Z`-timestamp start/end fields, top-level-array and required Signal/whale-label completeness checks, and an explicit `complete`-before-comparator gate. It maps `malformed`, `incomplete`, `unmatched_window`, and `inconclusive` to stop conditions with no comparison, project-side collection, or wallet-config change, while retaining neutral results in external notes after a complete check. The committed diff leaves `config/wallets_to_track.yaml` unchanged and contains no data collection, acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credential, live-path, or trading behavior. It is safe and usable as a manual manifest only. This approves no outcome, PnL, attribution, readiness, policy, or trading claim.

- 2026-07-12 Implemented MT-329 window-1 fixture manifest template. Added `docs/WINDOW_1_FIXTURE_MANIFEST_TEMPLATE.md`, a manual checklist for the two future candidate/sample labels and paths, identical UTC `Z`-timestamp window fields, required top-level-array/Signal/whale-label completeness conditions, and a single external gate outcome. It requires `check_json_signal_fixtures()` to return `complete` before comparison; `malformed`, `incomplete`, `unmatched_window`, or `inconclusive` stops the batch with no comparison, project-side collection, or wallet-config change. The document is blank and does not create a fixture, collect data, persist an outcome, or call any runtime path. No wallet config, acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, credentials, live path, or trading behavior changed.

- 2026-07-12 Planned MT-328 first fixture batch - window 1 only, no collection. The future batch consists of exactly two manually supplied top-level JSON-array files, not files created by this task: `<candidate-label>-window-1.json` labeled `<candidate-label>-window-1`, and `<sample-label>-window-1.json` labeled `<sample-label>-window-1`. Before either file exists, the operator records one exact inclusive UTC interval as `window_start` and `window_end` in ISO-8601 `Z` form; both `FixtureCheckRequest` values must use byte-for-byte equivalent UTC instants, and every record's explicit timezone-aware `observed_at` must fall within that interval. Every record requires `source`, `type`, nonblank `mint_address`, and `observed_at`; whale records require nonblank `payload.tracked_wallet.label`. Window-1 quality passes only if `check_json_signal_fixtures()` returns `complete`; `malformed`, `incomplete`, `unmatched_window`, or `inconclusive` stops the batch with an external manual outcome of the same name and no comparator/config action. Only after complete quality may the operator compare those same two explicit paths. The diagnostic comparison outcome is `candidate_observation` only if the candidate has at least one valid novel mint, higher unique or novel mint count than its sample, no worse duplicate rate, complete baseline-potential fields, and recorded source/wallet-origin mix; otherwise it is `fail_or_inconclusive`. Neither outcome changes `config/wallets_to_track.yaml`, claims price/PnL/outcome/readiness/policy, or authorizes trading. A candidate observation merely supplies the first of two independent complete matched windows needed for a future separately approved wallet-config request. No data was collected, no fixture file or config was written, and no acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, credentials, live paths, or trading behavior changed.

- 2026-07-12 Reviewed MT-327 fixture template - PASS. The MT-326 documentation template correctly keeps the identical UTC `Z`-timestamp window metadata outside the JSON array while showing the explicit label/path/start/end inputs required for `FixtureCheckRequest`. Its JSON array supplies the checker's required raw `source`, `type`, nonblank `mint_address`, and timezone-aware `observed_at` fields, plus the required `payload.tracked_wallet.label` for the whale example; it also documents non-whale requirements, the complete-state quality gate, and the same-path comparator sequence. The placeholders are not collected fixture data. The committed diff leaves `config/wallets_to_track.yaml` unchanged and contains no acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credentials, live path, or trading behavior. It is safe and usable as a manual caller-supplied fixture template only. This approves no outcome, PnL, attribution, readiness, policy, or trading claim.

- 2026-07-12 Implemented MT-326 manual candidate wallet fixture template. Added `docs/CANDIDATE_WALLET_FIXTURE_TEMPLATE.md`, which shows the paired candidate/sample labels, identical UTC `Z`-timestamp window metadata kept outside each file, required top-level JSON-array record fields (`source`, `type`, nonblank `mint_address`, timezone-aware `observed_at`), and the required nonblank whale label for `WHALE_TRACKER`. It explains explicit `FixtureCheckRequest` construction, requires `check_json_signal_fixtures()` to return `complete` before `compare_json_signal_fixtures()`, and keeps results in external manual notes. The placeholder JSON is a template only, not a collected fixture. No wallet config, acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credentials, live path, or trading behavior changed.

- 2026-07-12 Planned MT-325 first candidate-wallet fixture collection runbook - manual-only. Do not collect through this project or change `config/wallets_to_track.yaml`. For one planned bounded interval, the operator first records an exact UTC start and end in ISO-8601 `Z` form, then manually supplies two separate top-level JSON arrays: one nonblank candidate label and one nonblank sample-comparator label. Every record must contain `source`, `type`, nonblank `mint_address`, and an explicit timezone-aware `observed_at` inside the inclusive UTC interval; whale records also require `payload.tracked_wallet.label`. Before comparison, create only in-memory `FixtureCheckRequest` values using the identical UTC start/end for both explicit paths and require `check_json_signal_fixtures()` to return `complete`; stop and label the observation malformed, incomplete, unmatched, or inconclusive if it does not. For a complete pair, run `compare_json_signal_fixtures()` over the same two explicit paths and record only its neutral totals, unique/novel mints, duplicates/rate, invalid counts, source mix, and wallet/source origins in external manual notes. One window is evidence only: it cannot edit wallet config or establish provider price, PnL, outcome, attribution, readiness, policy, or a trade. A later wallet-config-change request needs two independent complete UTC matched windows, each showing candidate valid novel mints and complete baseline-potential fields, reproducible unique-mint improvement over its sample comparator, no worse duplicate rate, and documented source-origin mix, followed by a separately approved decision. No data was collected, no fixture file was created, and no acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, credentials, live paths, or trading behavior changed.

- 2026-07-12 Reviewed MT-324 fixture checker - PASS WITH NOTE. MT-323 adds only the fixture-only `check_json_signal_fixtures()` helper and temporary JSON-file tests. It consumes explicit caller paths and caller-declared windows, classifies raw required-field absence separately from malformed JSON/structure or invalid normalized `Signal` data, preserves unmatched-window and zero-novel-mint inconclusive states, and returns in-memory diagnostics only. The committed diff leaves `config/wallets_to_track.yaml` unchanged and contains no acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credential, or live-path call. Tests cover complete, incomplete, malformed JSON and structure, unmatched declared windows, inconclusive empty fixtures, source/wallet origin content, and unchanged fixture bytes. Safe/useful for manual fixture quality gating only, not wallet selection or runtime use. Note: callers must provide timezone-aware declared windows matching the timezone-aware serialized `observed_at` values; MT-325 should require UTC fixture timestamps and UTC window metadata. This approves no outcome, PnL, attribution, readiness, policy, or trading claim. Validation retained: 16 focused tests and compileall passed; Ruff remains unavailable in the active environment.

- 2026-07-12 Implemented MT-323 fixture checker v2. Added `src/signals/novelty_quality.py` with an explicit `FixtureCheckRequest` path/label/declared-window contract and `check_json_signal_fixtures()`. It reads only caller-supplied JSON fixtures, requires raw `source`, `type`, nonblank `mint_address`, and explicit serialized `observed_at`; requires nonblank whale labels for `WHALE_TRACKER`; validates normalized `Signal` values; checks record timestamps against each declared window; and compares declared windows across the set. It reports `malformed` for unreadable/invalid JSON, top-level shape, structural records, or invalid normalized values; `incomplete` for missing required metadata; `unmatched_window` for record or declared-window mismatch; `inconclusive` for fewer than two fixtures or zero valid novel mints; otherwise `complete`. Temp-file tests cover all states and fixture-byte immutability. No wallet config, acquisition, aggregation, config, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credentials, or live path was added or called. Validation: `python3 -m pytest -q tests/test_novelty.py tests/test_novelty_fixture.py tests/test_novelty_compare.py tests/test_novelty_quality.py` (16 passed); `python3 -m compileall -q src/signals/novelty_quality.py tests/test_novelty_quality.py` passed; Ruff is unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Planned MT-321 candidate wallet fixtures - manual-only. For every bounded matched-window comparison, provide exactly one explicit top-level JSON array per candidate label and one for the sample comparator; labels must be nonblank and the candidate/sample windows must have the same manually recorded start/end interval. Each record must retain the normalized `Signal` fields `source`, `type`, nonblank `mint_address`, and an explicit serialized `observed_at` value in the supplied JSON. A `WHALE_TRACKER` record must additionally carry a nonblank `payload.tracked_wallet.label`; absent/invalid mint or whale-label records make the fixture incomplete rather than silently discarded. Run only `compare_json_signal_fixtures()` over explicit paths and record its neutral totals, unique and novel mints, duplicates, duplicate rate, source mix, invalid counts, and source/wallet origins in manual notes; do not write results into this project. Fixture validity passes only when parsing succeeds, labels/windows match, every whale record is labeled, and every candidate novel mint has a nonblank mint, explicit timestamp, source, and applicable label. A candidate is a diagnostic comparison pass only when it has at least one valid novel mint, more unique or novel mints than its matched sample, no worse duplicate rate, and documented source-origin mix; if its fixture is incomplete, has zero valid novel mints, a worse duplicate rate, or an unmatched window, label it fail/inconclusive and do not retest through project acquisition. Valid-baseline potential means this complete fixture evidence only, never a provider quote, price, PnL, or outcome. Any diagnostic pass merely qualifies a second matched fixture window; wallet configuration remains unchanged unless a separate approved decision finds the MT-317 two-window reproducibility criteria satisfied. No acquisition, aggregation, config, SQLite, evaluation, pricing, execution, DB, ledger, keys, live paths, outcome, PnL, attribution, readiness, policy, or trading behavior changed.

- 2026-07-12 Reviewed MT-320 fixture comparator v2 - PASS. MT-318 adds only the fixture-only `compare_json_signal_fixtures()` helper and temporary-file tests. It requires two or more nonblank caller labels mapped to explicit JSON fixture paths, preserves caller mapping order, delegates each path only through `summarize_json_signal_fixture()`, and reports neutral per-label novelty summaries, source/wallet origins, and valid-observation duplicate rates without selecting a winner. The committed diff leaves `config/wallets_to_track.yaml` unchanged and contains no acquisition, aggregation, config loading, SQLite/DB/ledger, evaluation, pricing, execution, persistence, credential, or live-path call. Temporary tests verify explicit fixture reads, unchanged bytes, ordered summaries, source/wallet fields, duplicate rates, blank labels, and insufficient fixtures. It is safe for manual caller-supplied matched-window diagnostics only; candidate selection, source collection, historical comparison, CLI exposure, or runtime integration still require separate review. This approves no outcome, PnL, attribution, readiness, policy, or trading claim. Validation retained: 10 focused tests and compileall passed; Ruff remains unavailable in the active environment.

- 2026-07-12 Implemented MT-318 matched fixture comparator. Added `src/signals/novelty_compare.py` with `compare_json_signal_fixtures()`, which requires at least two nonblank caller labels and explicit fixture paths, preserves mapping order, delegates only to `summarize_json_signal_fixture()`, and returns each label's total signals, unique/novel/duplicate mint summary, source mix, wallet/source origins, and duplicate rate over valid observations (`None` when there are none). It does not select a winner or promote a wallet. Temporary-file tests cover a sample/candidate comparison, ordered labels, summary/origin fields, duplicate rates, fixture-byte immutability, blank labels, and insufficient fixture count. No wallet config, acquisition, aggregation, config loading, SQLite/DB/ledger, evaluation, pricing, execution, persistence, keys, or live path was added or called. Validation: `python3 -m pytest -q tests/test_novelty.py tests/test_novelty_fixture.py tests/test_novelty_compare.py` (10 passed); `python3 -m compileall -q src/signals/novelty_compare.py tests/test_novelty_compare.py` passed; Ruff is unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Designed MT-317 wallet expansion screen - fixture-only, no wallet change. Current evidence is insufficient to add or enable a tracked wallet: the MT-305/306 record has 110 replayable observations but only four distinct valid provider-normalized baseline mints, 19 explicit provider failures, 75 historical `not_recorded` rows, and repeated five-mint whale samples. For a manual candidate-wallet comparison, prepare one caller-supplied top-level JSON `Signal` array per candidate and one matching sample-wallet comparator for the same bounded observation window; do not acquire data through this project. Run only `summarize_json_signal_fixture()` on explicit fixture paths, keep returned summaries in the operator's manual comparison notes, and leave fixture bytes, SQLite, config, and wallet watchlists unchanged. Record for each fixture: total valid/invalid observations, unique and novel mints, duplicates and duplicate rate (`duplicates / (novel + duplicates)` when the denominator is nonzero), source mix, and `WHALE_TRACKER` wallet-label origins. A mint has fixture-level baseline potential only when its supplied record has a nonblank mint, explicit serialized observation timestamp, source, and nonblank whale label where applicable; this is data completeness only, not a provider quote, price, outcome, or eligibility claim. Test a candidate only when its fixture has at least one valid novel mint and no unlabelled whale records; compare it against the matched sample fixture by higher unique/novel mint count, lower duplicate rate, and documented source-origin diversity. Do not add a candidate wallet unless separately approved after at least two bounded matched fixture windows show a reproducible unique-mint increase, no worse duplicate rate, and complete fixture-level baseline-potential fields; this task does not establish a numeric trading threshold or authorization. `config/wallets_to_track.yaml` remains unchanged. No acquisition, aggregation, config, SQLite, evaluation, pricing, execution, DB, ledger, keys, live paths, PnL, attribution, readiness, policy, or trading behavior changed.

- 2026-07-12 Reviewed MT-316 file novelty diagnostic - PASS. MT-315 adds only `summarize_json_signal_fixture(path)` plus temporary-file tests. The helper consumes one caller-supplied explicit JSON path, accepts only a top-level array of object records, validates those records as `Signal`, and delegates only to the existing in-memory `summarize_novelty()` helper. It does not default or discover paths; acquire signals; instantiate/call aggregation; resolve config/credentials; access SQLite/DB/ledger; evaluate, price, execute, persist, rank, filter, size, or enter a live path. Tests use temporary JSON fixture files and verify read-only bytes plus explicit malformed-input failures. It is safe for manual wallet/source novelty diagnostics from supplied fixtures only; any source acquisition, historical comparison, CLI exposure, or runtime integration needs a separately reviewed design. This approves no outcome, PnL, attribution, readiness, policy, or trading claim. Validation retained: 7 focused fixture/novelty tests and compileall passed; Ruff remains unavailable in the active environment.

- 2026-07-12 Implemented MT-315 file novelty diagnostic. Added `src/signals/novelty_fixture.py` with `summarize_json_signal_fixture(path)`, an opt-in helper requiring one explicit caller-supplied JSON file whose top level is an array of normalized `Signal` records. It validates each object then delegates only to `summarize_novelty()` and returns its counts; it has no default path, CLI, source acquisition, aggregation, config/credential lookup, SQLite/DB/ledger access, evaluation, pricing, execution, persistence, ranking, filtering, sizing, or live path. Temporary-file tests verify totals/source origins, fixture-byte immutability, and invalid top-level or record-shape rejection. Validation: `python3 -m pytest -q tests/test_novelty.py tests/test_novelty_fixture.py` (7 passed); `python3 -m compileall -q src/signals/novelty_fixture.py tests/test_novelty_fixture.py` passed; Ruff is unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-314 novelty summarizer - PASS. MT-313 adds only the pure `src/signals/novelty.py` diagnostic helper and in-memory `tests/test_novelty.py` fixtures. `summarize_novelty()` imports only the shared `Signal` contract, receives raw signals from its caller, computes sequence-local novelty/duplicate/source/origin diagnostics, and leaves inputs unchanged. It does not acquire signals, instantiate or call aggregation, resolve configuration/credentials, use SQLite/DB/ledger, evaluate risk, price, execute, rank, filter, size, or enter a live path; no CLI or runtime integration was added. The helper is safe as a diagnostic building block only. Future source acquisition, historical comparison, CLI exposure, or paper-cycle/aggregator integration requires a separately reviewed design. This approves no outcome, PnL, attribution, readiness, policy, or trading claim. Validation retained: 10 focused signal/novelty tests and compileall passed; Ruff remains unavailable in the active environment.

- 2026-07-12 Implemented MT-313 pure novelty summarizer v2. Added `src/signals/novelty.py` with caller-supplied, sequence-order-preserving raw `Signal` summaries: total, unique, novel, duplicate, invalid mint and whale-origin counts; source-level counts; and deduplicated per-mint source/optional existing wallet-label origins. Blank mints and malformed/missing whale labels remain explicit diagnostics rather than filters, while Solana address casing is preserved. Added only in-memory fixture tests for cross-source duplicates, origin accounting, invalid fields, and no input mutation. No CLI, source acquisition, aggregation, config, SQLite, evaluation, pricing, execution, DB, ledger, credential, sizing, ranking, filter, or live-path code was added. Validation: `python3 -m pytest -q tests/test_novelty.py` (4 passed); `python3 -m compileall -q src/signals/novelty.py` passed; Ruff is unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-311 read-only novelty diagnostic - IMPLEMENT (pure helper only). MT-310 changes only `STATUS.md` and defines duplicate versus novel coverage deterministically from caller-supplied raw signals: distinct nonblank mints, first occurrence novel, later occurrences duplicate, source-level counts, per-mint source origins, optional existing whale label, and explicit invalid/unknown handling. It neither constructs nor invokes sources, the aggregator, paper-cycle, a decision/risk path, price provider, execution adapter, persistence, SQLite, ledger, configuration/credentials, sizing, ranking, filters, or live behavior. Implement only the specified in-memory helper and focused fake-signal tests; do not add a CLI command, source acquisition, historical novelty comparison, or a paper-cycle/aggregator integration without a separately reviewed design. This authorizes no outcome, PnL, attribution, readiness, policy, or trading claim.

- 2026-07-12 Designed MT-310 read-only novelty diagnostic. Implement the diagnostic as a pure in-memory `summarize_novelty(signals)` helper over a caller-supplied bounded sequence of raw `Signal` records, not as a `paper-cycle` option, a source-polling command, or a `SignalAggregator` wrapper: the aggregator ranks/composites and can log, while paper-cycle initializes SQLite, evaluates, prices, and persists. Candidate-mint count is the number of distinct nonblank normalized mint addresses in the supplied sequence; the first raw occurrence of each mint is `novel`, and every later occurrence is `duplicate`. This is session novelty only, not a historical-freshness or provider-baseline claim: it must not read a database, rejected snapshots, ledger, or prior-run state. Source mix reports raw signal count, distinct-mint count, first-occurrence count, and duplicate-occurrence count by `signal.source`; a per-mint origin set records contributing source names, plus a whale-origin label only when the already-present `tracked_wallet.label` payload field is a nonblank string. Missing/invalid mint or origin fields are counted explicitly as unknown/invalid observations rather than filtered, inferred, or ranked. A separate future task would need to review any signal acquisition boundary; this helper must not construct/start/poll/stop sources or resolve configuration/credentials. It invokes no aggregator, decision engine, risk/filter logic, price provider, execution adapter, position/trade/decision persistence, database/ledger access, configuration/credential lookup or display, sizing, ranking, or live path. Focused tests should inject fake signals and prove aggregate counts, cross-source duplicate origin accounting, unknown-field handling, absence of the prohibited runtime components, and unchanged SQLite bytes when a path is supplied only to the test harness for comparison.

- 2026-07-12 Reviewed MT-309 source diversity fix design - REVISE. MT-308 changed only `STATUS.md` and correctly identifies source-agnostic prior-mint exclusion as the way to stop repeated whale-tracker samples, with no filter, live-path, ledger, key, or schema change. However, routing novel mints through `paper-cycle` is not strictly diagnostic-only: even `--fresh-evaluation-session` persists a paper trade for an accepted candidate, and aggregation logs signals before the proposed gate. Do not implement the proposed flag as written. Revise it into a separate read-only unique-candidate diagnostic that reads the bounded exclusion set, constructs the aggregator with no DB logger, filters after aggregation, and reports only novel/duplicate mint and source counts without calling `DecisionEngine`, a price provider, paper execution, or persistence. Required tests must prove default paper-cycle behavior and DB bytes remain unchanged, all-duplicate inputs report zero novel candidates, and novel inputs are reported without a decision/trade/provider call. A later separately approved design can decide whether any unique candidate should enter a baseline-capture workflow.

- 2026-07-12 Designed MT-308 source diversity fix v2. Smallest proposed implementation: add an explicit opt-in diagnostic-only `paper-cycle` flag that reads the existing bounded rejected-snapshot window, forms a source-agnostic exclusion set from mints with a previously recorded provider-mark status, then removes those mints after aggregation but before the five-signal cap and decision engine. Default paper-cycle behavior, source ordering, aggregation/ranking, strict filters, execution, and position handling remain unchanged. The opt-in summary should show duplicate mints skipped by source and evaluate/provider-capture/persist only novel mints; if no novel mint appears before timeout, report no fresh sample rather than forcing a duplicate. It must not write skip rows, alter schema/DB access patterns, change wallet config or keys, call live paths, or make PnL, outcome, attribution, readiness, or policy claims. Future focused tests should prove default behavior is unchanged, duplicates never reach decision/provider/persistence in opt-in mode, novel mints do, and an all-duplicate batch safely times out with zero collected diagnostics.

- 2026-07-12 Reviewed MT-306 fresh unique baselines 2 - PASS WITH NOTES. MT-305 changed only `STATUS.md`; its committed diff has no outcome, PnL, attribution, readiness, policy, filter, runtime, DB, ledger, key, or live-path change. Read-only bounded coverage confirms 110 replayable rows, 16 valid provider-normalized baselines/later-eligible rows, 19 explicit provider failures, 75 `not_recorded` rows, and four unique valid baseline mints. The five-mint MT-305 cycle naturally repeated the prior fresh-cycle mint set, so its three valid baselines did not expand unique baseline-mint coverage; all candidates remained rejected with zero trades or positions. Next evidence step: pause immediate collection. Do not collect contemporaneous later marks or repeat the same fresh workflow until a naturally distinct source sample can increase unique-mint coverage. This is not an outcome, PnL, attribution, readiness, or policy conclusion.

- 2026-07-12 Collected MT-305 fresh unique baselines 2 v2. The existing strict five-signal fresh-session paper cycle evaluated five unique whale-tracker mints within the cycle, rejected all five, and created no trades or positions. It did not force uniqueness; the source repeated the prior fresh-cycle mint set, so its three valid `live_dexscreener` baseline/later-eligible rows did not increase aggregate unique baseline-mint coverage beyond four. The fresh sample also preserved two explicit provider failures (`no_pairs` and `no_requested_mint_sol_pair`) and zero `not_recorded` rows. Bounded persisted coverage is now 110 replayable rows, 390 legacy/missing rows, 16 valid provider-normalized baselines/later-eligible rows, 19 explicit provider failures, and 75 historical `not_recorded` rows. Unknown and inconclusive states remain explicit. This is diagnostic-only: no outcome, PnL, attribution, readiness, policy, filter, runtime, schema, ledger, DB-client, key, or live-path change was made. Validation: focused capture tests passed (2 passed); compileall passed.

- 2026-07-12 Reviewed MT-303 fresh unique baselines - PASS WITH NOTES. MT-301 changed only `STATUS.md`; its committed diff has no outcome, PnL, attribution, filter-quality, readiness, policy, runtime, database, ledger, or live-path claim or change. Current bounded coverage is 105 replayable rows, 13 valid provider-normalized baselines/later-mark-eligible rows, 17 explicit provider failures, and 75 historical `not_recorded` rows. Although the fresh five-row cycle had five unique mints, its three valid baseline rows raise the aggregate only to four unique baseline mints because one repeats a prior mint. The new baseline captures are contemporaneous and the earlier repeated-mint later marks were inconclusive. The next evidence step is another bounded fresh collection that preserves unique-mint observation where naturally available, not immediate later-mark collection or a pause; this is not an outcome or policy conclusion.

- 2026-07-12 Collected MT-301 fresh unique baselines. The existing strict five-signal fresh-session paper cycle evaluated five unique whale-tracker mints, rejected all five, and created no trades or positions. It did not force uniqueness. Fresh provider capture recorded three valid `live_dexscreener` baselines and later-mark-eligible rows, two explicit failures (`no_pairs` and `no_requested_mint_sol_pair`), and zero fresh `not_recorded` rows. Bounded persisted coverage is now 105 replayable snapshots, 395 legacy/missing snapshots, 13 valid provider-normalized baselines/later-mark-eligible rows, 17 explicit provider failures, and 75 historical `not_recorded` rows. Unknown and inconclusive states remain explicit. This is diagnostic-only and makes no outcome, PnL, attribution, filter-quality, readiness, or policy claim; no runtime behavior, filters, ranking, sizing, execution, schema, ledger contract, DB client, key, or live-path code changed. Validation: focused capture tests passed (2 passed); compileall passed.

- 2026-07-12 Reviewed MT-300 bounded later marks - PASS WITH NOTES. MT-299 changed only `STATUS.md`; its committed diff contains no runtime/trading, filter, ranking, sizing, execution, live-behavior, database-schema, ledger-contract, or DB-client change. The persisted query confirms exactly 10 eligible baseline-linked rejected decisions, matching the MT-295 authorization; the bounded limit-10 collection used only those rows. Coverage still preserves 100 replayable rows, 10 provider-normalized marks, 15 explicit provider failures (`no_pairs` 10 and `no_requested_mint_sol_pair` 5), and 75 historical `not_recorded` rows. The 10 later marks cover two repeated mints only and remain `inconclusive`, so they support only more bounded fresh data collection, not PnL, attribution, readiness, filter-quality, policy, or outcome claims and not a new paper-mode logging step.

- 2026-07-12 Collected MT-299 bounded later marks v2 for exactly the 10 MT-295-approved persisted baseline-linked rejected rows (five observations each for `EbnthRfqnSLv6juqwwGHXbAn8SpdyH5zRC6SLKG1pump` and `9xtAfAmqnxUTEaSnA6QRVQFNrdDoR6QrCcmzZCwwpump`). The existing `paper-rejected-later-marks --limit 10 --marks live` workflow returned 10 `live_dexscreener` later marks; all rows remained `inconclusive`. It skipped 400 legacy/missing snapshots and 90 replayable rows without an eligible baseline. The SQLite database SHA-256 was unchanged before and after collection, preserving explicit unknown/missing/not-recorded and prior provider-failure evidence. This is diagnostic-only: no PnL, attribution, readiness, filter quality, policy, ranking, sizing, execution, live behavior, schema, ledger contract, or DB client change was made. Validation: focused later-mark and provider-coverage tests passed (2 passed).

- 2026-07-12 Reviewed MT-297 ledger import enablement gate - PASS. The MT-296 gate remains documentation-only and explicitly keeps imports disabled pending a separate review. It requires fixed explicit provenance, preserved unknown/unavailable evidence, `unknown`/`inconclusive` nested and import outcomes with `not_claimed`, linked redacted provider evidence, passing fail-closed fixture coverage, and no import-side PnL, attribution, performance, or outcome labels without separately linked evidence. No DB client, SQL, planner endpoint, runtime caller, SQLite dual write, trading, readiness, live behavior, keys, or transaction path was added.

- 2026-07-12 Implemented MT-296 - added a documentation-only historical import enablement gate to `MEMECOIN_DECISION_LEDGER_PATTERNS.md`. It requires preserved allowlisted provenance, explicit unknown/unavailable evidence, `unknown`/`inconclusive` plus `not_claimed` import outcomes, nested-outcome and provider-link validation, passing disabled-fixture coverage, runtime/SQLite isolation, and no PnL/attribution/outcome claim without separately linked evidence. It explicitly does not authorize implementation, DB access, dual writes, or trading behavior changes.

- 2026-07-12 Reviewed MT-295 provider coverage sample 4 - PASS WITH NOTES. MT-294 changed only `STATUS.md`; it made no runtime/trading behavior, filter, ranking, sizing, execution, PnL, attribution, live-readiness, database schema, ledger contract, migration, DB-client, key, or real-transaction-path change, and its strict paper observation created no trade or position. Compared with MT-290's 8 eligible baselines, 12 explicit failures, and 75 `not_recorded` rows, the current state is 10 eligible baselines, 15 explicit failures, and 75 `not_recorded` rows. The added five observations yielded 2 valid marks and 3 explicit failures, leaving recorded provider-attempt success at 40% (10 of 25). Ten baseline-linked rows now support a narrowly bounded later-mark collection task for those rows only, preserving explicit provider/unknown/not-recorded states; this does not support outcome, PnL, attribution, or policy claims. Do not tune filters, alter source handling, or change runtime behavior.

- 2026-07-12 Observed MT-294 provider coverage sample 4 v2. A strict bounded fresh-session paper cycle evaluated 5 whale_tracker candidates and rejected all 5 (2 liquidity unknown, 2 top10-holder failures, 1 top10-holder unknown), with 0 accepted trades and 0 created positions. The fresh post-rejection provider attempts recorded 2 `live_dexscreener` marks, 2 `no_pairs`, and 1 `no_requested_mint_sol_pair`. Persisted bounded coverage is now 100 replayable rejected snapshots, 400 legacy/missing, 10 valid provider-normalized marks and later-mark-eligible baselines, 15 explicit provider failures, and 75 historical `not_recorded` rows. Provider status remains explicit: `live_dexscreener` 10, `no_pairs` 10, `no_requested_mint_sol_pair` 5, and `not_recorded` 75. The evidence remains diagnostic-only and no PnL or outcome claim is made; no database schema, ledger contract, migration, DB client, filter, source-suppression, ranking, sizing, execution, attribution, readiness, live-trading, key, or transaction behavior changed. No report file was produced.

- 2026-07-12 Reviewed MT-292 disabled ledger import fixture harness - PASS. MT-291's test-only fixtures cover allowlisted `paper_decisions` provenance, an `UNKNOWN` risk check, unknown/not-claimed outcome state, and a provider observation linked to the decision. The existing rejection test uses the same provenance to reject a nested `measurable` outcome and unlinked provider evidence. The fixture reaches only the always-raising importer; no DB client, SQL, planner endpoint, runtime caller, SQLite dual write, trading, PnL, attribution, readiness, live behavior, keys, or transaction path was added.

- 2026-07-12 Implemented MT-291 - refactored disabled historical-import tests into explicit pytest fixtures for allowlisted SQLite `paper_decisions` provenance and a linked unknown/not-claimed import record. The fixture is exercised through the always-raising importer, while rejection coverage retains the nested measurable-outcome and unlinked-provider cases. This is test-only and adds no DB client, SQL, runtime caller, SQLite dual write, trading, PnL, attribution, readiness, live behavior, keys, or transaction path.

- 2026-07-12 Reviewed MT-290 provider coverage sample 3 - PASS WITH NOTES. MT-289 changed only `STATUS.md`; it made no runtime/trading behavior, filter, ranking, sizing, execution, PnL, attribution, live-readiness, database schema, ledger contract, migration, DB-client, key, or real-transaction-path change. Its strict paper observation produced no trade or position. Against the prior 90 replayable rows, 6 successes, 9 explicit failures, 75 `not_recorded`, and 6 later-eligible baselines, the sample raised totals to 95, 8, 12, 75, and 8 respectively: the five new observations yielded 2 valid marks and 3 explicit failures, so recorded provider-attempt success remains 40% (8 of 20) rather than improving. The eight baseline-covered rows remain too thin for a later-mark collection task or outcome claim. Next safe step: collect another bounded fresh provider-coverage observation, preserve explicit provider/unknown/not-recorded states, and do not change policy.

- 2026-07-12 Observed MT-289 provider coverage sample 3. A strict bounded fresh-session paper cycle evaluated 5 whale_tracker candidates and rejected all 5 (2 liquidity unknown, 2 top10-holder failures, 1 top10-holder unknown), with 0 accepted trades and 0 created positions. The fresh post-rejection provider attempts recorded 2 `live_dexscreener` marks, 2 `no_pairs`, and 1 `no_requested_mint_sol_pair`. Persisted bounded coverage is now 95 replayable rejected snapshots, 405 legacy/missing, 8 valid provider-normalized marks and later-mark-eligible baselines, 12 explicit provider failures, and 75 historical `not_recorded` rows. Provider status remains explicit: `live_dexscreener` 8, `no_pairs` 8, `no_requested_mint_sol_pair` 4, and `not_recorded` 75. The evidence remains diagnostic-only and too thin for an outcome claim; no database schema, ledger contract, migration, DB client, filter, source-suppression, ranking, sizing, execution, PnL, attribution, readiness, live-trading, key, or transaction behavior changed. No report file was produced.

- 2026-07-12 Reviewed MT-288 ledger import outcome validation fix - PASS. MT-287 validates nested decision outcome status as `unknown` or `inconclusive`, so a nested `measurable` outcome is rejected before a historical import can be constructed; the focused regression test covers the exact MT-282 bypass. Provenance, explicit unknown evidence, and provider-to-decision links remain preserved, and the importer still immediately raises. No DB client, SQL execution, runtime caller, planner endpoint, SQLite dual write, trading, PnL, attribution, readiness, live trading, keys, or real transaction path was added. This clears the MT-282 contract blocker only; imports remain disabled.

- 2026-07-12 Implemented MT-287 - constrained nested `LedgerDecisionEvidence.outcome_status` at the disabled historical-import boundary to `unknown` or `inconclusive`; a claimed nested outcome now fails validation before the importer is reached. The regression test preserves explicit unknown handling and verifies `measurable` nested outcomes are rejected. The importer still immediately raises with no database client, SQL, runtime caller, SQLite dual write, trading, PnL, attribution, readiness, live trading, keys, or transaction behavior.

- 2026-07-12 Reviewed MT-282 ledger import contract - FAIL. MT-281 keeps the importer fail-closed, provenance allowlisted, provider evidence linked to the decision, and production free of a runtime caller, planner endpoint, live DB connection, SQLite dual write, broad SQL access, trading, PnL, attribution, or readiness integration. However, `LedgerHistoricalImport` accepts a nested `LedgerDecisionEvidence` whose unrestricted `outcome_status` can be `measurable`, bypassing the import-level `unknown`/`inconclusive` outcome rule without linked baseline/later evidence. Do not enable imports. Smallest safe remediation: constrain or validate the nested decision outcome status for historical imports and add a regression test rejecting a claimed outcome; preserve the always-raising import entry point.

- 2026-07-12 Reviewed MT-284 provider coverage sample 2 - PASS WITH NOTES. MT-283 changed only `STATUS.md`; it made no source, test, migration, ledger, schema, or database-code change. Its existing strict bounded paper workflow evaluated 5 candidates, rejected all 5, and created no trade or position, while the post-rejection provider capture remained diagnostic-only and did not alter gates, ranking, sizing, execution, PnL, attribution, readiness, source suppression, thresholds, live trading, keys, or real transactions. Strictly, MT-283 was not database-free because the pre-existing workflow persisted the requested rejected-snapshot/provider-observation rows to the existing SQLite paper-decision store; this was operational evidence capture, not database migration, schema, or ledger work. Evidence remains too thin for later-mark collection or any outcome claim: 6 valid baselines/later-eligible rows versus 9 explicit provider failures and 75 historical `not_recorded` rows. Next safe main-project step: collect additional bounded fresh provider coverage using the established strict paper workflow, preserving explicit provider statuses and unknown/not-recorded states; do not add later-mark collection or change policy.

- 2026-07-12 Implemented MT-281 - added a disabled `LedgerHistoricalImport` contract for explicit `paper_decisions` exports only. It requires allowlisted SQLite provenance (source ID, observation timestamp, and explicit-export method), preserves `UNKNOWN` checks/provider states, requires every imported provider observation to link to the imported decision, and permits only `unknown`/`inconclusive` outcome status with `not_claimed` outcomes. The import entry point always raises; it has no database client, SQL, runtime caller, SQLite write, PnL, attribution, readiness, or trading behavior.

- 2026-07-12 Observed MT-283 provider coverage sample 2. A strict bounded fresh-session paper cycle evaluated 5 whale_tracker candidates and rejected all 5 (2 liquidity unknown, 2 top10-holder failures, 1 top10-holder unknown), with 0 accepted trades and 0 created positions. The fresh post-rejection provider attempts recorded 2 `live_dexscreener` marks, 2 `no_pairs`, and 1 `no_requested_mint_sol_pair`. Persisted bounded coverage is now 90 replayable rejected snapshots, 410 legacy/missing, 6 valid provider-normalized marks and later-mark-eligible baselines, 9 explicit provider failures, and 75 historical `not_recorded` rows. Provider status remains explicit: `live_dexscreener` 6, `no_pairs` 6, `no_requested_mint_sol_pair` 3, and `not_recorded` 75. The evidence remains diagnostic-only and too thin for an outcome claim; no database migration, filter, source-suppression, ranking, sizing, execution, PnL, attribution, readiness, live-trading, key, or transaction behavior changed. No report file was produced.

- 2026-07-12 Reviewed MT-280 migration constraint verification - PASS. MT-279 remains an opt-in test-only PostgreSQL fixture: absent `MEMECOIN_LEDGER_TEST_POSTGRES_DSN` skips before importing `psycopg` or opening a connection, an existing schema skips the fixture, and the test rolls back then asserts schema removal. Production `src/` has no ledger migration/DSN/PostgreSQL import or caller; SQLite remains the sole project runtime store, with no dual write, planner endpoint, broad SQL access, trading, PnL, attribution, or readiness integration. Next safe DB step, only with separate approval: run the existing constraint test against a newly provisioned disposable empty PostgreSQL database; do not add project DB configuration, runtime persistence, or schema expansion.

- 2026-07-12 Implemented MT-279 - extended the opt-in disposable PostgreSQL migration test to require an empty fixture, verify all four primary keys, core NOT NULL columns, expected foreign-key target relationships, and schema removal after rollback. It still requires `MEMECOIN_LEDGER_TEST_POSTGRES_DSN` plus lazy `psycopg`, with no project DB configuration or runtime caller. Manual disposable Docker PostgreSQL transaction validation confirmed the catalog checks and `rollback_clean = true`; the fixture was removed. Default focused suite: 8 passed, 1 skipped; compileall passed; Ruff unavailable.

- 2026-07-12 Reviewed MT-278 runtime safety audit - PASS. The MT-277 check is narrow and factual: it guards only runtime imports/references to the diagnostic ledger and synthetic harness plus runtime calls to the provider coverage command, while the commit-range audit separately confirms no strategy, execution, risk, signal, or chain change. The diagnostic batch preserved trading behavior, live-readiness, gates, ranking, sizing, execution, PnL, attribution, thresholds, live mode, keys, and real transaction paths.

- 2026-07-12 Audited MT-277 runtime safety drift v2 across MT-257 through HEAD. The commit range changed only the diagnostic ledger prototype/migration, test-only harness and migration fixture, provider coverage CLI presentation, tests, and status; no `src/strategy`, `src/execution`, `src/risk`, `src/signals`, or `src/chain` file changed. Live source contains no decision-ledger or synthetic-harness reference, and provider coverage appears only as its CLI definition. Extended static AST coverage to fail if runtime code imports the ledger/harness or calls the provider-coverage command. Gates, ranking, sizing, execution, PnL, attribution, readiness, thresholds, live mode, keys, and real transaction paths remain unchanged. Validation: 77 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-275 provider evidence summary - PASS. The compact table is a display-only aggregation of persisted rejected-snapshot evidence; it preserves exact provider status detail in the existing breakdown and leaves `not_recorded` distinct from explicit failures. It does not call providers or feed runtime policy, PnL, attribution, readiness, gates, ranking, sizing, execution, source suppression, or thresholds. Validation from MT-274: 69 focused tests and compileall pass; Ruff unavailable.

- 2026-07-12 Implemented MT-274 provider evidence summary view. `paper-rejected-provider-mark-coverage` now renders a compact read-only Rich table with replayable rejected snapshots, valid provider successes, explicit provider failures, historical `not_recorded` rows, and later-mark-eligible baselines, while retaining the exact status and source/mode/blocker detail below it. The summary reads only persisted snapshots and has no provider call, gate, ranking, sizing, execution, PnL, attribution, readiness, threshold, source-suppression, or live behavior effect. Focused test confirms the command remains read-only and renders the summary. Validation: 69 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-273 ledger migration verification - PASS WITH NOTES. MT-272 keeps all PostgreSQL verification opt-in and test-only: no DSN is configured in the project, `psycopg` is lazy-loaded only for an explicit disposable fixture, migration work rolls back, and the manual Docker fixture was removed. No runtime caller, planner endpoint, SQLite dual write, broad SQL access, trading, PnL, attribution, or readiness path was added. The successful table-creation check proves the initial migration applies, but not production integration readiness. If separately approved, the next safe database step is disposable-fixture verification of schema constraints and rollback behavior only; do not add a project database connection or any runtime ledger writer.

- 2026-07-12 Reviewed MT-271 fresh provider coverage - PASS WITH NOTES. MT-270 remained diagnostic-only: the bounded strict paper run and post-rejection provider capture did not alter gates, ranking, sizing, execution, PnL, attribution, readiness, source suppression, thresholds, live trading, keys, or transactions. It preserved explicit success/failure/not-recorded status and raised valid later-mark-eligible baselines from 2 to 4, but 4 baselines against 6 explicit failures and 75 historical not-recorded rows remain too thin to justify later-mark outcome collection or interpretation. Continue collecting bounded fresh provider coverage without policy changes.

- 2026-07-12 Implemented MT-272 - added an opt-in disposable PostgreSQL migration test gated by `MEMECOIN_LEDGER_TEST_POSTGRES_DSN`; it imports `psycopg` only when explicitly configured, applies the fixed migration, verifies exactly the four diagnostic tables, and rolls back before closing. No project DB config or runtime connection was added. Manual disposable Docker PostgreSQL validation applied the migration successfully and produced `decisions`, `provider_snapshots`, `rejection_records`, and `risk_check_results`; the fixture was removed afterward. Default focused suite: 8 passed, 1 skipped because no DSN/psycopg; compileall passed; Ruff unavailable.

- 2026-07-12 Observed MT-270 fresh provider coverage. A strict bounded fresh-session paper cycle evaluated 5 whale_tracker candidates and rejected all 5 (2 liquidity unknown, 2 top10-holder failures, 1 top10-holder unknown), with 0 accepted trades and 0 created positions. The fresh post-rejection provider attempts recorded 2 `live_dexscreener` marks, 2 `no_pairs`, and 1 `no_requested_mint_sol_pair`. Persisted coverage is now 85 replayable rejected snapshots, 415 legacy/missing, 4 valid provider-normalized baselines/later-mark-eligible rows, 6 explicit provider failures, and 75 historical `not_recorded` rows. The evidence remains diagnostic-only and too thin for an outcome claim; no filters, ranking, sizing, execution, PnL, attribution, readiness, source suppression, thresholds, or live behavior changed. Report: `provider-next-wave/MT-270-provider-coverage.md`. Validation: 69 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-267 provider coverage extension - PASS WITH NOTES. The MT-266 report remains a bounded, persisted-snapshot-only diagnostic with no provider calls, writes, or runtime caller in gates, ranking, sizing, execution, PnL, attribution, or readiness. It preserves the explicit `live_dexscreener`, `no_pairs`, `no_requested_mint_sol_pair`, and historical `not_recorded` states by source, mode, blocker, and later-mark eligibility. Evidence is unchanged at 80 replayable rows with only 2 successful baselines and 3 explicit failures, so it is insufficient for later-mark collection beyond the two already eligible rows or for outcome interpretation. Continue bounded provider-failure observation as new rejected snapshots accumulate; do not tune filters, suppress sources, or change runtime policy.

- 2026-07-12 Reviewed MT-269 disabled ledger adapter harness - PASS WITH NOTES. `SyntheticLedgerAdapter` exists only under `tests/`, keeps deep-copied records per process-local instance, rejects duplicate IDs, and exposes only the existing typed bounded filters; it has no DB/file/SQL surface and no runtime caller. Real ledger read/write functions still raise `LedgerPrototypeDisabledError`, and no trading, PnL, attribution, or readiness path changed. The next database branch, only under separate approval, is migration verification in a disposable non-runtime PostgreSQL fixture; do not add a project connection, planner endpoint, or runtime integration. Validation: 8 focused tests and compileall pass.

- 2026-07-12 Implemented MT-268 - added `tests/ledger_harness.py`, a test-only process-local synthetic adapter for the disabled ledger contracts. It stores deep-copied synthetic decisions/provider observations only per instance, rejects duplicate IDs, and applies only the existing bounded search filters; it has no database, file, SQL, or runtime dependency. Production ledger writer/reader stubs remain fail-closed. Validation: 8 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Observed MT-266 provider coverage v2 through the existing bounded read-only report, which already groups each persisted provider status by source, mode, blocker, and later-mark eligibility. The 500-decision slice is unchanged from MT-261: 80 replayable rejected snapshots, 420 legacy/missing, 2 `live_dexscreener` successes, 2 `no_pairs`, 1 `no_requested_mint_sol_pair`, and 75 explicit historical `not_recorded` rows. The only later-mark-eligible rows remain the two whale_tracker/unknown top10-holder successes; every failure/unknown remains explicit and no outcome is inferred. No gates, ranking, sizing, execution, PnL, attribution, readiness, source suppression, threshold, or live behavior changed. Validation: 69 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-262 provider failure coverage - PASS WITH NOTES. MT-261 reused the existing persisted-snapshot report, which is diagnostic-only and has no provider calls, writes, or caller in gates, ranking, sizing, execution, PnL, attribution, live-readiness, source suppression, or threshold tuning. The 80 replayable rows contain only 5 recorded provider attempts: 2 successes, 3 explicit failures, and 75 historical `not_recorded` rows; therefore evidence is insufficient for broader later-mark collection or any outcome interpretation. Continue bounded provider-failure coverage observation through the existing report as newly rejected snapshots accumulate, preserving explicit status taxonomy and treating historical absence as unknown. Do not tune filters, suppress sources, or change runtime policy.

- 2026-07-12 Reviewed MT-264 ledger tool contracts - PASS WITH NOTES. The MT-263 contracts permit only allowlisted decision/provider observation data, immutable decision lookup, and a bounded (1-100) mint/source/outcome search; unsupported fields are rejected and all four operations fail closed with `LedgerPrototypeDisabledError`. No runtime caller, PostgreSQL client, migration runner, broad SQL access, trading change, attribution/PnL claim, or readiness path was added. Smallest later step, only under separate approval, is a disabled planner-tool adapter test harness using synthetic requests; keep it disconnected from runtime and SQLite. Validation: 6 focused tests and compileall pass.

- 2026-07-12 Implemented MT-263 - expanded the inert ledger boundary with allowlisted typed contracts for decision recording, redacted provider observations, decision-ID lookup, and bounded decision search by mint/source/outcome status. Every operation still raises `LedgerPrototypeDisabledError`; there is no PostgreSQL client, SQL, runtime caller, migration runner, or broad access contract. Searches require at least one supported filter and cap results at 100; unknown checks/provider states remain explicit. Validation: 6 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Observed MT-261 provider failure coverage using the existing bounded read-only `paper-rejected-provider-mark-coverage` report; no redundant runtime/report code was needed because it already groups provider status by source, mode, and blocker. The 500-decision window contains 80 replayable rejected snapshots and 420 legacy/missing snapshots. Only 5 rows have recorded provider attempts: 2 `live_dexscreener` successes and 2 `no_pairs` under whale_tracker/unknown top10-holder blocks, plus 1 `no_requested_mint_sol_pair` under whale_tracker/unknown liquidity-unknown; 75 rows are historical `not_recorded`, not inferred provider failures. Pump.fun/launch has 48 not-recorded rows (17 age, 9 creator-unknown, 22 top10-holder); onchain/unknown has 3 and whale_tracker/unknown has 24 additional not-recorded rows. Only the 2 successful whale top10-holder baselines are later-mark eligible; no outcome quality or performance is inferred. No gates, ranking, sizing, execution, PnL, attribution, live-readiness, source suppression, threshold, or live behavior changed. Validation: 69 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Reviewed MT-258 diagnostic ledger prototype - PASS WITH NOTES. `0001_memecoin_decision_diagnostic.sql` is a standalone artifact with no migration runner, and `write_memecoin_decision` always raises without a database client or SQL. Runtime source has no import/call to the boundary; no broad SQL tool, trade/position/PnL/attribution/outcome/readiness table, gate, execution path, or live behavior was introduced. The schema preserves explicit `UNKNOWN` risk results; existing design rules still require verified attribution for PnL and linked baseline/later marks for non-inconclusive outcome labels. Smallest later step, only under separate approval, is non-runtime narrow planner read/write tooling over this diagnostic schema; do not add runtime integration or SQLite dual writes. Validation: 4 focused prototype tests and compileall pass; worktree clean before this review.

- 2026-07-12 Reviewed MT-260 provider mark observation v2 - PASS WITH NOTES. MT-259 remains diagnostic-only: its new report reads persisted rejected snapshots and does not invoke providers, mutate candidates, or feed gates, ranking, sizing, acceptance, execution, fills, PnL, attribution, source suppression, or live-readiness. The two `live_dexscreener` baselines mean later rejected-outcome collection is technically available for those rows, but the 50-row sample has only 2 marked rows, 3 explicit provider failures, and 45 historical `not_recorded` rows; the no-network later check produced 0 current marks and both labels stayed inconclusive. Next safe branch: collect and report additional bounded post-rejection provider observations with explicit failure taxonomy/coverage, then reassess later-mark coverage. Do not tune filters, suppress sources, infer outcomes, or change any runtime policy.

- 2026-07-12 Implemented and observed MT-259 provider-normalized rejection marks v2. Added `paper-rejected-provider-mark-coverage`, a bounded read-only report of persisted post-rejection provider marks that separates valid marks, explicit provider statuses, legacy `not_recorded` rows, later-outcome eligibility, and source/mode/blocker evidence without calling a provider or changing candidate state. Current 50-row evidence: 2 `live_dexscreener` marks, 2 `no_pairs`, 1 `no_requested_mint_sol_pair`, and 45 `not_recorded` historical rows. The two valid marks are whale_tracker/unknown top10-holder rejects; the explicit failures are two whale top10-holder `no_pairs` and one whale liquidity-unknown `no_requested_mint_sol_pair`. Pump.fun (34) and onchain (2) remain historical/not-recorded. Exactly 2 baselines are eligible for later collection; the default unavailable-provider follow-up returned 0 later marks and both remain inconclusive. No candidate was promoted, reranked, accepted, traded, resized, used for PnL, attribution, or live-readiness. Validation: 69 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-12 Implemented MT-257 - added a standalone PostgreSQL `memecoin_decision` diagnostic migration for only decisions, risk-check results, rejection records, and redacted provider snapshots. `src/core/decision_ledger.py` exposes an allowlisted evidence shape and a deliberately disabled writer boundary with no database client or SQL execution. Static checks prove runtime modules do not import/call it, only core diagnostic tables exist, unknown checks remain explicit, and no trade, position, PnL, attribution, outcome, or readiness table is added. SQLite remains the sole runtime state. Validation: 4 focused tests and compileall pass; Ruff unavailable as `python3 -m ruff` is not installed.

- 2026-07-11 Reviewed MT-254 decision-ledger plan - PASS WITH NOTES. The proposed `memecoin_decision` schema is observability-first: it preserves SQLite as the execution/paper source of truth; uses append-oriented normalized evidence; keeps unknowns explicit; requires linked valid baseline and later marks for non-inconclusive labels; and blocks PnL/performance attribution without verified links. Contracts are narrow, structured, and SQL-free, and no table or tool authorizes execution or readiness. Smallest safe future slice: a standalone migration for `decisions`, `risk_check_results`, `rejection_records`, and redacted `provider_snapshots`, plus `write_memecoin_decision` and `read_coin_history` tested with synthetic or explicit import data only. Do not add a runtime caller, SQLite dual write, trade/PnL tables, outcome writers, or learning tables in that slice.

- 2026-07-11 Implemented bounded read-only provider mark normalization at rejection. After a candidate is already rejected, the bounded paper cycle queries DexScreener once for each valid Solana mint and persists normalized provider mark, USD liquidity, provider/timestamp/status, or explicit failure reason; invalid non-Solana test mints are skipped without a network call. Source baselines remain as fallback only when provider collection fails. A five-signal fresh strict run rejected all five and created no trades/positions: two whale_tracker mints received valid `live_dexscreener` SOL baselines, while three remained explicit `no_pairs`/`no_requested_mint_sol_pair`. Outcome reports now surface baseline provider/status. No gate, ranking, acceptance, execution, fills, PnL, attribution, or readiness behavior changed. Validation: 85 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-253 - drafted a diagnostic-only `memecoin_decision` schema centered on immutable decisions, normalized risk/provider/rejection evidence, mirrored paper trade/position facts, exact attribution links, and baseline/later-mark outcome evidence. The proposed tools are narrow and planner-facing; learning tables remain future-only. No migration, tool, writer, or SQLite/runtime behavior changed.

- 2026-07-11 Reviewed MT-249/250 baseline coverage lane - PASS WITH NOTES. The field-coverage diagnostics are display-only persisted snapshot metadata and have no runtime caller in gates, ranking, acceptance, sizing, execution, fills, PnL, attribution, or live-readiness. Missing baseline/later marks remain explicit and inconclusive. Latest 50-row coverage: pump_fun/launch has 17 age and 17 top10-holder rejects, all `source_price_missing`; whale_tracker/unknown has 12 holder and 2 liquidity rejects, all `source_price_missing`; onchain/unknown has one creator-unknown record without field metadata and one top10-holder record with `metrics.liquidity_usd`/`dexscreener` provenance but no token SOL price. Next safe branch: separately scope bounded read-only provider mark normalization at rejection, preserving provider failure reasons. Do not tune filters, suppress sources, or implement a database ledger without measured outcome evidence. Validation: 82 focused tests pass; worktree clean; live-readiness NOT READY.
- 2026-07-11 Implemented/observed MT-250 fresh baseline coverage. Extended `paper-rejected-baseline-coverage` to group bounded evidence by source/mode/first blocker, then ran a fresh strict 25-signal session: 25 rejected, 0 accepted, 0 trades, 0 positions (15 holder, 9 age, 1 liquidity blocks). All fresh rows remain `source_price_missing` / baseline-missing. The current 50-row report confirms no recognized price field for pump_fun launch or whale_tracker unknown groups; only an onchain top-holder row exposes `metrics.liquidity_usd` with `dexscreener` provenance, still without a token SOL price. Missing marks remain explicit and no candidate was promoted, reranked, resized, used for PnL, or used for readiness. Validation: 82 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Diagnosed MT-249 baseline coverage gap. The baseline parser reads only normalized `price_sol`, `priceSol`, and `priceNative`; fresh pump_fun events preserve raw payload but the observed 17 launches had no supported price field, whale_tracker carries Helius transaction/transfer metadata without price/liquidity price fields, and onchain carries `metrics.liquidity_usd` but not a token SOL price. Composite opportunities inherit the latest source payload rather than merging a price. Added `paper-rejected-baseline-coverage`, a display-only bounded field-presence report; existing 50 snapshots predate this metadata and truthfully show field coverage `not_recorded` (with 445 legacy missing snapshots skipped). New snapshots retain only present field names/provider, not raw values. Next safe step: separately scope bounded read-only at-rejection quote capture through the existing DexScreener provider, retaining explicit failure reasons; do not change filters or runtime policy. Validation: 82 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-252 - documented Hive/Custodian patterns and SQLite compatibility constraints for a future `memecoin_decision` observability ledger. The document confines any future ledger to narrow tool access, normalized/provenanced evidence, explicit unknown states, and diagnostic-only consumers; it excludes migrations, runtime writes, SQLite replacement, and all trading behavior changes.

- 2026-07-11 Reviewed MT-244 through MT-247 fresh outcome collection - PASS WITH NOTES. Baseline and later-mark fields are bounded diagnostic metadata only: they are neither read by nor wired into gates, ranking, acceptance, execution, fills, PnL, attribution, or live-readiness paths. Historical and fresh missing marks remain explicit/inconclusive; current collection has 50 replayable records without a baseline, 445 legacy/missing snapshots skipped, and zero candidates eligible for a later mark. The fresh strict run itself produced 25 rejects and no trade. Next safe branch: provider/data mark-coverage investigation to determine why fresh source signals lack normalized baseline price fields; do not alter filters, source selection, or runtime policy. Validation: 83 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-247 later-mark observation. Current bounded read-only collection checked 0 baseline-covered rejected candidates, found 0 later marks, 0 later-liquidity observations, 0 measurable outcomes, and 0 label changes; all outcomes remain inconclusive. It skipped 50 replayable records for missing baseline and 445 legacy/missing snapshots. No source/rejection-reason breakdown is measurable because no row qualified for a provider call. No candidate behavior changed. Validation: 83 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-246 - `paper-rejected-later-marks` fetches bounded later read-only marks only for rejected snapshots with a valid persisted baseline. It reports mark price, selected-pair USD liquidity, provider/status/timestamp, and retains evidence labels inconclusive unless both baseline and later mark exist; missing-baseline rows never trigger a provider call. Current bounded output: 0 baseline-covered candidates, 0 later marks, 50 baseline-missing fresh/replayable records, and 445 legacy/missing snapshots skipped (with traded rows excluded). No ranking, gates, acceptance, execution, fill, PnL, attribution, or readiness behavior changed. Validation: 83 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-245 fresh baseline observation. A bounded strict fresh-session run evaluated 25 live-discovered signals (pump_fun 17, whale_tracker 7, onchain 1), rejected all 25, accepted 0, created 0 trades/positions, and persisted fresh rejected snapshots. Baseline coverage was 0/25: every new snapshot has explicit `unavailable` / `source_price_missing` provenance, so no performance is inferred. Rejection liquidity is available for 19 rows and unavailable for 6 whale-tracker rows. Main fresh blockers: top10 holder 15, age 8, creator unknown 1, liquidity unknown 1. No candidate was promoted, reranked, resized, used for PnL, or used for readiness. Validation: 82 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-244 - new rejected candidate snapshots now persist bounded baseline diagnostics at rejection: normalized source-supplied SOL mark when available, provider (`signal_payload` or `unavailable`), UTC timestamp, explicit `source_price_missing` reason, and normalized rejection liquidity. Numeric source price strings are safely normalized only in the baseline parser. Existing historical rows remain missing-baseline rather than backfilled or assigned performance. No gates, ranking, acceptance, execution, fills, PnL, attribution, or readiness behavior changed. Validation: 82 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-237/238/240/241 rejected-outcome lane - PASS WITH NOTES. Outcome tracking and labels are bounded read-only diagnostics over persisted rejected snapshots plus an optional read-only price provider; they have no runtime caller in aggregation, gates, ranking, sizing, execution, fills, PnL, attribution, persistence, or live-readiness paths. Labels require stored baseline plus later valid mark and preserve all missing evidence as `inconclusive`. Current 25 rows are all inconclusive due to absent baseline/current default marks and only ~10h age; 465 legacy rows remain explicit missing/skipped. Next safe branch: keep collecting fresh rejected outcomes until source-supplied normalized baselines and sufficient age produce measurable evidence; do not adjust filters, provider behavior, or sourcing yet. Validation: 81 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-241 outcome-label observation. Default/no-network current output labels all 25 replayable rows `inconclusive`; breakdown: pump_fun creator unknown 9 and holder 5, whale_tracker holder 8/creator 1/liquidity 1, onchain creator 1. All 25 remain inconclusive because each lacks a stored baseline and later default mark, and all are only about 10h old (0-24h bucket), so no measurable outcome/maturity claim is made. 465 legacy rows remain skipped. No candidate was promoted, traded, reranked, resized, or accepted. Validation: 81 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-240 - `paper-rejected-outcome-labels` labels only evidence with a valid stored rejection baseline and later valid mark. Labels are `good_block_dumped` at <=0.5x, `good_block_liquidity_failed` only with a real zero current USD-liquidity observation, `possible_too_strict_pumped` at >=2x for strict-risk blockers, `data_issue_possible` at >=2x for unknown-data blockers, else `inconclusive`. The selected read-only DexScreener pair now exposes optional USD liquidity for this diagnostic. Current default/no-network output is truthfully 25 `inconclusive`: pump_fun creator unknown 9 and holder 5, whale_tracker holder 8/creator 1/liquidity 1, onchain creator 1; 465 legacy rows skipped. No ranking, gates, acceptance, execution, fill, PnL, attribution, or readiness behavior changed. Validation: 81 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-238 rejected-outcome observation. Bounded 25-row default/no-network tracker result: 25 considered/replayable, 0 baseline marks, 0 current marks, 25 missing baselines, 25 missing current marks, 25 unavailable rejection-liquidity fields, and all rows in the 0-24h age bucket. Every outcome is `inconclusive`/not computable; no pumped, flat, dumped, or rugged conclusion is claimed. By persisted rejection reason: top10-holder 13, creator unknown 11, liquidity unknown 1. By source: whale_tracker 10, pump_fun 14, onchain 1. No candidate was promoted, traded, reranked, resized, or accepted. Validation: 63 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-237 - `paper-rejected-outcomes` is a bounded paper/research report over persisted rejected snapshot evidence. It can use the existing read-only DexScreener mark provider only when `--marks live` is requested; default marks are unavailable/no-network. It shows source/mode/blocker, age since rejection, optional normalized rejection baseline, current mark, return multiple only when both are valid, and rejection-time liquidity when stored. Current 25 replayable rows truthfully report 25 `missing_baseline`, 25 unavailable default marks, no computable return multiple, and unavailable legacy rejection liquidity; 465 legacy snapshots are skipped. Future rejected snapshots retain supplied normalized price/liquidity evidence only. No ranking, gates, acceptance, execution, fill, PnL, attribution, or live-readiness behavior changed. Validation: 63 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-232 through MT-235 discovery-quality diagnostics - PASS WITH NOTES. Source buckets and weak-source preview are isolated display-only CLI views over persisted non-traded snapshot evidence; they neither call nor feed aggregator, ranking, suppression, risk gates, decision, sizing, execution, fills, PnL, attribution, persistence, or live-readiness paths. Both reconcile to 25 replayable rejected snapshots (whale_tracker 10, pump_fun 14, onchain 1) and retain 465 legacy rows as explicit missing/skipped evidence. Current labels are whale `severe_risk_source`, pump_fun `provider_data_gap`, and onchain `sparse_sample`; no unchanged-gate pass exists. Next safe branch: provider/data-reliability investigation for unknown-heavy pump_fun/launch (9 creator unknowns), without source filtering or runtime change; whale severe-holder evidence remains a separate future candidate-supply quality investigation. Validation: 61 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-235 weak-source preview observation. Real labels: whale_tracker/unknown `severe_risk_source` with 10 rejects (8 top10-holder failures, 1 creator unknown, 1 liquidity unknown, 2 unknown-data blocks); pump_fun/launch `provider_data_gap` with 14 rejects (9 creator unknown, 5 holder failures, 9 unknown-data blocks); onchain/unknown `sparse_sample` with 1 creator unknown. Source samples are 10, 14, and 1 respectively; the onchain label is explicitly non-actionable due to its one-row sample. All 25 replayable rows remain rejected and 465 legacy rows remain missing/skipped; no suppression, filtering, promotion, trade, rerank, resize, or acceptance occurred. Validation: 61 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-234 - `paper-recheck-snapshot-weak-source-preview` maps persisted source buckets into display-only investigation flags. Real preview: whale_tracker/unknown `severe_risk_source` from 8 top10-holder failures; pump_fun/launch `provider_data_gap` from 9 creator unknowns; onchain/unknown `sparse_sample` from one creator unknown. It explicitly states flags are not ranking, suppression, acceptance, or trading signals, and is not wired into aggregation, decision, sizing, risk, execution, or readiness paths. Validation: 61 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-233 discovery source-bucket observation. Real bounded output: whale_tracker/unknown is `severe_holder_heavy` with 10 rejects, primary top10-holder blocker, 8 severe holder failures, and 2 unknown-data blocks; pump_fun/launch is `unknown_data_heavy` with 14 rejects, primary creator-unknown blocker, 4 near-threshold and 1 severe holder failure, and 9 unknown-data blocks; onchain/unknown is a one-row `sparse_sample` creator unknown. Snapshot totals remain 25 rejected/replayable and 465 legacy missing/skipped. No candidate was promoted, traded, re-ranked, resized, or accepted. Validation: 60 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-232 - `paper-recheck-snapshot-source-buckets` assigns deterministic display-only source/mode labels from persisted rejected snapshot evidence. Real buckets: whale_tracker/unknown `severe_holder_heavy` (10 rejected, 8 severe holder, 2 unknown data); pump_fun/launch `unknown_data_heavy` (14 rejected, 9 unknown data, 4 near-threshold and 1 severe holder); onchain/unknown `sparse_sample` (1 creator unknown). Each row reports primary blocker, holder severity, unknown-data count, and bounded examples. Labels are not trading signals and change no gates, ranking, sizing, acceptance, execution, fill, PnL, attribution, persistence, or live readiness. Validation: 60 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-229/230 source-quality lane - PASS WITH NOTES. Both commands read only persisted non-traded replayable snapshots, retain 465 legacy rows as explicit missing/skipped evidence, and are labeled diagnostic-only/non-signal. Real source totals reconcile to 25 rejects: pump_fun/launch 14 (9 creator unknown, 5 holder failures), whale_tracker/unknown 10 (8 severe holder failures, 2 unknown-data blocks), and onchain/unknown 1 creator unknown. No ranking, scoring, gate, acceptance, execution, fill, PnL, attribution, persistence, or live-readiness behavior changed. There is no unchanged-gate pass evidence. Next safe branch: improve candidate supply/discovery quality, beginning with investigation of the whale_tracker severe-holder cluster; do not alter its selection behavior. Validation: 58 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-230 - `paper-recheck-snapshot-source-quality` gives a concise diagnostic-only source/mode summary of replayable rejected snapshots: rejected total, top blockers, holder severity, unknown-data count, and normalized recent mint examples. Real output: whale_tracker/unknown has 10 rejects (8 severe holder, 2 unknown data); pump_fun/launch has 14 rejects (9 creator unknown, 4 near-threshold holder, 1 severe holder); onchain/unknown has 1 creator unknown. It is explicitly not a trading signal and changes no ranking, scoring, gate, acceptance, execution, fill, PnL, attribution, or live-readiness behavior. Validation: 58 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Implemented MT-229 - `paper-recheck-snapshot-risk-sources` is a display-only grouping of rejected replayable snapshot dry-runs by source, mode, first blocker, holder bucket, and normalized candidate context. Real 25-row output: whale_tracker/unknown produces 8 repeated severe top10-holder failures; pump_fun/launch produces 4 repeated near-threshold and 1 severe holder failure. Creator unknowns are pump_fun/launch 9, onchain/unknown 1, and whale_tracker/unknown 1; one whale_tracker liquidity unknown remains. No ranking, scoring, gate, acceptance, execution, fill, PnL, attribution, or live-readiness path changed. Validation: 57 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-226/227 snapshot dry-run lane - PASS WITH NOTES. The command reads only non-traded snapshot rows, skips all 465 legacy missing snapshots, and applies the existing fail-closed `RiskAssessment.all_checks_pass` gate to bounded normalized check outcomes; older snapshots preserve only their recorded blocker while other checks remain UNKNOWN. It does not call decision, ranking, sizing, execution, fill, PnL, attribution, persistence, or live-readiness paths. Any future dry-run pass is re-evaluation evidence only, never acceptance or strategy proof. Real results remain 0 passes / 25 rejects: explicit top10 holder failures (13) narrowly exceed creator/liquidity data gaps (12), so the next safe branch is discovery-quality analysis, not a paper observation or provider reliability change. Validation: 56 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Recorded MT-227 snapshot dry-run observation. Real bounded output: 25 replayable snapshots, 465 legacy missing/skipped snapshots, 0 dry-run passes, and 25 fail-closed dry-run rejects. First failures were top10_holder_check 13, creator_holding_unknown 11, and liquidity_unknown 1. Every result remains diagnostic-only/rejected; no candidate was traded, promoted, ranked, sized, filled, attributed, or evaluated for live readiness. Validation: 56 focused tests pass; live-readiness remains NOT READY.
- 2026-07-11 Implemented MT-226 - `paper-recheck-snapshot-dry-run` deterministically replays only bounded snapshot-covered paper candidates through the existing fail-closed normal risk gate. It reports replayable, legacy missing/skipped, pass, reject, and first-failure counts; no dry-run result can execute, rank, size, accept, persist, change attribution, or touch live readiness. Fresh snapshots now retain normalized per-check outcomes only; older covered snapshots retain their recorded blocker and otherwise remain fail-closed. Current output: 25 replayable, 465 skipped legacy, 0 dry-run pass, 25 reject (holder 13, creator unknown 11, liquidity unknown 1). Validation: 56 focused tests and compileall pass; live-readiness remains NOT READY.
- 2026-07-11 Reviewed MT-223/224 snapshot lane - PASS WITH NOTES. Snapshot persistence uses a fixed safe normalized allowlist and stores no secrets, raw providers, transactions, headers, or execution artifacts. Readiness truthfully distinguishes 25 fresh replayable snapshots from 465 legacy missing snapshots, and snapshot fields remain diagnostic/replay inputs only with no gate, acceptance, ranking, execution, fill, PnL, attribution, or readiness effect. Next safe step is deterministic display-only dry-run rechecks from the fresh snapshots; more collection is needed for broader coverage. Live-readiness NOT READY. Validation: 165 focused tests and compileall pass.
- 2026-07-11 Recorded MT-224 raw-safe snapshot observation. Real readiness output reports 490 queued rejected candidates, 25 snapshot-covered/replayable fresh candidates, and 465 explicit legacy missing snapshots. Main historical blocker categories remain holder 315, creator unknown 147, liquidity unknown 20, capacity 7, age 1. Fresh snapshot candidates remain rejected/display-only; no normal full check, trade, position, or PnL mutation occurred. Live-readiness NOT READY.
- 2026-07-11 Implemented MT-223 raw-safe snapshot lane. New non-traded paper decisions persist a bounded allowlist snapshot of normalized candidate, risk, holder, creator, liquidity, provenance, and blocker fields; no raw provider payloads, headers, keys, or transactions are stored. `paper-recheck-snapshot-readiness` reports coverage/replayability without running a recheck. Fresh bounded soak produced 25 replayable snapshots; 465 legacy decisions remain explicit missing snapshots. Validation: 165 focused tests and compileall pass; 0 trades, no gate/execution/PnL/live change, live-readiness NOT READY.
- 2026-07-11 MT-219 observed paper recheck queue: real output contains 15 creator `data_fix`, 2 liquidity `provider_retry`, 3 `threshold_review+full_recheck`, and moderate holder `full_recheck` entries; all remain rejected and attribution stays BLOCKED. MT-220 dry-run recheck - BLOCKED: persisted decision telemetry is normalized diagnostics, not a replayable raw signal/provider snapshot. Re-running current risk checks would require new provider collection or reconstructing historical inputs, so no dry-run was implemented and no state was mutated. MT-221 review - PASS WITH NOTES: queue is display-only and dry-run correctly stopped at the replayability boundary; safe next step is persist bounded raw-safe recheck snapshots under a separately scoped task. Live-readiness NOT READY.
- 2026-07-11 Implemented MT-218 - `paper-recheck-queue` display-only queue from persisted replay evidence. It lists only `provider_retry`, `data_fix`, `threshold_review+full_recheck`, and `full_recheck` hints; every entry remains rejected and it invokes no risk or execution path. Current output includes 15 creator data-fix rows, 2 liquidity provider-retry rows, 3 threshold-review/full-recheck rows, and moderate full-recheck cases. Validation: 165 focused tests pass; attribution BLOCKED and live-readiness NOT READY.
- 2026-07-11 Reviewed MT-213/214/215 replay hint lane - PASS WITH NOTES. Bucket summaries and hints are evidence-based display-only follow-up labels; no rejected candidate becomes accepted, no hint invokes execution, and threshold cases require full re-evaluation. Attribution remains exact-link-only/BLOCKED and live-readiness remains NOT READY. Safe next step is resolve provider/data gaps and collect more observations, not relax gates. Validation: 165/165 focused tests pass.
- 2026-07-11 Recorded MT-215 hinted replay observation. Real 50-row replay displays 15 `data_fix` creator missing-field rows, 2 `provider_retry` liquidity-unavailable rows, 28 `severe_skip` holder rows, 3 `threshold_review+full_recheck` rows at the 60% hypothetical cap, and remaining moderate/other full-recheck labels. Every row remains rejected; no paper trade was forced or entered. Paper state remains 4/0, PnL 4/4 high-confidence marks, attribution BLOCKED, and live-readiness NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-214 replay action hints. `paper-candidate-replay` now renders display-only evidence-based follow-up labels: `provider_retry`, `data_fix`, `threshold_review+full_recheck`, `severe_skip`, `unknown_hold`, or `full_recheck`. Hints do not imply acceptance or alter any decision state. Current replay shows creator missing-field rows as `data_fix`, liquidity unavailable rows as `provider_retry`, severe holder rows as `severe_skip`, and three 60% review rows as `threshold_review+full_recheck`. Validation: 165 focused tests pass; attribution BLOCKED and live-readiness NOT READY.
- 2026-07-11 MT-213 replay bucket summary. Current 50 persisted rejected rows divide into 15 creator `missing_field` provider/data-gap rows, 2 liquidity `unavailable` rows, 28 severe holder-risk rows, 3 moderate holder rows, 2 near-threshold rows, and 4 numeric 60% threshold-review/full-recheck candidates. Every candidate remains rejected; counts are safe next-step labels only, not acceptance or PnL claims. Attribution remains BLOCKED and live-readiness NOT READY.
- 2026-07-11 Reviewed MT-208/209/210 provider replay wave - PASS WITH NOTES. Provider categories are derived only from persisted provenance, fresh observation forced no trade (25 rejected), and replay is explicitly display-only: scenario review flags do not alter rejection state or create trades. Unknown rows remain blocked, attribution remains exact-link-only and BLOCKED, and live-readiness remains NOT READY. Safe next step is collect more provider-coverage observations and investigate provider reliability, not relax gates. Validation: 165/165 focused tests pass.
- 2026-07-11 Implemented MT-210 - `paper-candidate-replay` display-only rejected-candidate review. Each row shows current blocker, persisted holder bucket, evidence-based provider category when applicable, and a hypothetical threshold review flag. It explicitly states every candidate remains rejected and needs full re-evaluation; no trade, acceptance, or PnL action is possible. At 60%, recent replay shows three holder review flags alongside creator missing-field rows. Validation: 165 focused tests pass; attribution BLOCKED and live-readiness NOT READY.
- 2026-07-11 Recorded MT-209 post-provider blocker observation. Bounded persisted paper discovery scanned 25 signals, accepted 0, rejected 25, and created 0 trades. Fresh 50-row blockers: 33 holder failures (severe 28, moderate 3, near 2), 15 creator `missing_field` blocks, 2 liquidity `unavailable` blocks. 60% holder shadow yields 4 review-set candidates only; verified linked attribution samples remain 0. Paper/live state 4/0 with 4/4 high-confidence marks; live-readiness NOT READY. No forced trade, gate change, or PnL claim occurred.
- 2026-07-11 Implemented MT-208 provider coverage audit. `paper-blockers` now maps only explicit persisted provenance to evidence-based provider/path categories: `creator:missing_field` (20), `liquidity:unavailable` (1, Jupiter provider missing), and `liquidity:legacy_unknown` (1). Unsupported rate-limit, malformed, not-collected, and policy-unavailable labels are not inferred. The audit is paper-only diagnostic output; no gates, ranking, execution, PnL, attribution, or live behavior changed. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 MT-205 safe missing-data collection repair - BLOCKED. MT-203/204 identify liquidity unknown as `dexscreener=no_solana_liquidity; jupiter=provider_missing`, a provider/data availability condition rather than a proven local not-collected field. Repair would require a new fallback/provider behavior change, so no collection patch was made. MT-206 liquidity provenance review - PASS WITH NOTES: provenance is real, legacy absence remains unknown, and no unknown candidate was accepted. Safe next step is provider reliability/coverage work under a separately scoped task, not risk relaxation. Attribution remains BLOCKED and live-readiness NOT READY.
- 2026-07-11 Implemented MT-203 and observed MT-204 liquidity provenance. Rejected paper telemetry now persists scorer-provided liquidity unknown reason/source and `paper-blockers` aggregates it. Fresh bounded paper soak scanned 25 signals, accepted 0, rejected 25; real output shows one liquidity unknown as `dexscreener=no_solana_liquidity; jupiter=provider_missing`, plus one legacy liquidity unknown. This proves a provider/data availability cause rather than a safe not-collected local diagnostic gap. No gate, execution, PnL, attribution, or live behavior changed; validation 165 focused tests pass and live-readiness NOT READY.
- 2026-07-11 Reviewed MT-200/201 unknown provenance lane - PASS WITH NOTES. Fresh creator provenance is sourced from real scorer diagnostics and correctly identifies absent normalized creator holding across signal payload, RugCheck, and holder lookup. Legacy creator rows and liquidity rows without diagnostic detail remain explicit `unknown`; displays make no acceptance implication. No unknown row was accepted and no risk, ranking, sizing, execution, fill, PnL, attribution, or live-readiness behavior changed. Safe next step is improve liquidity/provider-specific provenance, not relax gates. Validation: 165/165 focused tests pass; attribution BLOCKED and live-readiness NOT READY.
- 2026-07-11 Recorded MT-201 unknown provenance observation. Fresh bounded paper soak after MT-200 scanned 25 signals, accepted 0, rejected 25, and created 0 trades. Real `paper-blockers` output shows 13 creator unknowns with persisted no-normalized-holding provenance, 8 legacy creator unknowns, and 2 liquidity unknowns with absent provenance preserved as `unknown`. No row became accepted and no risk/execution/PnL/live behavior changed. Attribution remains BLOCKED; live-readiness NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-200 - unknown blocker provenance persistence. Rejected paper telemetry now carries existing creator holding unknown reason/source and liquidity data state/source; `paper-blockers` aggregates provenance without changing policy. Fresh observation shows 13 creator unknowns with real reason `no normalized creator holding in signal payload, RugCheck, or holder lookup`; 8 legacy creator rows and 2 liquidity rows remain explicit `unknown` rather than fabricated. Final soak was paper-only (25 rejected, 0 trades); no gate, execution, PnL, attribution, or live behavior changed. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-197/198 next blocker tuning lane - PASS WITH NOTES. Unknown-data analysis remains factual (13 creator unknown, 2 liquidity unknown) and identifies a provenance limitation rather than claiming unavailable-vs-uncollected causes. Holder threshold shadow is explicitly HYPOTHETICAL/NOT TRADING: at 60%, 3 of 35 numeric holder failures require full review, not acceptance; unknown numeric rows are excluded. No approval, risk gate, ranking, sizing, execution, fill, PnL, attribution, or live-readiness behavior changed. Safe next step: persist provider/status provenance and collect more paper observations before designing any paper-only threshold experiment. Validation: 165/165 focused tests pass; attribution BLOCKED and live-readiness NOT READY.
- 2026-07-11 Implemented MT-198 - `paper-holder-threshold-shadow` paper-only hypothetical cap analysis. It evaluates only persisted numeric `top10_holder_check_failed` rows, excludes unknown values, and reports candidates at/below a supplied cap as full re-evaluation sets, never acceptances. Real 60% cap output: 35 numeric holder failures, 0 excluded unknowns, 3 candidates requiring review. Actual 50% holder gate, approval, ranking, execution, fills, PnL, attribution, and live behavior are unchanged. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 MT-197 unknown-data blocker analysis. Current 50 persisted decisions show 13 `creator_holding_check_unknown` and 2 `liquidity_check_unknown` blocks. Sample creator-unknown rows are pump.fun launch candidates with `metadata_completeness_state=partial`, `creator_policy_state=discovery_relaxed`, and other conservative unknown fields; source/mode are persisted. Telemetry truthfully identifies missing field/policy state but does not yet distinguish provider-unavailable from never-collected upstream input, so no availability-cause count is claimed. No gates, execution, PnL, attribution, or live behavior changed; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-194/195 holder-risk bucket lane - PASS WITH NOTES. Buckets are truthful display-only comparisons of persisted observed holder percentage to that row's actual applied threshold; missing/legacy values remain `unknown`. Real output explicitly states no bucket implies acceptance, and all observed bucketed records remain strict-rejected. No risk relaxation, acceptance logic, ranking, sizing, execution, fills, PnL, attribution readiness, or live-readiness behavior changed. Future paper-only threshold experiments can investigate new numeric rows only; live-readiness NOT READY. Validation: 165/165 focused tests pass.
- 2026-07-11 Recorded MT-195 holder-risk bucket observation. Real persisted `paper-blockers` output showed 35 holder failures split into unknown 19 (legacy/missing numeric telemetry), severe_fail 13, near_threshold 2, and moderate_fail 1. The output labels all buckets diagnostic-only/not acceptance, and existing decision rows remain strict-rejected. No paper run, forced trade, gate relaxation, position mutation, PnL, or live change occurred; attribution readiness remains BLOCKED and live-readiness NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-194 - paper-only holder-risk buckets in `paper-blockers`. Persisted holder percentage is compared only with the persisted applied threshold: `under_threshold`, `near_threshold` (<=10% over), `moderate_fail` (<=50% over), `severe_fail` (>50% over), or `unknown` when either value is unavailable. Output explicitly says buckets are diagnostic only and not acceptance. Current 50-row view: unknown 19, severe 13, near 2, moderate 1. No holder gate, ranking, sizing, execution, PnL, or live behavior changed. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-191/192 holder telemetry lane - PASS WITH NOTES. Fresh decision telemetry truthfully records scorer-selected holder concentration and the actual applied policy threshold (`100.0` vs `50.0` in the observed holder failure); legacy data remains missing/unavailable rather than backfilled. The initial rejected-row threshold propagation omission was fixed before observation completion. No holder buckets, risk relaxation, acceptance logic, ranking, sizing, execution, fill, PnL, or live-readiness behavior changed. Future threshold experiments can use new numeric telemetry only. Validation: 165/165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 MT-192 numeric holder telemetry observation - PASS WITH FIX. Initial fresh holder record exposed the observed value but not threshold because rejected-diagnostic propagation omitted the policy field; corrected that telemetry-only omission and reran a bounded soak. Fresh real `top10_holder_check_failed` record now persists `top10_holder_pct=100.0` and actual `top10_holder_threshold_pct=50.0`; unavailable values remain unknown/null. Final soak scanned 25 signals, accepted 0, rejected 25 (16 holder failures, 9 unknown-data blocks), with no execution change. Validation: 165 focused tests pass; paper/live 4/0 and live-readiness NOT READY.
- 2026-07-11 Implemented MT-191 - numeric holder telemetry persistence. Holder policy diagnostics now carry the scorer-selected top-10 holder percentage and the actual applied `RiskConfig.max_top10_holder_pct` threshold; paper decision telemetry persists both fields. Unavailable source values remain `None`; legacy rows remain missing/unavailable. Strict holder approval, ranking, sizing, execution, fills, PnL, and live behavior are unchanged. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 MT-189 holder-risk shadow buckets - BLOCKED. Near-threshold/moderate/severe labels require persisted numeric top-10 holder percentage and the threshold used; current records expose only pass/fail state. No bucket command was added because any classification would be invented. MT-190 observation - FAIL / unavailable output: existing blocker and shadow views truthfully report 35 holder failures but cannot show holder severity buckets. Safe follow-up is telemetry persistence for numeric holder values and policy threshold, not risk relaxation. Attribution remains BLOCKED; live-readiness NOT READY.
- 2026-07-11 MT-188 holder concentration analysis - BLOCKED by telemetry fidelity. Current persisted holder-failure decisions retain `holder_policy_state=fail` and `top10_holder_check_failed`, plus edge/attention/narrative/mode/source context, but do not retain the numeric top-10 holder percentage or applied threshold. The latest 50 rows show 35 holder failures (all paper pump.fun launch examples are thin/weak-identity with strict rejection), 13 creator unknowns, and 2 liquidity unknowns; this supports blocker counts only, not near/moderate/severe concentration claims. No gates or behavior changed; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-185/186 paper blocker analysis lane - PASS. Real persisted telemetry shows the current causes of zero paper acceptance, while shadow output is clearly HYPOTHETICAL ONLY and correctly reports re-evaluation sets rather than accepts, PnL, or trade recommendations. Strict approval, risk gates, ranking, sizing, execution, PnL, attribution readiness, and live readiness are unchanged. Next safe action is investigation of data availability and strict holder failures, not gate relaxation. Validation: 165/165 focused tests pass; attribution remains BLOCKED and live-readiness NOT READY.
- 2026-07-11 Implemented MT-185/186 paper-only blocker analysis. `paper-blockers` groups persisted rejections into strict risk failures and unknown-data blocks with safe examples; `paper-shadow-blockers` reports hypothetical review-set counts only and explicitly states it does not relax gates or create trades. Current 50-row sample: 35 `top10_holder_check_failed`, 13 `creator_holding_check_unknown`, 2 `liquidity_check_unknown`; shadow unknown review set 15, strict review set 35. No approval, execution, PnL, or live behavior changed. Validation: 165 focused tests pass; attribution remains BLOCKED and live-readiness NOT READY.
- 2026-07-11 MT-184 verified-link readiness observation - BLOCKED. One bounded persisted paper discovery soak scanned 25 signals, accepted 0, rejected 25, and created 0 trades/positions (15 strict risk failures, 10 unknown-data blocks; no capacity/source/execution failures). Exact attribution readiness remained 0 verified linked positions and 4 unlinked legacy/open positions; rejected/non-traded rows rose from 380 to 405. No trade was forced, no risk was relaxed, and no PnL was attributed. Paper/live state remains 4/0 with 4/4 high-confidence marks; live-readiness NOT READY.
- 2026-07-11 Implemented MT-182 - `paper-attribution-readiness` read-only report. It counts only exact accepted telemetry `trade_id` to open position `entry_trade_id` matches, separately reports unlinked legacy/open positions and rejected/non-traded rows, and explicitly blocks attribution when verified samples are zero. It never uses mint matching or attributes unlinked PnL. Current output: 0 verified links, 4 unlinked open positions, 380 rejected/non-traded rows, attribution BLOCKED. Validation: 165 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Completed MT-180 deterministic verified-linkage coverage. The accepted paper-cycle test now proves persisted decision telemetry `trade_id` equals both the simulated trade id and `Position.entry_trade_id`; the corresponding rejected record explicitly persists no trade id. This forbids mint-only attribution and leaves legacy rows unlinked. Validation: 165 focused tests pass; paper state remains 4/0 and live-readiness NOT READY.
- 2026-07-11 MT-176 verified trade identity observation - FAIL / insufficient accepted data. Bounded persisted paper soak scanned 25 signals, accepted 0, rejected 25, and created 0 trades/positions (20 strict risk failures, 5 unknown-data blocks). Therefore no new accepted position existed to prove the MT-178 persisted `trade_id` / `entry_trade_id` linkage; no trade was forced. Legacy rows remain unlinked. Paper/live state remains 4/0 with 4/4 high-confidence marks and live-readiness NOT READY. MT-177 attribution is blocked until a naturally accepted paper trade can be observed.
- 2026-07-11 Implemented MT-178 - accepted paper decision telemetry now persists the existing simulated `trade_id`, the same durable identity stored as `Position.entry_trade_id`. The field is populated only from accepted `decision.trade.id`; rejected rows and legacy records remain unlinked rather than receiving a guessed mint/time association. No risk, ranking, sizing, execution, fills, PnL, or live-readiness behavior changed. Validation: 165 focused tests pass; state remains 4 paper/0 live, live-readiness NOT READY.
- 2026-07-11 Reviewed MT-167/168 paper candidate watchlist lane - PASS. Real watchlist output is visible and truthfully summarizes persisted rows only, including actual appearance counts, recency, approval/reason, edge, attention, narrative, and source/mode context; it explicitly says those counts are not a trading signal. It remains paper-only and outside strict risk, execution ranking, sizing, fills, PnL, live-readiness, and strategy decisions. The observation made no live action or paper close. State remains 4 paper/0 live with 4/4 high-confidence marks; live-readiness NOT READY. Validation: 165/165 focused tests pass.
- 2026-07-11 Recorded MT-168 paper watchlist observation. A bounded persisted soak was run (25 signals, 25 rejected, 0 trades; 16 strict risk failures and 9 unknown-data blocks). Real watchlist output showed only persisted rows with actual appearance counts, latest timestamps, source/mode, approval/reason, edge, attention, and narrative; it explicitly states counts are not a trading signal. State remains 4 paper/0 live with 4/4 high-confidence marks; live-readiness NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-167 - read-only `paper-watchlist` grouped from persisted paper decision telemetry. It shows actual row appearance counts, latest timestamp, source/mode, approval/reason, edge, attention, and narrative under explicit paper-only/non-signal language; missing telemetry degrades safely. No schema, risk, ranking, execution, fill, PnL, or live behavior changed. Validation: 165 focused tests and compileall pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-164/165 paper candidate drilldown lane - PASS. Real persisted-candidate drilldown visibly shows normalized approval, rejection/block reason, edge, attention, narrative, source context, and warning diagnostics under explicit paper-only language. It exposes no raw payload or secrets and safely degrades missing fields; strict risk, ranking, sizing, execution, PnL, and live-readiness remain unchanged. The observation made no live action or paper close. Validation: 165/165 focused tests pass; live-readiness NOT READY. Next paper-only lane is safe.
- 2026-07-11 Recorded MT-165 paper drilldown observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 trades (14 strict risk failures, 11 unknown-data blocks; no capacity/source/execution failures). Real drilldown safely displayed a persisted candidate's approval, reason, edge, attention, narrative, source context, and diagnostic-only labeling; missing source count rendered `not-recorded`. Validation: 165 focused tests pass; no paper/live state changed and live-readiness remains NOT READY.
- 2026-07-11 Implemented MT-164 - `paper-drilldown --mint` read-only candidate view. It shows normalized safe persisted fields only: mint/candidate/timestamp, source/mode, reason, approval, edge, attention, narrative, source count, and warning count. Missing values degrade to `not-recorded`; no raw payloads, credentials, or secret fields are rendered. Validation: 165 focused tests pass and real candidate drilldown output is correct; risk/execution/PnL/live-readiness unchanged.
- 2026-07-11 Reviewed MT-161/162 filtered shortlist lane - PASS. Real persisted-telemetry filters and edge/recent sort outputs are visible, truthful, and paper-only; missing diagnostics safely normalize to `not-recorded`. No strict risk, strategy ranking, sizing, execution, PnL, or live-readiness behavior changed, and the observation made no live action or paper close. State remains 4 paper/0 live with 4/4 high-confidence marks; live-readiness NOT READY. Validation: 165/165 focused tests pass. Next paper-only lane is safe.
- 2026-07-11 Recorded MT-162 filtered shortlist observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 trades (15 strict risk failures, 10 unknown-data blocks; no capacity/source/execution failures). Real approval/edge and narrative/attention/recent filter-sort combinations returned only matching persisted rows with the paper-only diagnostic label. State remained 4 paper/0 live, 4/4 high-confidence marks, and live-readiness NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-161 - display-only `paper-shortlist` filters for approval, narrative, attention, and rejection/block reason, plus edge/attention/recent display sort. Filters operate solely on loaded persisted telemetry and missing diagnostics normalize to `not-recorded`; invalid sorts fail clearly. Strict risk, strategy ranking, sizing, execution, PnL, and live readiness are unchanged. Validation: 165 focused tests pass; filtered smoke output is correct.
- 2026-07-11 Reviewed MT-158/159 paper shortlist lane - PASS. Real `paper-shortlist` output visibly presents persisted candidates with truthful approval, rejection/block reason, edge, attention, and narrative fields under explicit paper-only/non-recommendation language. It remains a display-only sort outside strict risk, ordering, sizing, execution, PnL, and live-readiness paths; MT-159 performed no live action or paper close. Current state remains 4 paper / 0 live, PnL has 4/4 usable high-confidence marks, and live-readiness is NOT READY. Validation: 165/165 focused tests pass. Next paper-only improvement lane is safe.
- 2026-07-11 Recorded MT-159 paper shortlist observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 trades (16 strict risk failures, 9 unknown-data blocks; no capacity/source/execution failures). Real `paper-shortlist` output visibly displayed persisted candidates with approval, reasons, edge, attention, narrative, and explicit paper-only/non-recommendation labeling. Paper/live state remained 4/0; PnL has 4/4 usable high-confidence marks; live-readiness remains NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-158 - `paper-shortlist` read-only discovery research view. It sorts only loaded persisted telemetry for display by edge score and attention quality, and shows candidate label, rejection/block reason, approval, edge, attention, and narrative diagnostics under explicit non-recommendation language. Legacy telemetry degrades to `not-recorded`; the shortlist is outside risk and execution paths. Validation: 165 focused tests pass; current state 4 paper/0 live with 4/4 usable high-confidence marks; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-155/156 paper decision digest lane - PASS. Real operator output visibly presents a paper-only, non-recommendation digest that truthfully aggregates persisted approvals, narrative labels, attention quality, finite edge range/average, and the existing explicit rejection/block counts. The digest is read-only and did not change strict risk, ordering, sizing, execution, PnL, position state, or live readiness. Current state remains 4 paper / 0 live with 4/4 usable high-confidence marks; live-readiness NOT READY. Validation: 165/165 focused tests pass. The next paper-only diagnostics lane is safe to start.
- 2026-07-11 Recorded MT-156 paper decision digest observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 paper trades (13 strict risk failures and 12 unknown-data blocks; no capacity/source/execution failures). Fresh operator output displays the paper-only non-recommendation digest: approval (`35` strict rejected / `15` discovery relaxed), narratives, attention quality, edge average `25.5` and range `14-38`, plus explicit rejection/block counts. Paper/live state stayed 4/0; PnL has 4/4 usable high-confidence marks; live-readiness remains NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-155 - read-only paper decision operator digest. `paper-decisions` now summarizes persisted approval state, narrative category, attention quality, and finite edge-score average/range; existing Summary retains rejection/block reasons. Legacy fields count as `not-recorded`, and absent finite edge values render `not-recorded`. Output is explicitly paper-only and not a trading recommendation. Strict risk, ordering, sizing, execution, PnL, position state, and live-readiness are unchanged. Validation: 165 focused tests pass; current state 4 paper/0 live, 4/4 usable high-confidence marks; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-151/153 narrative label diagnostics lane - PASS. Fresh persisted real operator output displays `narrative=weak_identity` together with attention quality, edge detail, and strict/discovery approval under explicit diagnostic-only paper language. Labels derive from safe existing context and do not alter strict risk, candidate ordering, sizing, execution, or position state; the observation made no live action or paper close. PnL remains truthful with 4/4 usable high-confidence marks; state remains 4 paper / 0 live and live-readiness NOT READY. Validation: 165/165 focused tests pass. The next paper-only improvement lane is safe to start.
- 2026-07-11 Recorded MT-153 narrative label paper observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 paper trades (17 strict risk failures, 8 unknown-data blocks; no capacity/source/execution failures). Fresh `paper-decisions` visibly shows `narrative=weak_identity` alongside attention, edge, and approval; labels are explicitly diagnostic-only. Paper/live state stayed 4/0, PnL has 4/4 usable live marks and high confidence, and live-readiness remains NOT READY. Validation: 165 focused tests pass.
- 2026-07-11 Implemented MT-151 - compact persisted paper discovery narrative categories. Diagnostics now classify safe existing context as `composite_attention`, `migration`, `whale_activity`, `social_mention`, `weak_identity`, `launch_hype`, or `unknown`, and `paper-decisions` displays `narrative=<label>` alongside edge and attention under diagnostic-only language. Existing telemetry safely falls back to `narrative=not-recorded`. Strict risk, ranking, sizing, execution, PnL, paper/live state, and live-readiness are unchanged. Validation: 88 focused tests pass; live-readiness NOT READY.
- 2026-07-11 Reviewed MT-146/149 attention diagnostics lane - PASS. Fresh real operator output displays persisted `attention=thin` and `attention=mixed` labels with truthful safe component details, alongside edge and approval, below explicit paper-only operator/research-only language. Strict risk approval remains distinct and unchanged; ordering, sizing, execution, paper/live state, and PnL are unchanged. Current state: 4 paper / 0 live; 4/4 usable live marks, high-confidence PnL; live-readiness NOT READY. Validation: 181/181 focused tests pass. Next paper-only diagnostics lane is safe.
- 2026-07-11 Recorded MT-149 attention diagnostic paper observation. Bounded persisted soak scanned 25 signals, rejected all 25, and entered 0 paper trades (17 top-holder risk failures, 1 age failure, 7 unknown-data blocks; no source/execution/capacity failures). Fresh `paper-decisions` visibly shows edge, attention, and approval together, e.g. `attention=thin` for low evidence and `attention=mixed` for discovery-relaxed candidates, with truthful source/social/mode/weak-identity details. Paper/live state stayed 4/0; PnL marks are 4/4 usable, high confidence; live-readiness remains NOT READY. Validation: 181/181 focused tests pass.
- 2026-07-11 Implemented MT-146 - paper-only attention quality diagnostics. New deterministic `attention_quality` (`strong`/`mixed`/`thin`) and compact attention detail derive only from existing mode, source/composite, social/attention, weak identity, approval, and warning fields. They persist through existing decision diagnostics and render in `paper-decisions`; legacy rows safely show `attention=not-recorded`. The CLI explicitly labels edge and attention as operator/research-only. Strict approval, ranking, sizing, execution, PnL, and live behavior are unchanged. Validation: focused 165/165 and full suite 535/535 pass; live-readiness remains NOT READY.
- 2026-07-11 Reviewed repaired MT-143–MT-144 discovery edge diagnostics lane - PASS. `paper-decisions` now displays persisted `edge`/`detail` fields under an explicit paper discovery operator-diagnostic header, including both `approval=discovery_relaxed` and `approval=strict_rejected` examples. The repair did not change strict approval, candidate ordering, sizing, or execution; MT-144 did not touch live or paper position state. Current state remains 4 paper / 0 live with 4/4 high-confidence live marks, truthful historical unpriced rows, and live-readiness NOT READY. Validation: review suite 164/164 pass. Next paper-only strategy-improvement lane is safe; no live work is authorized.
- 2026-07-11 Recorded MT-144 repaired discovery edge operator observation. Bounded persisted soak collected 25 signals and entered 0 trades; 15 strict risk rejections and 10 unknown-data blocks were recorded with no capacity/source/execution failures. Real `paper-decisions` output now visibly shows `edge=38 detail=... approval=discovery_relaxed` and strict-rejected examples, under an explicit diagnostic-only header. Paper/live counts stayed 4/0; active PnL remains 4/4 live-marked high-confidence. Validation: focused suite 146/146 pass and live-readiness remains NOT READY. No live state was touched.
- 2026-07-11 Implemented MT-143 - fixed paper-decisions edge display gap. Persisted discovery diagnostics now render as compact `edge=<score> detail=<component list>` fields, prefaced by an explicit operator-diagnostic-only statement; legacy records degrade to `edge=not-recorded`. Real CLI smoke showed stored edge and strict/discovery approval state without changing risk, ranking, sizing, execution, or live behavior. Validation: targeted 164/164 and full suite 534/534 pass; live-readiness remains NOT READY.
- 2026-07-11 Reviewed MT-140–MT-141 discovery edge diagnostics lane - FAIL. Safety/PnL stayed correct: edge metadata is paper-only/research-only in code, strict unknowns remain unknown, 4 paper / 0 live state and 4/4 high-confidence live marks are preserved, and live-readiness remains NOT READY. Validation: focused 162/162 and full suite 532/532 pass. However, the real bounded MT-141 observation could not display `edge_score`/`edge_breakdown` through `paper-decisions --limit 50`; telemetry stored it but the operator report omitted it. Do not start the next strategy-improvement lane until a follow-up display fix makes stored edge diagnostics visible in the intended CLI review surface.
- 2026-07-11 Recorded MT-141 discovery edge observation - BLOCKED DISPLAY GAP. Bounded persisted paper soak collected 25 signals, accepted/entered 0 trades, had 20 risk rejections (top10 holder), 5 unknown-data blocks, 0 capacity blocks, and no source/execution failures. Paper/live state stayed 4/0; active PnL remains 4/4 live-marked high-confidence. Edge scores were persisted in decision telemetry but `paper-decisions --limit 50` did not display the compact edge diagnostics, so the required real operator observation could not be confirmed. Per task escalation, no display patch was made in this observation task. Validation: focused discovery suite 162/162 pass; live-readiness remains NOT READY.
- 2026-07-11 Implemented MT-140 - paper-only discovery edge diagnostics. `edge_score` and compact `edge_breakdown` explain existing discovery candidate context (composite/source count, candidate mode, social/attention, weak identity, existing risk warnings, and discovery-relaxed state) without changing strict approval, risk checks, sizing, execution, or ranking order. Output and paper telemetry label the data diagnostic/research-only. Validation: focused 162/162 and full suite 532/532 pass; live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-11 Reviewed MT-137–MT-138 priced paper mark/close lane - PASS. MT-137 observed 5 priced paper positions without mutation. MT-138 previewed and closed exactly one paper-mode row with its verified live mark; no live row was selected or touched. Current state: 4 open paper / 0 live, 6 closed paper, realized `-0.035045 SOL`, unrealized `+0.001639 SOL`, active coverage 4/4 high-confidence marks. Historical unpriced rows remain unavailable/low-confidence as intended. Validation: focused close/state/cycle suite 134/134 pass; live-readiness remains NOT READY. Next paper-only feature lane is safe; no live work is authorized.
- 2026-07-11 Recorded MT-138 safe priced paper close validation. Selected paper mint `5QxWa9G3VAcUfb6A...` (priced_quote, positive entry/token data), previewed with a valid live mark, then closed only that one paper row using `paper-close --mint ... --use-mark`. Preview estimated and close realized `-0.035045 SOL`. State moved from 5 paper / 0 live to 4 paper / 0 live; reports show 6 closed rows, remaining open `priced_quote=4`, 4/4 usable marks, high-confidence unrealized `+0.001639 SOL`, and truthful historical unpriced rows remain unavailable. Focused safety tests 84/84 pass; live-readiness remains NOT READY.
- 2026-07-11 Recorded MT-137 time-separated priced paper PnL observation. Read-only checkpoint: 5 open paper / 0 live, all active rows `priced_quote` (0 unpriced / 0 legacy), 5/5 usable live marks, high-confidence simulated unrealized PnL `-0.033406 SOL`; no-mark mode correctly remains unavailable/partial. All five rows are aging and only render paper-close preview suggestions. Five old closed unpriced rows remain visible as truthful unavailable history. No state was mutated, no paper soak/close ran, and live-readiness remained NOT READY. Validation: `test_paper_pnl.py` + `test_cli_paper_cycle.py` 118/118 pass.
- 2026-07-10 Post-remediation MT-136 safety audit - PASS WITH NOTES. (1) Public direct Jito submission fails closed; guarded adapter transport runs only after breaker/guardrail/preflight/config/daily-cap checks. (2) Position identity is mode-aware, including same-mint rows; paper-close selects paper only and live-exit requires live only. (3) DexScreener requires requested-mint/wSOL orientation and finite price; missing/no-mark/unpriced/partial-close paths preserve unavailable or remaining-basis truthfulness; live buy stores no fabricated fill. (4) Discovery relaxation is explicit research metadata, strict unknowns stay unknown, and quote output is not liquidity. (5) Daily caps fail closed without fresh state, canonical env names and diagnostic redaction are aligned, invalid marks are rejected. Validation: 529/529 tests and compileall pass; Ruff unavailable. Runtime remains 5 paper / 0 live with 5/5 mark coverage and live-readiness NOT READY. Remaining notes: private guarded Jito transport remains conventionally internal (underscore) and live mark collection remains sequential. Next paper-only feature lane is safe; live trading remains NOT READY until explicit future arming and operational state are supplied.
- 2026-07-10 Implemented MT-135 - enforced live daily caps and hardened diagnostics. Live submissions now require an injected fresh `DailyLiveState`; missing, stale, malformed, exhausted trade-count, or exhausted loss state fails closed before Jito/RPC submission. Canonical cap env names are `MAX_LIVE_DAILY_TRADES` and `MAX_LIVE_DAILY_LOSS_SOL` across runtime, readiness, CLI hints, and runbooks. RPC labels strip Basic Auth; provider/RPC/Jito exception diagnostics are sanitized; invalid `paper-pnl`/`paper-report --marks` values are rejected. Validation: targeted suite 125/125, full suite 529/529, and compileall pass; Ruff remains unavailable. Live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-10 Implemented MT-134 - separated discovery ranking from strict risk approval. Unknown creator, buyer, authority, and honeypot checks remain `UNKNOWN`; paper discovery attaches explicit `discovery_relaxed` / `risk_approval_state` metadata instead of converting missing evidence into strict PASS. CLI/telemetry carry that state for candidate review. Jupiter swap `outAmount` is no longer treated as pool liquidity; only explicit liquidity data is accepted, otherwise the check remains unknown/conservative. Validation: targeted risk/liquidity/cycle/strategy suite 147/147 and full suite 521/521 pass; paper decisions remain diagnostic-only and live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-10 Implemented MT-133 - hardened price normalization and PnL truthfulness. DexScreener only accepts finite requested-mint / wSOL base-oriented pairs; USDC-quoted, inverted, ambiguous, zero, NaN, and infinite prices become unavailable. Paper adapter/PnL reject non-finite values; bulk paper close skips no-valid-price positions instead of fabricating zero-PnL closures; final close uses remaining token/cost basis after partial exits; and live buy no longer persists invented `price_sol=1.0` or token quantity after submission acknowledgement. Added price, no-mark close, partial-close, and unpriced live-buy regressions. Validation: targeted 109/109 and full suite 521/521 pass; live PnL remains 5/5 covered for current valid SOL pairs and live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-10 Implemented MT-132 - mode-aware paper/live position safety. `PositionManager` now keys cache entries by `(mint, mode)` and mode-aware lookups/close/partial-exit/exposure calls preserve same-mint paper and live rows independently. Paper cycle capacity ignores live rows; `paper-close` explicitly closes paper rows only; guarded live buy duplicate checks and live exit selection/closure require `mode=live`. Added regressions for same-mint coexistence, paper-only bulk close, paper-capacity isolation, and live-only exit selection. Validation: focused suite 130/130 and full suite 515/515 pass; paper-state remains 5 paper / 0 live and live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-10 Implemented MT-131 - guarded direct Jito transport submission. Public `JitoBlockEngineClient.submit_bundle()` now returns `direct_jito_submission_blocked` without an HTTP request; only `JupiterLiveExecutionAdapter` uses the private transport method after its circuit-breaker, live-guardrail, preflight, and execution-config checks pass. Added regression coverage proving raw Jito calls are blocked with no fake HTTP call while guarded adapter tests retain injectable fake-client coverage. Validation: targeted Jito/guardrail/readiness tests 39/39 and full suite 512/512 pass; live-readiness remains NOT READY. Live stays disabled/default paper; no private key used.
- 2026-07-10 Full code audit after MT-127–MT-129 - FAIL. Full report: `/mnt/c/Users/Big A/custodian-shared/memecoin-trader/code-audits/full-code-audit-after-priced-paper-wave.md`. Validation: 512/512 tests and compileall pass; Ruff unavailable (not installed). Paper runtime is internally consistent (5 active `priced_quote` rows, 5/5 live marks, high-confidence simulated PnL, 0 live positions, live-readiness NOT READY), but audit identified critical blockers: direct Jito submission bypasses live guardrails, same-mint cache collisions can let paper bulk-close affect live rows, live exit does not enforce `mode=live`, and DexScreener `priceNative` lacks pair-orientation/quote-asset validation. Additional high findings cover risk unknown relaxation, liquidity fallback, PnL close accounting, daily live caps, and redaction. No new feature or live lane is safe to start; remediation-only work is required.
- 2026-07-10 Reviewed MT-127–MT-128 fresh priced paper-entry observation - PASS WITH NOTES. MT-127 used guarded CLI preview/confirmation to close only 5 stale paper rows; 0 live positions existed or were touched. MT-128 then created 5 fresh persisted paper positions, all `priced_quote`, each with positive `entry_price_sol` and `token_amount`; the active set is 5/5 live-marked with high-confidence simulated PnL (currently `-0.035045 SOL`). Historical closed unpriced rows remain truthful and unavailable for PnL; no missing mark was converted into a fake entry. Review of provider, paper adapter, position manager, PnL, and CLI paths confirms missing/invalid quotes degrade to `unpriced`, live mark commands are read-only, paper cycle force-applies paper settings, and no live guardrail or risk-check behavior changed. Validation: full suite 512/512 pass; paper-state shows 5 open paper / 0 live; live-readiness remains NOT READY. Recommended next lane: paper-only time-separated mark and exit-observation reporting. Live remains disabled/default paper; no private key used.
- 2026-07-10 Recorded MT-128 fresh priced paper-entry observation. Starting state after MT-127: clean git, 0 open paper / 0 live, and live-readiness NOT READY. Ran `paper-soak --max-signals 25 --timeout-seconds 120 --persist-positions`: 25 signals collected, 5 accepted, and 5 persisted paper trades entered; two later candidates were capacity-blocked after the cap filled. All 5 fresh open positions are `priced_quote` (`unpriced=0`, `legacy_unknown=0`) with positive entry prices and positive token amounts, so none has `tokens=0` or `entry=0`. `paper-pnl --marks live` found 5/5 usable live marks with high-confidence, meaningful simulated PnL coverage (currently +0.000000 SOL because marks matched fresh entry quotes); unavailable-marks mode remains partial/unavailable by design. Decision telemetry recorded 5 traded candidates. Live-readiness remained NOT READY, 0 live positions were touched, default mode remained paper, and no private key was used. Validation: `tests/test_execution.py tests/test_paper_pnl.py tests/test_cli_paper_cycle.py` 116/116 pass. Real-runtime priced-entry observation: PASS.
- 2026-07-10 Recorded MT-127 stale unpriced paper-capacity clearance. Starting state: clean git, 5 open paper positions / 0 live, all five `unpriced` with zero entry/token data, and live-readiness NOT READY. `paper-close --all --preview` targeted exactly those 5 paper rows and explicitly showed 0 live positions untouched. Confirmed through `paper-close --all --confirm`: closed 5 simulated paper positions, with 0 live positions touched. Final state: 0 open paper / 0 open live, 5 closed historical rows, 5 archived exclusions, 0 deployed SOL, and high-confidence report coverage because no open positions remain. Validation: `tests/test_paper_pnl.py` 63/63 pass and `tests/test_cli_paper_cycle.py` 50/50 pass. Live remains disabled/default paper; no private key used.
- 2026-07-10 Reviewed MT-123–MT-125 priced paper PnL lane - PASS. Review of `.gitignore`, `src/execution/paper.py`, `src/cli.py`, `tests/test_execution.py`, and the MT-125 checkpoint commit found no safety regressions. The new paper quote path does not fabricate prices: it only uses `PriceProvider` output when a provider returns a positive quote, otherwise it safely falls back to static test lookup or preserves `unpriced` behavior. Invalid or missing prices still degrade to `price_sol=None`, `token_amount=0`, and truthful `unpriced` / unavailable simulated PnL. `priced_quote` remains the only reliable fill-quality path, unpriced entries still block unrealized PnL, live positions remain untouched, and no live guardrails or risk checks changed. `shared/` is now ignored repo-locally. Validation: full suite `512/512` pass, `paper-state` still shows only 5 open paper positions / 0 live, `paper-pnl --marks live` and `paper-report --marks live` remain low-confidence only because the persisted open positions predate priced-entry capture, and `live-readiness` remains NOT READY. Result: PASS. Next paper-only feature lane is safe to start. Live remains disabled, default mode paper, and no private key was used.


## Known Issues

- MT-130 remediation tasks MT-131 through MT-135 are implemented: direct transport is guarded, state identity is mode-aware, price/PnL truthfulness is hardened, discovery semantics are separated from strict approval, and daily caps/diagnostics now fail closed and redact credentials. MT-136 post-remediation audit remains required. Do not enable live trading or relax any guardrail until that audit passes.
- All three protocol-based providers are now implemented and healthy when env config is present: `transaction_simulator` (available), `wallet_balance_lookup` (provider reached, wallet unfunded — insufficient balance), `wallet_holdings_lookup` (available). Position reconciliation passes (`no_live_positions_to_reconcile`) — paper-mode positions no longer trigger false mismatches. The remaining blocker before a guarded micro-live smoke test is live arming config: `TRADING_WALLET_PRIVATE_KEY`, `LIVE_TRADING_ENABLED`, confirmation phrase, `LIVE_KILL_SWITCH`, tiny live caps, RPC URLs.
- Live Jupiter execution is still intentionally disabled for direct swaps; the serialized submission helper only wires optional Jito/RPC routing for future guarded use and does not enable trading by itself.
- Whale tracker is polling-only for now; webhook ingestion still needs a public callback endpoint and receiver.
- pump.fun websocket token-creation and migration subscriptions are now verified against PumpPortal, but the short dry-run did not capture a live migration payload; graduation normalization is still proven by offline fixtures rather than observed runtime traffic.
- Risk checks use conservative local token fields until on-chain enrichment is implemented.
- Dynamic exits are only active when `exits.dynamic_exits_enabled` is enabled and the exit runtime receives `DynamicExitState` context.
- Latest integrated paper-cycle runs still produce 0 strict paper trades, and MT-054 confirms holder concentration remains the dominant strict-mode blocker even after the narrow pump.fun bonding-curve artifact fix; the current recommendation is to treat further holder work as a parallel calibration lane while attention/narrative scoring becomes the primary lane for ranking discovery candidates that now make it through the warning-policy stack. MT-055 now makes capacity blockers explicit and adds a fresh-session paper-only evaluation mode when older persisted paper positions would otherwise mask that ranking signal.
- Liquidity-aware sizing remains disabled by default; operators must opt in through `position.liquidity_sizing_enabled` before the new conservative liquidity caps affect decisions.
